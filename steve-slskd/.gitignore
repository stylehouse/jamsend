# Secrets — slskd.yml holds your Soulseek + web-UI passwords, .env holds host paths.
.env
config/slskd.yml

# slskd runtime state & logs (regenerated). NOTE: config/data holds your favourited
# users and share cache — back the folder up with rsync/tar, just don't git it.
config/data/
config/logs/
config/*.db
config/*.db-*
