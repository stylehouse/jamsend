<script lang="ts">
    // /BigShapeland — the SHAPE room's route (V/BigShapeland.svelte), third sibling to
    //  /BigSoundland (music) and /BigWordland (code).  Its whole job: somewhere definite to LOOK at
    //   whatever the glass can currently do, without knowing a Book name.
    import BigShapeland from "$lib/V/BigShapeland.svelte";
</script>
<BigShapeland />
