# Backbone_plan.md — retired to `history/` (2026-07-07)

> **Retired.** This was the build-order that fused the attention layer's three organs — the Keep
> (persistence), Interest (the channel), and Lens (the UI) — into one spine, P0–P7. **P0–P6 landed.**
> Full plan at [`history/Backbone_plan.md`](history/Backbone_plan.md). The living homes it built out are
> `Keeping_spec.md` (persistence), `Interest.md` (the channel), and `Lens_posable_TODO.md` (P5, the
> Keep↔Lens weld). See memory `[[keeping-phase1-kindtable]]`.

## Still-owed residuals (lifted out so they don't archive silently)

- **P4 — THE CUT: LIVE acceptance owed** (`:9091`, owner): focus moved to B *stays* B (no boomerang),
  then drop the conservative guard to an unconditional write.
- **OWED — fleet re-record** of the feed (`req:Langoer` + Cursor Langos) once a runner sits stable/idle.
- **P4 residual (iv):** the emit half landed (2026-07-01), conservative guard kept — live-eyeball owed.
- **P7 — `%Spotlight` dissolution:** deliberately a separate, later cut; never folded into Keeping.
