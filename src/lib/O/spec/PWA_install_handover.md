# PWA_install_handover.md — retired to `history/` (2026-07-07)

> **Retired.** The install path (manifest / service worker / iOS meta / MediaSession) is built; the only
> thing left is an infra gate — an HTTPS phone origin + `ALLOWED_HOSTS` — not design work. Full text at
> [`history/PWA_install_handover.md`](history/PWA_install_handover.md); device-verification status in
> memory `[[pwa-install-built]]`.
