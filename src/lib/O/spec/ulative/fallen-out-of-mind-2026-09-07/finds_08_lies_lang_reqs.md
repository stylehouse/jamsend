# Finds 08 — the Lies/Lang req machine, for the music side (reader 8)

1. Coding_guide §compile→rerun chain — the canonical all-holds chain: "Every link holds the snap (an unfinished req or a ttlilt), so the snap can never catch a half-done state. A missing hold anywhere in the chain = a race." edit → req:text_mutated re-arms req:compile → e_Lies_compiled un-finishes req:Codebit → Codebit finishes with new dige → req:Rundown computes a moment → req:BlatDo fires + ttlilt → run lands → %ran → BlatDo reaped. = the template for a landed network ask: it holds from the causing frame until the result lands. Walking the chain IS the attention.
2. Coding_guide §Wake ≠ Hold — RULE (LakeTiles 2026-07-01): "arm the hold synchronously, at the cause — never in a later gate you reach via a wake." Flaky-test smell = missing hold.
3. history/Reqdrop §0 — owner 2026-07-14: "transient reqs are scaffolding, not ledger — once a %req has SERVED, drop it; leave in the snap only the reqs whose in-flight state is worth SEEING." The gem = Repli.g:549 model: a LANDED req:awaitbuf drops the moment its bytes attach; an UNLANDED one stays because its in-flight state IS the warn-if-late reconciler. Drop at an owner-seam after all_finished() consumed, never in finish(). ⚑ stage 2 parked by verdict; the MusuHeist "blocker" was a MISDIAGNOSIS.
4. Network_procedures §req lifetimes — eternal (standing watcher) · permanent (one-per-ghost, owns a write) · transient ("an owner DROPS its finished transient reqs"). Hold from the causing frame when the RESULT must be in the fixture; a bump suffices when it's only UI.
5. Backpressure §4.3 RULED: "req:Land should be a plain unfinished req, not a ttlilt: unfinished while the writer works, finishes when close() + read-back gate pass." req:Ask's retransmit wants NO timer primitive — stays unfinished while any hole is outstanding, the ambient beat re-drives it (§7.1); req:Land beside req:Ask at the same maz (§7.2).
6. Error_channel_todo — faults as legible matter, built and gated. Human 2026-07-29: "I totally need Errors|warnings to notice… Story should really have an error channel that should be empty but is UI'd as such." Lazy-mint (clean run = zero bytes); home on the Run world w; ring-then-drain-at-the-snap-seam; errors gate red, warnings show.
7. ulative/memory-raw/self-healing-sweep-2026-07-30 §7 — contract "errors thread back through Codebit%of_dock" silently violated: a failed disk write only console.error'd ⇒ req:Codebit PARKED FOREVER. Fix: the error branch ALSO hands off. A fault must travel the same rail as a success, or the hold becomes a hang.
8. Springcore_meander §"work is a tree of held breaths" — "the taper… is a kind of ttlilt of attention… Focus is a req the eye arms." "a %req is a verb caught mid-motion"; sc.ok re-arms each pass — "the world refuses to remember it was fine yesterday."
9. Lies_handover thesis + §7 (P7) — attention is ONE position in one Waft, today smeared across "some twenty homes… all workaround for Wafts that were never handed an Interest to hang on." ⚑ Frontier:122 still [OWED] P7 collapse-the-cursor; §8's seven owner decisions unticked.
10. Supervisor_todo §4 CORRECTION — the beliefs-mutex ruling: "a req runs in reqy(w).do() → the belief pass → under the beliefs mutex. So a req-based supervisor is queued behind the very wedge it exists to detect… The req idiom is right for state that must persist and be seen; it is wrong for the detector itself." + §8 owner: recoverability depends on WHO IS WATCHING — a runner reloads unasked, a listener is always offered.
11. Keeping_spec — req:Keeping proposed as one driver, BUILT DISTRIBUTED: "the CONCERN; realised across the homes below — not one particle." "Keeping owns 'which'; convergence owns 'tell the view'." Non-goal: in-flight edits don't resume. ⚑ #7 one locator/resolver (three drifting copies) and #8 rename-caretaking flagged, not done.
12. LiesEnd_spec — %push_dirty is the FAULT PARTICLE; a fault is a named state, not a log line. ⚑ FALLEN OUT: "push_dirty not yet wired to a req fault particle in the reqy system" + the vanish fix — "Needed before unaccepted is usable in production."
Also: Hovercraft.design §9 ("req stops naming a subsystem and names the transient level of one unfoldment"), §3 (capture ≠ work), §7 Subscriptions — designed, untouched: one %subscribe{target,on,wake} unifying Stuffing/watched/%Good, "with req/%ttlilt as the backpressure a slow consumer arms." Music side already reaching back: "prefer a req over a status string; it carries its own liveness" (Radio_todo:1934; SoundPooling §3.1's permanent req:PoolSteward).
