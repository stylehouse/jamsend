# Finds 09 — the render substrate: Cyto / Cytui / Voro / Vyto / Matstyle (reader 9)

MAP: Cyto.svelte (model side: commission, cyto_scan mirrors the C tree into cyto_nodes with .c.source_n backlinks, make_wave diffs old/new into a WAVE) → Cytui.svelte (cytoscape renderer + HTML overlay system keyed by node id, the voronoi glass, the vlog telemetry ring) → Ghost/V/Voro.g (the crush: which subtrees become one cell; the drift/radio attention tick; snapped w:Voronoiology) → Vyto.g + Vytui.svelte (the reborn glass: own solver/foam-pile geometry, cells as real SVG+DOM, faces as children; organs Scan·Fold·Gang·Slope·Mesh·Calm·Focus·Relate·Express·Spool) → Matstyle.svelte (mainkey → auto-swatch; meta:dose drives:size = the one live quantity→channel binding) → glass_kinds/glass_faces (a particle gets a face WORN (sc.face) or IMPOSED (FACE_MAINKEYS), mounted with {n, H}).

1. Vyto_todo §0.0 THE LOOK — binding ruling: understanding arrives as RELATIONS AND MOTION, never new boxes. "tiny ants moving buffer into the Record there"; rules out "answering 'the glass should show X' by adding another cell with another readout." (b) HALF-BUILT: top_House().c.xfer.pulls (object keyed by id8) carries held/total/goodput_kbps/asked/landed + want-first/page-first ring marks — per-Record cursor+gap data, thrown away at the face boundary. FLAG: every later round added faces/HUDs anyway.
2. Vyto_spec §4 focus engine — explanation SPECIFIED AS CHOREOGRAPHY: "the involved cells move through intermediary visuals that explain the outcome… The viewer should be able to say afterwards how things ended up where they are." "One truth, two renderings." An overlay in LANGUAGE = the missing second rendering of the same spool diff.
3. Vyto_spec §9 the board + Situations — explain-layer vocabulary exists: each organ one sentence reads→decides→writes; a %Situation is "a standing sentence about a state of the glass worth noticing", minting a %Sighting pinning evidence. FLAG: Vyto_todo §THE CHROME CULL deleted the bar/organ panel — "live|depths|flows I never found out what any of that did anyway" — culled for being UNEXPLAINED, not unwanted. %Bar/%Organ survive (status:stub).
4. Voro_render_faults §0.1 — the render telemetry film strip exists, CLI-only: vlog ring (wave/armed/morph/settle/flood/diag, dt-stamped) via runner_shot --why/--svg/shot. "the render telemetry isn't a nicety; it's the flashlight for the half that can't snap." "A sound instrument pointed at a corpse is indistinguishable from a broken instrument." (a) the Book-explainer's data source is BUILT — never on screen.
5. vyto_workingouts/spool.md §7 + Vyto_spec §8 — the strip's data contract fully specified sc-side (yore_n·step_n?·dige·o:1?·bless·forced:1? hollow pips, %Seek.open_at), two clocks, ring ~60. FLAG: the strip face was culled; "Moments, still spooled model-side." = the ready-made spine of "see a Book run".
6. Vyto_todo §VYTO REQUESTS #1 — owner's headline: "A timeline face — step around in time (the human's headline ask)… an ongoing Story, not just the live frame." Unbuilt.
7. Vyto_sizing §5 attention taper — size(t) = S·φ(importance)·ψ(dist_from_focus) + haze: "Near/attended = big · warm · sharp; far/unattended = small · blue-grey · soft." Seeing attention needs no new cells — a taper on the flat glass; named the cheapest prototype.
8. Voro_todo roster #6/#29 — %req ruled on twice: "swarms of %req:awaitbuf and kin escape the fold and litter the graph. Fold them anyway, as GROUPS" and "I want all ugly bits of eg %req included, no hiding stuff". Noisy mainkeys gang at min 2; over:N marks the crowded-out tail. A %req-stack visual = group + counted overflow, not a cap.
9. Voro_vtuffing §📻 the radio — attention as a SUPPLIED SERVICE, built: age / choose / open-a-little / glide, stamps drift_focus, popped_auto, dwell ~7s. Read those stamps for (c).
10. Vyto_todo §ORCHESTRA OF SPHERES — "every particle is a POOL… COVERAGE IS EARNED BY PRESSURE, not granted by the frame." "A wire gaining weight REELS it in — you see a fact join the pile." foam_cells/pile_step in vyto_geometry.ts, node-tested.
11. Vyto_perf §1-4 + Vyto_todo §0.2(c) — cost warning: face-mold left/top/width/height % rewritten per faced cell EVERY FRAME — "the item a phone actually pays for"; plug_of restarts every <animateMotion> ant per frame. Ants must be SVG/transform, driven at settle.
12. Voro_svg_stuffing — the unbuilt SVG row layer with stable rid identity, monospace metrics, translate-only ~200ms tweens between settles. = the correct substrate for a per-record cursor/gap row; <text> greppable by runner_shot --svg.
13. Vyto_client §9 + Vyto_todo §THE SPLIT — "runner_shot screenshots Cyto's canvas only… a Vyto frame has no screenshot path" (an overlay on Vyto is unverifiable today); owner's unbuilt ask "some overall sanity checking thing going on" — ONE cell that speaks when something is wrong. The split rules an explaining overlay is JOB 2 (the application model dumper), not a renderer capability.
