# Finds 04 — the wire: Repli / Peeroleum / the %req spine (reader 4)

1. Repli_design §0/§4 — THE HONEST VERDICT, pre-written: Repli is a Seem-driven particle replicator that half-uses the machine's own awareness primitive. "All three are the SAME gap seen from different sides — Repli half-uses the machine's awareness primitive and hand-rolls the other half." The three: no i_Seem/o_Seem; the Seem is blind below the particle; key-memory is a .c.* smear that dies on reload. Sound spine, hand-rolled organs. §2a is genuinely Seem-shaped — "Nobody calls 'offer this record'. A record appearing in a library IS the offer" — hackiness localised, not systemic.
2. Repli_design §2b — "ABSENCE IS NOT DELETION… a key cleared at the source stays set on the mirror forever." Ruling §6.4 owed since 2026-08-05, never taken. THE sharpest app-facing contract: apps can never un-set a key over the wire. FALLEN OUT OF MIND.
3. Repli_design §9.5 — "presence IS fill state"; %Fill is a tombstone (Float32 staging only). Gap visual reads chunk-particle presence, not a counter; %Fill = transport-internal legacy, not an app contract.
4. Repli_design §9.4/§9.6 — a replicated particle needs a stated OWNER per key: "which side writes this, and what happens to the other side's copy when it lands?" (%Fill.have carried two meanings; fix tried + reverted same day). Owner: "there's only one of anything." THE single best rule for the frontier.
5. Backpressure §3.1b — the unit of asking is a PAGE, so "do I still need this" must be page-granular: "the loop then reads the page as held and never asks again … a permanent, invisible hole." Fix = Ra_page_hole. "held" ≠ "covered".
6. Backpressure §3.1c — "a park is a contract, not a hint"; "do not let a high-water mark answer a question about coverage." Grep heuristic: "grep for the pattern before you grep for the symptom: a high-water cursor (sent, have, frontier, last_asked) being asked a question about coverage. That single shape has now produced three distinct bugs in two days."
7. Backpressure §0 (2026-08-28) — the MISSING STATE, not a cursor: "the sink has landed and not-landed but no arrived-but-not-yet-drained." Flood = re-ask duplication inside a bounded window (2050 inbox vs 32-page window). A "where is reliable" visual needs THREE states minimum. Fix gated, unverified live. FALLEN OUT OF MIND.
8. Backpressure §0 — the charter for the visualisation ask: "legible matter — C particles under the Pier, not closure locals — so the loop is ordinary C ops, Story can %see it, and Vyto can render the convoy instead of us grepping ☠. One representation serves the loop and the view." NOBODY BUILT IT.
9. Backpressure §3.2/§5.3 — "Implicit inference from timeout is the slowest, most ambiguous signal available; an explicit one-bit message beats it every time." repli_parked landed 2026-08-06. FRAME RELIABILITY POLICY: "a frame that opens a door (a handshake) or carries pushed app data with no re-ask behind it is RELIABLE; a frame that is gossip, a beacon, self-re-asking — or the RESPONSE to a self-re-asking pull — is EPHEMERAL." §9.1: a new frame type must join BOTH the ephemeral set and Tribunal's ambient map — "Neither is optional, and neither will fail loudly."
10. Peeroleum_spec §1/§3 — the %req spine: "Every piece of a peer's state is a particle … Every message — hello, trust, data, binary — is one signed envelope. Reqs are unified on %req … Observation queries particles, not booleans." Owner (Backpressure §7.5): "req can take the business end of a lot of the model." Split = PRESENTABILITY. §5.4's req refactor never landed.
11. Peeroleum_spec §5 — the relay's own-door rule: "Browsers talk only to their own-origin /relay"; "loop-safety is structural, not a flag … deliver-local-or-drop, never re-forwarded."
12. Repli_idspace §1b/§2 — id-space partitioned by (peer.pub, disk layout): keep-ids vs radio-stock ids never intersect. repli_no_idspace BUILT but default-OFF, live-verify pending since 2026-08-30 + MusuIdspace Book. FALLEN OUT OF MIND.
Also settled-then-ignored: §3.4 owner ladder "prioritise the current track over the potential next track, unless we're >16s ahead" — "closed 2026-08-08 … HEARD BY NOBODY"; §6.2 sphere ruling (K**/O**, "I have literally lost what the plot of that was") blocks §5's rewrite; MusuNeGrind = the missing test LEVEL.
