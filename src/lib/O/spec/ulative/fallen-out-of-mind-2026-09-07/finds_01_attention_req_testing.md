# Finds 01 — attention / reqs / testing / visuals (reader 1)

1. Backpressure_todo §4.2 + §7.1 — "the beat as clock not worker". The %Heist req pile: req:Census / req:Ask,id / req:Land,id under one eternal pump. "No new timer, no new loop; the beat stops being the worker and stays the clock." RULED 2026-08-05, NOT BUILT. §7.1 RULED away the retransmit timer: a req:Ask stays unfinished, the ambient beat re-drives it — "there was never a gap." A plain unfinished req, never a ttlilt ("a deterministic hold beats 'hope N seconds is enough'").
2. Backpressure §7.5 — "RULED — the req pile owns it": "req can take the business end of a lot of the model — it is a better place for state than a string on a particle". Split criterion = PRESENTABILITY: what the human looks at stays on the particle the face dresses; "req is the less user-presentable side of things."
3. Social_demarcation §7 — line of reality; prescription already written (move one Book's line DOWN: real rummage + real Repli). Unexecuted (the fork is doing it).
4. Social_demarcation §4.1 — a reach must be able to die; a landed ask-as-req must carry state:dead, why:.
5. Datalayer_todo §1 — owner's question answered: yes, pile is the shape, pump the verb. "A req is a proto-w — lighter and curlier, that does its work and finishes rather than persisting". §3.2 records a corrected wrong "no action" verdict that cost a live crash.
6. history/Reqdrop_todo §0 — reqs as attention: "leave in the snap only the reqs whose in-flight state is worth SEEING" (human 2026-07-14). PARKED by verdict — but the SELECTIVITY principle is what keeps a pile legible.
7. Story_hygiene §0a.1 — owner overturning the analyst: "we probably need more ttlilts not less — Heist might have way more flaps with less ttlilts". Quiescence is a 75ms idleness heuristic, not causal; "a %ttlilt is the only thing that makes the wait causal."
8. Story_hygiene §0 — "A VERIFICATION SWEEP CANNOT CURRENTLY BE TRUSTED, and it fails SILENTLY GREEN"; owner: "please push a TODO somewhere for that nondeterministic runner problem."
9. Hovercraft.design §2/§3/§9 — "beliefs() is the walk, and req** are just more legs"; "req is beliefs() run at the transient end"; LAW: capture (lexical) and work (maz) never merge — a churn visual must render the CAPTURE walk, not pump order.
10. Story_future §8.3 — "the snap is the control panel": fold the wake into the ttlilt so every pending impulse is a particle (snap/diff/surf; dropping the req subtree tears the timer down). "Another-day'd by the owner" (Everything_todo:335); auto_reset_story leak still open.
11. Vyto_spec §4 + §9 — explanation as choreography: "The viewer should be able to say afterwards how things ended up where they are." Replayable from two spool moments' diff — "One truth, two renderings." SITUATIONS mint a %Sighting when a pathology recurs — "the pathologies watched-for are the regression suite the glass carries with it, alive." = the explanation layer over a running Book, already designed.
12. Seemables (Matstyle census) — "the human SEES churn immediately… That is actually the safety"; the tautology trap (diff the mainkey set, never a Seem's own output). Vyto_todo — owner: "cells need a navigation|attention currency!" — Vyto_attend: attention earned, self-taxing, .c-only.
Also: Backpressure §0.00 — owner: "all these Musu* tests really didn't prepare us too well for the clusterfuck of them all together" → MusuNeGrind = a missing test LEVEL asserting invariants not a snap; §9.5 "a headless green is a bubble"; Story_future_directions §1 (a %see is an emission not state — "fix the harvest, not the lifetime"); Vyto_todo LAW B ("%see sentences written BEFORE the station's code").
