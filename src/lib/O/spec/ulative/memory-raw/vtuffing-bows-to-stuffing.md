---
name: vtuffing-bows-to-stuffing
description: "2026-07-14 steer — Vtuffing distiller bows to Stuffing's ALGORITHM (not HTML); naming-cut DROPPED, bijective key PARKED; Vtuffing = neighbour-aware layout (Se) compressing visual noise; head for Wes-Wilson rendering"
metadata: 
  node_type: memory
  type: project
  originSessionId: 12f2682d-1c3e-4f5e-ba2c-7e09ee65b139
---

The human's 2026-07-14 live steer (recorded in Voro_todo.md §0, "THE 2026-07-14 STEER"):

1. **Distiller bows to Stuffing's algorithm** — `compute_groups` (Stuff.svelte.ts:1207: group like rows → columns → value-chips-with-counts, is_one collapse) as the MODEL for Vtuff_default's rewrite, NOT mounting the Stuffing HTML into cells. Typed k:v bits end-to-end; display text derives only at paint. Kills the grasp's text re-parse (`Voro_grasp_kv` parsing `'year: 2007'` back out of display strings).
2. **Vtuffing = the visual extension**: laying bits out in the cell WRT how NEIGHBOURS lay theirs — compress visual noise (shared claims quiet locally + promote to family lettering / Wes-Wilson river pass; differing claims shout). Se doctrine applied to layout.
3. **Naming-cut DROPPED as filed; bijective key PARKED** (keep `Voro_model_namekey`, don't thread further). region|axis|name = fallible analytics, don't grow them. Eventual true shape = %dome-style instruction (data tells machinery how to treat a key, `D%the_key,the_very_key`) — explicit over inferred. Identity anchor direction: Se-side (Dip_assign persists across Seem replace via resume_X; mem.further-pathed stash) — noted, not now.

**Why:** the human worries about baking in fundamentally fallible analytics; wants "truer to reality to make things simpler to get to really nice rendering first."

**How to apply:** any Vtuffing/render work goes through the Stuffing-shaped grammar + neighbour-aware layout; never expand naming/axis/region inference. See [[voro-model-ruling]], [[see-is-not-a-latch]].
