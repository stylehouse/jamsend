---
name: lens-primitive
description: Lens hoisted-UI dock + the Brink HUD (Rundar/Relay/Sound) + collapse/MiniBrink pattern + the Plank twin; the one Lens doc is spec/Lens_posable_TODO.md
metadata:
  node_type: memory
  type: project
  originSessionId: e6f866cf-7c7d-468a-925f-ea1d6f768711
---

The **Lens** = ambient hoisted-UI dock. A particle is `Lens:<LensKind>,of_Funkcion:<kind>`, held off `top_House().ave.{Lenses}` (off-snap, never pumped), suggested by `Lies_lens_suggest/dismiss/toggle` (now in **LiesFunk**, was LiesWaft). `LensHost` mounts `comp_<lens.Lens>` off the funk kind (`Funk/kinds.ts`); as of 2026-07-02 it takes a **`face` override + `mini` flag** so the SAME lens particle can render a different face — the collapsed-Brink trick.

**Surfaces** = one `<Lens kind="X">` host per kind: **Panel** (Otro, global/fullscreen — IdHatch) and **Brink** (Liesui, the cluster HUD — Rundar/Relay/Upkeep/Sound). The **Plank** ended up NOT a Lens — it's a standalone `ui/Plank.svelte` hosting DocWaftMap, flowing IN Liesui (`.ls-plank-slot`, in-flow under the Waft columns). A face shows ONLY when all three hold: host mounted **and** `Lies_lens_suggest(kind, funk)` posits a particle (else `{#if panels.length}`=nothing) **and** the component renders with empty data. Registering a kind ≠ showing it — that's the usual "NOWHERE" trap.

**Brink HUD** (COMMITTED `40f03df4` 2026-07-02): a flex child of Liesui's sticky base-shell (`.ls-baseshell`, zero-height, `bottom:8px`, BOUND inside `.ls-ui` — NOT `position:fixed`, that escaped and floated over the Langui editor; reverted). The base-shell cell is the Brink's alone (the Plank moved to the in-flow slot); the side button is GONE. The grips bar (Vexpandy + collapsed mini row) is one row tall; open faces float OFF it (absolute, `bottom:100%`), so growing guts never shift the bar (the "Vexpandy jumps its own height" fix) nor overhang NaviCado. **Vexpandy = collapse**: collapsed shows a one-row **MiniBrink** (`comp_MiniBrink` faces — Rundar=liveness dots, Relay=one carrier dot); open unfurls the full `comp_Brink` stack upward, MiniBrink on the INTERIOR side so the toggle never moves.

**Rundar** (was Runner — renamed for fleet multiplicity): editor RACK (`rack:'all'`, snapped `%Runner` roster 1:1 with `%HostedIdentity`) + runner single-pair view; culls a heard-then-silent runner from the DISPLAY at ~5 min (`rack_shown`). **Sound** (funk kind): the "tap for sound" AudioContext-gate beg, a Brink tenant on **BOTH roles** (2026-07-03 — an AC grant is per-TAB, the resume gesture must happen in the owning tab, so each tab begs for itself; was editor-only, which left a needAC runner with NO grant surface since Otro's fullscreen is suppressed under dev boots). Self-gating (invisible until a gat fires window `AudioContext_wanted`), wakes on click *within the gesture*; Otro still fullscreens the disk-gate, a hard blocker.

Bombs: pinned_stable spine owns reconnection (not O/*); Sound's gesture-riding + a pre-mount `AudioContext_wanted` race are the :9091 things to verify. `spec/Lens_handover.md` was finished, folded into `spec/Lens_posable_TODO.md` (§"The %Aim residue" = CreduFunk→viewer, Waft:Aim, excite scheme), and DELETED 2026-07-03. See [[creduler-runner-architecture]], [[editron-verdict-phase2]], [[upkeep-errand-brink]].

----
## merged from lens-posable-layout.md

---
name: lens-posable-layout
description: Lens posable-container/torus vision + Waft:Cluster layout-state TODOs; spec/Lens_posable_TODO.md
metadata: 
  node_type: memory
  type: project
  originSessionId: 9e3dbb8f-9bbc-47ee-ac57-34c8fb9daac7
---

Design stream toward making **Lens a posable UI-container** — a two-hemisphere torus: Lies side = `Lens:Decor` (back wall the Waft is *put into*, via `Funkcion:Decoring` on `Lens:Brink`) + Brink (shooting down in front); Lang side = `Lens:Mapping`/`Lens:Interesting` stem; bridge = **Brink→Interest**. This far half **needs cooking** (the pose/anchor model is unbuilt) — captured, not queued. Full writeup `src/lib/O/spec/Lens_posable_TODO.md`. Relates to [[lens-primitive]], [[upkeep-errand-brink]].

**DONE so far:** Brink in `ui/Lens.svelte` is now a **zero-height sticky anchor + absolute floating child** (`.lens-brink-anchor` height:0 sticky / `.lens-brink` absolute) — reserves NO flow space (its growing height never pushes the waft list) yet stays pinned to a corner of the `.ls-ui` scrollport however it scrolls. Re-perches all 4 corners: Vexpandy bottom↔top (`jumped`), a `⟨/⟩` side button right↔left (`lefted`). (First try was plain `position:sticky` in-flow — it reserved a strip; the user wanted floating-no-space, hence the anchor.)

**Also DONE — fat copper scrollbar handle, OWNED IN LANGUI COMPONENT SCOPE** (took several wrong passes). Asset is fine: `static/i/copper_anodes.jpg`, served at `/i/`. **ROOT CAUSE (user nailed it: "fine once, broke when hoisted to global"):** the `b787f1fd` refactor moved the handle to the global `.scrollbig` class (one class, specificity 0,1,1) — which is **too weak to beat CodeMirror's own injected `.cm-scroller` scrollbar styles**, so on the editor it silently lost (thin/beige). The ORIGINAL worked because it was a component-scoped `.lte-cm .cm-scroller::-webkit-scrollbar*` rule (0,3,1). FIX = un-hoist: removed `.scrollbig` from the editor scroller (the `view.scrollDOM.classList.add('scrollbig')` line is gone) and restored the full handle (width 2em + `background:#6e4e2e url(i/copper_anodes.jpg)` RELATIVE + radius + min-height + Firefox `scrollbar-color`) in Langui's style block. Width is a **literal 2em** not `var(--lte-scrollbar-w)` — custom props don't inherit into `::-webkit-scrollbar` pseudos either (secondary factor; see [[webkit-scrollbar-no-var]]). URL form: **relative `i/...` in component styles** (resolves at runtime to /i/), **absolute `/i/...` in app.css** (Vite/static). The global `.scrollbig` (app.css) keeps the copper for NON-CodeMirror users (Storui, capped Waft, and `DocGhostList` `.ghl-groups` now wears `scrollbig` as the user's proof-test that global copper works where CM isn't fighting it). app.css: `.scrollbig`=12px base, `.scrollsmall`=4px. Open Q: make `.scrollsmall` a global default? Re-hit [[svelte-comment-tag-gotcha]] (wrote `<style>` in a JS comment AGAIN) — watch for it. **FINAL killer found by user:** a "Firefox fallback" `scrollbar-color`/`scrollbar-width` on the same selector **disables the webkit texture on Chromium** (flatly replaces the thumb bg-image) — removed everywhere; scroll areas are now webkit-pseudo ONLY (Firefox = default bar). See [[webkit-scrollbar-no-var]] Pitfall A. Sizes bumped: `.scrollbig` 12px→18px, `.scrollsmall` 4px→12px (user: "12px is tiny for a big, that's scrollsmall's size"); editor stays 2em = minimap gutter.

**GOTCHA (cost a debugging cycle):** a literal `<style>` (or `<script>`) token inside a Svelte JS `//` comment makes svelte-check report **"`<script>` was left open"** at `</style>` (it mis-scans the script block as running to the style close). Write "style block", never the literal tag, in `.svelte` comments. See [[svelte-comment-tag-gotcha]].

**LAYOUT SERVICE — BUILT (P5/P6: `Lies_keep_layout_host/_get/_set` in LiesKeep.svelte + wired clients — Langui scroll_line/minimap, DocTing, InterestStrip, DocGhostList; Waft `minimised` via `Lies_keep_cfg_*`; `capped`/`sidebyside` deliberately ephemeral). Design record kept below for the gotchas; spec/Lens_posable_TODO.md carries the BUILT stamp — and is now the ONE Lens doc (Lens_handover.md folded in + deleted 2026-07-03).** Owner: "a layout service to bridge tiny UIs up to their Lens thing, backed by Keep." Pattern: tiny UIs/Lens panels hold ephemeral local `$state` for layout; some ALREADY hand-roll persistence to `House.stashed` ad-hoc (`Storui`→`stashed['Storui:'+book]`, `Cyto`→`stashed.Cyto_layout_name`, `Otro`→`stashed.showC`) while others are LOST on reload (`Lens.svelte` Brink `jumped`/`lefted`, `Waft.svelte` `minimised`/`capped`/`sidebyside`, `Langui` `minimap_open`/`expanded`, `DocTing` `open`/`sort`, `MiniWaft` `orbed`, `InterestStrip` `show_cold`). Service = ONE `(scope,key)→get/set` backed by the Keep that a tiny UI binds to instead of bare `$state`/hand-rolled stashed. Lens stays a HOLE (ave, off-snap); durability lives in the Keep, projected at hoist + written back on gesture — the Keep's own `Funkcion` is the natural host. THREE SCOPES → Keep: per-Waft→`WaftTimes,of_Waft` (minimised/capped/sidebyside/scroll/in_Doc), per-Lens→`Keep/Layout,of_Lens:(LensKind,of_Funkcion)` (Brink jumped/lefted/corner, altitude), global-chrome→Keep top-level or `mem()` (client picks). GOTCHAS: opt-in per state (NEVER persist input buffers/confirm-guards/init-guards/DOM-refs/geometry/live-selection); CM selection restores via its own EditorState (don't dup); ave↔Keep write must coalesce (no feedback loop); 800ms save throttle absorbs churn; per-Waft keys on `waft.sc.Waft` (share rename-locator caretaking); keep value model general for future poses (torus far-vision rides this too). **SUPERSEDES old Near-TODO-1** (`Waft:Cluster` dontSnap-split for layout) — `Waft:Keep` is the snapped home; open/closed set = the Keep `WaftTimes` ledger + an `open`/`minimised` flag; reopen-on-boot is now `Lies_keep_reopen`. **DEFERRED (owner): the Vexpandy client** — build service+Keep backing first, wire clients "once seen."
- **Interest-Docs mini-GhostList in the Brink** — a per-excited-Interest compact list of its Docs (reuse `in_Doc`/`alpha_doc`, [[multidocwhat-chosen-doc]]); clicking a Doc lands in the **last What that had it** → needs **What↔Doc-last tracking** ("beneath `Aside:Ting`", stamp `doc.c.last_What`).
- **Nav frictions (fix after the Interest-Docs model settles):** GhostList-Doc click opens as Aside then the **Trail confiscates `active`** — eager-ActiveInterest arbitration at `Lang.svelte` ~754-778 (Trail sets ActiveInterest eagerly on real checkout; a Light/GhostList kind doesn't), cf [[aside-stance-aware-foreground]]; dock→What sometimes lands on `wafts[0]` (`Lies.svelte` ~833) / roster-epoch (`Lang.svelte` ~1548).
