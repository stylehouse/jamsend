---
name: voro-se-as-seem
description: Voro Se lane = crush becomes a %Seem (LiesEnd's Selection.process); staged isolation-first
metadata:
  type: project
  originSessionId: 127f6ff9-4278-4954-b130-ada7f9af70d9
---

The Voro **Se** development (the human's chosen lane 2026-07-12, parallel to the F2 hand-off) is
 **the crush becoming a `%Seem`** — the mature model the human pointed at: `%Seem` in `LangHold`
  (LiesEnd), a `Selection()` with identity whose `.process()` walks a source `n**` and mints a
   PARALLEL awareness mirror (the **D-sphere**, `C.c.D = D`) via `resolve()` → survivors keep history
    (`bD`), departures = `goners`, arrivals = `neus`. Same primitive Cyto (`Cyto.svelte:369`) + the
     editor's Understanding stand on = the human's `Selection.process()` north star.

**Why it's the fix, not a feature:** it folds the human's TWO original gripes into one move.
- *"too much scattered `C.c.*`"* + *"not making large enough concepts"* → the six fold flags
   (`c.stuff`/`c.stuffy`/`c.fold_kind`/`c.fold_n`/`c.gang`/`c.represented`, smeared on every source by
    `Voro_stamp_fold`, wiped by `Voro_unstamp`) leave the source and live on the **fold node**; source
     keeps ONE ref `c.fold`.
- *"Se = read surroundings"* → `traced_fn(D,bD,C,T)` fires AFTER the sibling layer resolves, so the
   verdict reads neighbours + history for free. Today `Voro_crushable`/`_swarmable` judge in ISOLATION.
- north star *"plaything that explains the data model through music piracy"* → the fold-sphere is the
   model explaining itself in the glass (particles + their awareness spheres; the reflux made visible).

**Staged (isolation-first, per [[fight-back-on-core-changes]]):** Slice 0 = a read-only `%Seem:fold`
 mirror ALONGSIDE the green crush, changes NO verdict, prove it reproduces the folds (Book `%see`);
  Slice 1 = `hold`-on-radio consults the sphere; Slice 2 = migrate the six flags onto the node;
   end = crush IS a Seem + `Voro_report` reads the sphere. Full design = `Voro_vtuffing.md §🎋`.

**The grasp vocabulary (2026-07-12, `Voro_vtuffing.md §🎋 "Se — the grasp"`):** `i_Se('scape')` mints
 `%Se:scape,Selection:Selection()`; traces `C%*` → `D%the_*`, weights the loud ones `D%the_very_*`
  (reductionist — lossy on purpose). The grasp dumps an INITIAL grip into the render model, dumps MORE
   on `/*3` open (deepen the trace), un-compresses `×N` chips that won't fit. "Surroundings" = neighbours
    AND fit: **14pt hard font floor**; the first VISIBLE gift is "big text stretch-ups" (render-side,
     no crush verdict touched). A **HUD** shows the container path (drop a sole `w:Something`), and
      expand is undoable (Ctrl-Z | edit path); the "square not fitting the viewport" = the top container
       cell the HUD should own. Far horizon = leave cytoscape, sync a pristine layout canvas off the Se
        (pilot = "own the tick", §The next moves #14). The doc was tidied — six ▦ passes → §Relics.

**THE SINGLE ROOT (2026-07-12, both threads converged):** the census storm AND the render's cell
 vanish|re-pop (`Voro_render_faults_todo.md` F3+F8) are ONE bug — the crush re-derives folds from
  scratch every beat (`Voro_unstamp` wipes, `Voro_stamp_fold` re-stamps, reps re-elected), so a fold has
   NO cross-beat identity. Downstream: Cyto's resolve (Se1→scan_id→Se2→`cyto_id`, `Cyto.svelte:961`)
    goes ambiguous → `Dip_assign` fresh id → vanish(F8)+re-pop(F3); AND the drop+rebuild census storms.
     ONE fix = persistent fold-sphere resolved beat-to-beat, anchored to DURABLE identity (container +
      family, NOT the volatile rep). **Home = `w:Voronoiology`** (the human's key): snap runs AFTER
       Cyto+Voro, and it's a snapped `dontGraph` world Voro already fills — so the grasp snaps (identity
        gate-able) AND persists. Metaphysics OK: grasp = the MODEL (may snap), not render-side. **Shared
         gate = assert `cyto_id` stable across a step for a known-persisting particle, off the CytoStep
          archive** (model-side, no pixels) — proves census-stable AND vanish-gone at once.

**Status SLICE 0 BUILT + LIVE-VERIFIED (2026-07-12)** — `Voro_grasp(w)` in `Ghost/V/Voro.g` stands the
 grasp on the REAL primitive: `i_Seem`/`o_Seem` (LangHold), `use_Understandable` wires each D→its source
  C, off-snap home on `w.c.grasp_home` (a free `new TheC`), projected as a clean `%Se:scape` row into
   `w:Voronoiology`. Driven inline from `VoroMitosis_drive` ONLY (isolation-first, zero core touch).
    Proven on the live runner: **`grasped` === census `cells` every beat** (5/5…12/12), and fold-level
     **`born`/`died`** (0–1 a beat) read the cell mitosis — NOT raw `news.neus/goners`, which are
      LEAF-level (a fast flora arrives ~19 species/beat; that drowned the story — the human caught it).
       died derived from the fold-count delta (`grasped = prev + born - died`), a goner D's source is
        stale. Read-only — no crush verdict touched.
 **.g GOTCHA that cost an hour:** an Edit whose `old_string` stopped one line short of a raw `for {`'s
  closing `}` swallowed the brace → "generated JS does not parse @ module line N" (the parse check lives
   in `src/lib/O/lang/compile.ts` `Lang_validate_rendered_module`, greppable only with `grep -a`; source
    is discarded on fail). NOT a translator/async bug. Raw `for (…) {` bails to verbatim (compile.ts
     ~644) — its `}` must be an explicit source brace; never insert between the last body line and it.
 **SLICE A DONE (2026-07-12, render-only, live-verified):** text stretch-ups in `Cytui.svelte` — a 14pt
  floor that DRIVES shedding (fit_stat bands BAND_VERY/LOUD/DECO ~1213), loud→bigger, un-compress ×N.
   Load-bearing statements under 14pt: 6→0, min 8pt→15pt. Faked loudness (title/mainkey/dominant chip),
    CLEARLY MARKED to swap for real grasp weights. Verify = `runner_shot --svg` + grep font-size (eyes-on,
     NOT Book-gateable). Molded-Stuffing face left alone (foreign DOM overlay — not clean).
 **SLICE B DONE (2026-07-12, `.g`, snap-verified):** `Voro_grasp` VOICES each fold — stamps `D%the:name,
  weight:100,very:1` (identity = the_very) + `D%the:n,weight:0..100` NEIGHBOUR-relative (vs the loudest
   sibling) on the OFF-SNAP D-sphere (drop+rebuild costs the snap nothing). Readout gained `loudest:<cell>`
    (Kunzea on the flora). Counts unperturbed. D%the reachable by the render via `src.c.D`.
 **SLICE B2 DONE (2026-07-12, compile+type-clean; PIXEL-VERIFY OWED on a Voro runner).** The join
  landed CLEANER than the D-sphere `grasp_of(src)=src.c.D→D.o({the:1})` sketch: the grasp weighs the SAME
   Vtuffing tree the render draws (`Vtuff_build`'s cached `src.c.vtuffing`), stamping each `%Vrow`/`%Vbit`
    `wgt` 0..100 — so the two halves align BY CONSTRUCTION, no cross-beat key-matching to drift. The
     weight comes from a NEIGHBOURHOOD CENSUS (`Voro_grasp_tally`/`Voro_grasp_weight` in Voro.g): a claim
      universal across cells → ~20 (recedes <14pt), unique → ~95 (towers); a rare key nudges up. That IS
       the Se — a cell weighed against neighbours, which `Voro_crushable`'s isolation judge never could.
        Render side (Cytui): `wgt_norm` carries the row `wgt` through `vtuff_rows`, `band_for(wgt,hiCap)`
         picks the band (≥80 lo16 / ≥45 lo14 / else lo7); faked `loud` deleted; grasp-blind panes still
          render sane (title 100 else 50). `%Se:scape` readout gained `trait` (loudest cell's claim).
 **THE BLOCKER**: pixel/behaviour verify needs a `?B=VoroMitosis` runner tab; the tab up now is
  `?B=MusuHeist` (a pinned runner — `become_book VoroMitosis` silently runs MusuHeist, DON'T fight it).
   Only VoroMitosis carries the grasp (isolation-first, book-driven); VoroScape is imposed-from-above so
    its rollout = fold `Voro_grasp` into `Voro_crush_scan` (do it AFTER pixel-proof, not blind).
 **THE KNOB to tune live** = `Voro_grasp_weight` (the 0.85/15 consts + the 80/45 band thresholds), eyed
  on `runner_shot --svg`. NEXT after that: Slice C (regions; grasp emits `the_family`) → D (arcs).
 Build crush = `.g` → LocalGen writes gen (Vite HMRs into a live runner) → verify via runner_ask; NEVER
  mid-run. See also [[voro-subgraph-slice]], [[voro-imposed-from-above]], [[fight-back-on-core-changes]].
