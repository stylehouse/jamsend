---
name: musu-cursor-c1-built
description: "The whole %Dogear cursor arc LIVE-GREEN x2 — C1 resolve + C2 heal + C3 berthed-resume; 2026-07-19 MusuCursor grew the FLAT/CROWD scene (flat Mag no Cloud level + 40 near-identical siblings) and converted to story_swear — declared 5 sworn 5 gaps 0; MusuHeal/MusuResume still on legacy %see (conversion owed)"
metadata:
  node_type: memory
  type: project
  originSessionId: 2d6954a9-460d-42bd-afcf-4431dd33d952
---

**C1 = cursors, LIVE-GREEN ×2 2026-07-13 (`Ghost/M/Heist.g` `Cursor_*` primitive + `MusuCursor` Book in `Ghost/Story/Heistation.g` after MusuDoor; 6/6 caveat:0 on the reloaded 49de).** The human's steer: model the cursor on `%lematch`. A `%Dogear` (the mainkeys `%Cursor`/`%cursor` are taken — LiesKeep focus-history + LangCurse Interest) is a linear spine of `%curs` match-segments, each storing ONE `o()`-query. `Cursor_make(home, into, queries)` mints; `Cursor_resolve(dog, root)` re-finds each level from a root down → `{ok:true, at, depth, landed}` or a CLEAN fail `{ok:false, at, depth, missing}` (no throw — the exact seam C2's `%Renamed` heal plugs into). **Snap-safe wildcard trick:** the presence TYPE rides as a `wild:<Type>` VALUE, re-inflated to `{Type:1}` at resolve; literal pins ride flat. This dodges the `Cloud:"1"` exactly() footgun ([[o-query-wildcards-on-1]]) AND makes the cursor KEY-AGNOSTIC — a level pins by whatever keys its node wears (id/randomic/shuffle), so the Cloud-model redirect (below) slides under it. Three `%see`: lands-on-leaf (depth 3), lands-on-a-level (depth 2, partial), clean-fail (knock the record out → fails at that level, reports `missing`). Adversarial review ([[adversarial-test-agent]]) found + fixed a real soft spot: see #3's "no crash" degraded to a SILENT un-latch on an upstream throw → a `reached:step_4` marker now gives the recorded snap-diff teeth; see #2 tightened to assert the landed node is a `%Cloud`. Compiles byte-clean (LocalGen 88271c + ghost-compile `@bbc3c8cc`). Registered Credence (`brand_new:1`) + Ality; toc seeded 6 lie-steps.

**Live gate DONE (2026-07-13):** recorded on the reloaded 49de — snap shows both `%Dogear` spines, the knockout removed t1, all 3 sees + `reached:step_2`/`step_4` latched.

**C2 = the HEAL, LIVE-GREEN x2 2026-07-14 (Book MusuHeal, `Heistation.g` after MusuCursor).** `Cursor_resolve` grew a heal branch: on a miss it calls `Cursor_heal(node, seg, q)` which looks beside the last node reached for a `%Renamed,key,from,to` marker and retries the query with the pin remapped to `to`, recording `heals:[{key,from,to}]`. `Renamed_mint(parent,key,from,to,at)` lays a marker beside the renamed node IN the magazine (positive, window-able, at:-stamped, newest-wins — cousin of `%Tombstone`/`%UnGrant`). DISCRIMINATION built in: MusuHeal renames a marked twin (t1→t1b, heals) and an unmarked twin (t2→t2b, fails clean) so the marker is provably load-bearing. Heal is TRANSPARENT to un-renamed cursors (MusuCursor re-ran green after the change — no re-record). Adversarial review caught steps 3/5 missing `reached:` markers → added. The `%Renamed` shape landing here also tees up **M3** (mint the same marker from a real rename mission).

**C3 = the RESUME, LIVE-GREEN x2 2026-07-14 (Book MusuResume, `Heistation.g` after MusuHeal; commit `a28f480f`).** A `%Dogear` homed INSIDE a magazine survives a full `enWaft`→`deWaft` round-trip and still resolves to the record it named — the resumable browse. Isolates the ENCODE|DECODE (MusuBerth owns the real FSA disk) so it needs NO FSA + runs on ANY runner, deterministic. `Cursor_resolve` walks DOWN via children (never `c.up`, which doesn't survive decode), so it resolves cleanly against a freshly-decoded tree. Discrimination: a live-only bookmark under `%testing` does NOT ride the snap (re-decoded mag carries EXACTLY one Dogear). ADVERSARIAL HARDENING: the `independent` leg was near-tautological (`deWaft` structurally can't alias — parses text into a fresh tree — so `!old.ok` was forced by our own rename) → re-gated on the re-decoded cloud being a DISTINCT node object. **CLAUDE.md CORRECTED this build:** the enWaft vocabulary gate is PARKED (`all_knowing` default true — ANY mainkey encodes; the old "unknown mainkey = fatal encode skip" was STALE — the ONLY encode-fatal is an object/function in `.sc`; session keys active|created_at|new|not_found are silently stripped; max_child_depth silently cuts). Credence regrouped (`What:mostly` → pipeline / magazine / replication; cursor trio homed under `magazine → the cursor`) commit `8b3b14a4`. **The whole cursor arc C1·C2·C3 is DONE.** RUNNER-WEDGE re-confirmed: the post-ghost-compile HMR re-froze 49de's boot — first MusuResume run came back `total:1` (vacuous Prep-only green) + clobbered the toc to a 1-step skeleton; SECOND run thawed to full 7 ([[frozen-boot-empty-first-run]] + [[toc-collapse-on-orphaned-save]], both self-heal; NEVER accept a `total:1`). **RUNNER-WEDGE LESSON (learned the hard way this build):** both runners were wedged mid-session (49de engaged a Book but never minted a Run — `run:null, uid:null` — even for a known-green control; 3c52 stuck at `"phase":"begun"` [[runner-wedge-begun]]). Cause = an in-session `ghost-compile` HMR of a big ghost stalling the live think loop ([[hmr-remixes-ghost-methods]]); only a browser-tab RELOAD clears it (I can't reload a tab — human action). After a reload, ALWAYS burn one cheap thaw run first ([[frozen-boot-empty-first-run]]).

**FLAT/CROWD coverage + SWORN conversion (2026-07-19, the Mag-preen round):** the human ruled the
 Cursoring must be "flexible and unconfusible… potentially trees, potentially a big list" — trees were
  proven, flat was NOT. `MusuCursor_flat_scene` (step 5) now mints `%Mag:FlatCrowd` with FORTY cards
   DIRECTLY under it (no `%Cloud` level, all sharing artist+title so the id pin is the only
    discriminator) and proves: a one-level spine lands clean (depth is the magazine's business, not the
     cursor's) + lands on exactly f23 of the forty. Same round the witness converted `%see` →
      `this.story_swear` (the current regime): **declared 5, sworn 5, gaps 0**, green ×2, contract in
       the toc. LESSON: `runner_ask declare` needs a LATCH RECORD on the ave/%Assertioning shelf — a
        `%see` Book has none, so declare silently no-ops ("no latch record" on the runner console);
         convert the witness to story_swear FIRST. MusuHeal + MusuResume still carry legacy `%see`
          witnesses — same conversion owed when next touched.

**CLOUD-MODEL redirect (the human, same turn — affects M3/M4 not C1):** drop `randomic`; the real rastock lays Records ordered by `shuffle | ctime | mtime`, PARTITIONED every ~20 (a 400-track collection → ~20 Clouds of 20, `Cloud,shuffle:0..19`). Cloud key = partition position, not a random draw-id. Touches `Musica_fold` batching + `repli_loc` → `['Cloud','shuffle']` + the real `Ra_stock` generator. C1 is key-agnostic so it is unaffected. See [[musu-ra-magazine-m2]], [[stimuli-magazine-vision]], [[musu-door-sabotage-wall]].

**D1 part a re-posed (the human asked what it's for — it IS Invites & Grants):** the demo Books fake the follow-consent with a `w.c.grants` boolean; part a swaps it for the live `Swarm_pier_live(pier,'Music')` verdict (signed grant, revocable by `%NotGrant`). MusuHeist ALREADY runs that verdict live (`Heistation.g:258`); the only unproven bit is revoke-mid-relationship → next-draw-refused over the magazine pipe → one scene folded into MusuHeist (needs the AudibleEntropy profile + a warming re-accept, attended). Not a fresh Book.
