---
name: vyto-process-engines
description: "The sizing|structure arc's integration doc (2026-07-21): vyto_workingouts/processes.md breaks the whole machine into C→C engines — joints (local geometry × global S · floor law · crush's voice · nest-solve · reframe), risk register, five-Book build order. Boot list for a fresh session + 4 HUMAN rulings pending."
metadata:
  type: project
  originSessionId: c04d18c2-95b5-4c6f-9e9e-1966a05f5bee
---

The human (2026-07-21): *"more engines for manipulating C/C/C structures would emerge when we
 understand how it breaks down into processes"* — and asked for the integrated design written down
  before a fresh-session reset.  Answer: **`src/lib/O/spec/vyto_workingouts/processes.md`** — every
   stage is a C-tree→C-tree transform (mirror, Vtuffing, importance, S, envelopes, moments — all C),
    which is WHY the constraints compose and why each engine Book-proves in isolation.  Read-engines
     have write-inverses (manipulations): Distil's inverse = the write-fan (edit the crest → fan to
      members), Solve's = the pin/drag, Capture's = seek.

**Boot list for a fresh session on this arc, in order:** `Vyto_todo.md §0` → `vyto_workingouts/
 processes.md` → `Vyto_sizing_todo.md §9` → `Stuff_todo.md` → `Vyto_spec.md §3–§6`.

**RULINGS LANDED 2026-07-22 (processes.md HUMAN block records them):** (1) floor law STANDS + the
 STEER — "one pretty accurately readable C** language" · registers-by-zoom not presentation modes;
  (2) zoom=reframe YES + bounded-memory rider (resident O(visible) never O(zoom-history); dip =
   door not cache); (3) `Vyto_fold` tenancy GRANTED ("you're in charge") — distil + Dip_assign land
    together · regression bar = all Vyto* Books green; (4) write-fan re-explained · PARKED awaiting
     appetite.  NEW COMMISSIONED DOC: `spec/Cstructures_todo.md` — the C** structure catalogue
      (holding · referring · join · ledger · family · spool · req · scope · furniture · crest) with
       one saying each · the Se's features (`by: rule: hid: pass:`) · the **Dip_assign law** (every
        supersession mints its dip — the dip `/*N` handle exists at Voro.g:1786) · the memory law
         · **§7 THE READ** (the keystone big think, on Opus 2026-07-22): render∘read ≡ identity
          MODULO declared dips — accuracy=round-trips (dips relocate behind COUNTABLE doors, so
           Dip_assign carries a count), readability=injective (cardinality-on-container separates a
            crest-of-5 from a lone particle), crest keeps per-key MARGINALS+counts while the joint
             stays behind the dip and the VEIN is the one cross-key correlation lifted up (WHY the
              vein exists), registers = read stopped at different door-depths (why "registers not
               modes" is formal), Se by/rule/hid/pass IS the grammar read parses.  Yields the
                **Readback** Book (night queue item 4, ahead of pods).  Cron retimed to 1.5h = two
                 jobs f92b03ed(:13)/34cde681(:43).  Data-dump .c reveal = "another way to divide sc
                  presentation space for coherence" (truly-sc vs merely-c), not a forensics mode.
                   The vein ruling is DONE (supersede default + coexist knob + wgt:1) — do not re-ask.

**J4 ALSO PROVEN (2026-07-22): the `Nestcut` Book** — nested power cut recurses depth 3 with EXACT
 tiling (gap=0; misfit-as-data 0 every level); a scope re-cuts its innards inside its STANDING
  polygon while the outside holds byte-still.  LESSON: a power weight shifts the wall LOCALLY —
   never assert global area order against position; pin deterministic constants and ALWAYS grep
    the snap for every %see after a fresh record (a missing %see leaves the run green).
     Night-2 queue (crons f92b03ed/34cde681): scratchpad/night2_log.md — DONE: 1 Nestcut ·
      2 structure.html crush-by-zoom (republished cdccea4b · 34-check node gate on the page's own
       [pure] code · probe-redded) · 3 **Deepcrest** (distillation COMPOSES — emergent №1 proven:
        Deep_memberise reads a crest into a %Gist member with veins un-superseding and spreads
         refusing to collapse; crest-of-crests ≡ pool-crest on family-uniform keys; door carries
          Σ counts + recounts on dissent; green ×4 + probe-red) · 4 **Readback THE KEYSTONE**
           (read∘render ≡ id modulo counted doors is a GREEN GATE: Read_crest/Read_audit/
            Read_surf/Read_saying; twins = identical sayings over different joints told apart
             only by the door; **TWO NOTATION LEAKS found → morning ruling** — valued facts and
              spread tails carry no carrier count · fix = n:have in Stuff_keyrows + Voro oracle
               but re-snaps 3 Books; LESSON: rank TIES leak insertion order into sayings — make
                benches count-decided) · 5 **Surfline** (the COMPOSED kernel: Nestcut_cut prices
                 the proven triple 136872|134622|88506 → each area IS Floorlaw_solve's frame →
                  crush 8720 | legible 6115 | freed-10 → surf re-solves ONE scope at 360000 →
                   un-crush 13093 while every other price + the chart hold to the byte; all six
                    pins landed FIRST record; probe = surf-without-reframe red) · 5b **pods.html**
                     (shape study two — NEW artifact c91a13ec · 🫧 · same 58-track data + same law
                      as cards so the human votes on SHAPE alone; deterministic spiral packer ·
                       21-check gate incl. independent no-overlap + containment; FLOOR_R 22 —
                        pod nesting crosses floors at different depths than cards · law constant).
                         Remaining unproven: ONLY tenancy + paint.  Next tenancy → sweep.
                          6 **TENANCY LANDED**: Vyto_distil/Vyto_distil_free (detached TheC ·
                           Books-invisible)/Vyto_dip_assign/veinrows/keyrows ADDITIVE in Vyto.g
                            (fold stub byte-untouched — mirror-side solver stays with the
                             display refactor); gate = VytoCrest Book (Vytonation.g · lying-door
                              probe red); FULL regression bar green (7 Vyto* + Stuffing oracle).
                               Paint choreography is now the ONLY unproven half.
                      CREDENCE CHURN ×3 (Typescale · Deepcrest · Surfline — foreign tab re-saves
                       at its own cadence; ANY edit window can lose) — re-home + reload pinned
                        runner each time; Shelver MERGE-on-save now firmly earned.
   **NIGHT 2 CLOSED — ALL 7 QUEUE ITEMS DONE (on Opus after fable ran out):** morning brief polished
    into night2_log.md FOR THE HUMAN (destination · 5 decisions recommendation-first · commit point).
     Item 7 sweep: runner was down ~2 firings (unreachable · dev :9091 fine so relay-host up · the
      TAB dropped) then came back ~14:20; healed a FOREIGN begun-wedge (another agent's Peeroleum
       stuck at `begun`/no-lease = debris) with `reload`→poll `running:null`→cheap green, then ran
        **all 15 arc Books GREEN 15/15, book-VERIFIED** (ok_pct 1 · no reds · step counts match the
         item-6 bar: Stuffing 4 Typescale 3 Floorlaw 4 Nestcut 4 Deepcrest 4 Readback 4 Surfline 4
          VytoCrest 4 · VytoStaple 8 VytoCell 7 VytoMitosis 6 VytoRadio 5 VytoTandem 4 VytoFreeze 3
           VytoSeek 4).  So the whole arc is now proven as DATA in ONE sitting, not just per-firing.
            Only PAINT choreography (the human's Vytui refactor) unproven.  TOOLING LESSONS (each cost
             me a wrong claim mid-sweep — the live runner is authoritative, so VERIFY): (1) parse a
              verdict from the `"phase":"done"` object — NEVER first-match `"total"` (the `stepping`
               frames print a transient `total:1` that mimics the every-Book-total:1 WEDGE tell → false
                wedge alarm; disprove by a Book whose real count you know, e.g. VytoCell 7/7); (2)
                 **VERIFY the done-object `"book"` field equals the book you requested** — on the SHARED
                  ★claude runner there is a lag between `run <book>` accepted and the tab switching
                   books, so a batch `--watch` mislabels a NEIGHBOUR's verdict (my batch said Mitosis
                    4/4 · Radio 4/4; direct re-run+`steps` proved 6/6 · 5/5) → run the sweep in ONE
                     uninterrupted process with retry-on-mismatch; (3) no `pgrep` and foreground
                      `sleep` is blocked in this shell — kill bg-tasks with `TaskStop`, pace pollers
                       with node `setTimeout`; (4) `release` + re-poll idle to untangle a collision,
                        and always `release` the shared runner when done.
   **COMMIT POINT TAKEN (2026-07-22 ~15:43):** the human committed the whole Vyto arc while away —
    `ba534fcc "Vyto the work"` + `759436a6 vyto: wire-side requests` + `68377187 vyto: per-book
     migration note`.  Voronation.g/Vyto.g/Vytonation.g/processes.md/Cstructures_todo.md now in git
      history (clean tree).  Post-commit heartbeat green (VytoCrest/Surfline/Stuffing book-verified).
       The ~60 modified `wormhole/Story/*/toc.snap` are BENIGN timing-sample churn (wall-clock
        `sample=,at=` lines accrete on every run · assertion `dige:` byte-unchanged → NOT drift ·
         `git checkout` them or commit at will).  Whole arc DONE + COMMITTED; only PAINT (the human's
          Vytui refactor) unproven.  The 5 open items are all HUMAN decisions (two-leak `n:have`
           ruling · cards-vs-pods shape vote · Credence churn+sub-What naming · doc preens to spec ·
            write-fan appetite) — nothing autonomously actionable without them.
   **DECISIONS NOW IN THE REPO (2026-07-22, human asked — the scratchpad night2_log lives in
    `/tmp/...`, untracked, so they never saw them):** lifted the 5 open decisions + arc status + both
     artifact URLs (cards cdccea4b · pods c91a13ec) into `spec/vyto_workingouts/processes.md` → the
      `## HUMAN` block, new `### STILL OPEN — your calls` subsection (items 5–8).  That block is now
       the canonical home for the arc's owed decisions; the firing changelog was NOT copied (disposable
        per CLAUDE.md).
   **SHAPE STUDIES + THE LAYOUTER RULING (2026-07-22, human present):** three artifacts explore how the
    solved model looks — cards (square · cdccea4b) · pods (round · c91a13ec) · **foam** (graph-laid ·
     9c7f1c33 · 🕸️).  The human's steer that RESOLVED the architecture: cards/pods were too regular AND
      "imposed" their own layout — the objection is that they threw the graph away.  RULE: **Cytoscape
       stays the graph-LAYOUTER** — it owns node POSITIONS (chaos, from real edges) + ROOM ("guying" =
        important nodes carry more charge so the force sim opens space around them · "foam what matters");
         the **voronoi owns only the SKIN** (cells nest for containment but the positions inside are the
          graph's, never a grid/pack); text is "puzzled into" the room the foam gives it; **focus = a
           FISHEYE** (magnify focus · ring the rest at the boundary · a VIEW transform, NOT a relayout).
            foam.html proves it: force layout w/ shared-meaning springs → power-voronoi skin · `[pure]`
             gate green ×47 (tiles every parent to 0.000% gap · shared-meaning siblings cluster · guying
              works · probe-red when graph edges cut).  LOAD-BEARING NEXT (not polish): the fisheye —
               uniform zoom barely magnifies a cloud filling ⅓ the frame (root S 0.73→focus 0.80) so the
                register can't open continuously by size yet (drills by click).  tiles.html was a dead
                 end (Lloyd-relaxed = still imposed · unpublished).  Same distil/crest/law byte-identical
                  across all three so the vote is shape-only.  Artifact verification = the `[pure]`/`[dom]`
                   node-gate pattern (ship code IS checked code); scratch in session dir.
   **FOCUS LENS BUILT (foam2 · 2249bca7 · 🔍):** the human's "focus an area · put the rest at the
    boundary."  A **zoom-box** (NOT a point-fisheye — that magnifies the centroid where children MEET so
     a cloud never opens; discovered+discarded): focus rectangle expands to fill centre, the rest
      compress into a margin ring (present at boundary, never clipped).  Piecewise-LINEAR + SEAM-VERTEX
       insertion ⇒ tiling preserved exactly under the lens.  Crush by median-MEMBER dim (container-kind
        OWN dims overlap; member dims separate).  Layout untouched (force still the layouter).  `[pure]`
         gate green ×16 + probe-red.  HONEST LIMIT: a cloud's box is big so focusing one only magnifies
          ~1.5× (drill by clicking DOWN; small cells magnify hard).  FOUR studies: cards cdccea4b · pods
           c91a13ec · foam 9c7f1c33 · foam+lens 2249bca7 — human's TASTE CALL still open.  If foam wins:
            wire REAL Cyto positions in place of the stand-in force layout + strengthen cloud-level zoom.
             Reusable lesson: for focus+context over a tiling, a zoom-box beats a point-fisheye and stays
              tiling-exact via seam insertion.
   **DIRECTION DECIDED (human, 2026-07-22): foam + lens (2249bca7) WINS** — stop maintaining cards/pods/
    foam; pour effort into foam2.  Human feedback landed as rulings: (1) REPRESENT C DATA FAITHFULLY —
     never case-munge a mainkey for aesthetics (was uppercasing "ARTIST", lowercasing); the dip's "tracks"
      was HARDCODED domain knowledge (fixed: leaf type DERIVED by walking down = node.leafmk).  (2) LABEL
       FORMAT = **Type×N** ("Cloud×3 · Track×58" — mainkey leads, count is the ×N already used in crest
        spreads).  (3) TEXT must "really fill the cell" viscerally (SVG textLength+spacingAndGlyphs, ~84%
         width, re-stretches as the lens resizes) — NEXT LAYER owed: choose WHICH SIDE to anchor a label
          from per cell, in concert with NEIGHBOURS (so labels don't fight across a shared edge).  (4)
           containers must read as FRAMES holding their inners (inset children more + brighter rim).  (5)
            clicks must work mid-animation → delegated svg listener (nodeRef per cell).  STILL NOT
             Cytoscape (stand-in force layout · said plainly · wiring real Cyto = named next, but that is
              the human's Vytui/display refactor turf).  `[pure]` gate green ×16 throughout.
   **THE JOINERY RULING (2026-07-22 · refines "Cytoscape stays the layouter"):** the two sides are
    DECOUPLED — a graph-WHISPERER (graph→positions) + a cell-DRAWER (positions→voronoi skin); the drawer
     never knows who whispered.  So the layouter is a **SWAPPABLE PROVIDER** behind one seam
      `(kids,poly)→[{x,y}]`.  FACTS found: cytoscape IS a dep (fcose/cola/dagre); Voro.g ALREADY says
       "Cyto stays the LAYOUT engine — fcose" + voronoi is "SVG over the live Cytoscape canvas" (so Vyto
        already borrows Cyto.svelte's canvas — the coupling the human wants to shed).  RULING: (a) leave
         Cyto.svelte alone (don't inherit its shortcomings); (b) that ≠ drop cytoscape — run it HEADLESS
          (`headless:true`, fcose, no canvas) as a Vyto-OWNED layout provider; (c) mobile is low-stakes
           because swappable: desktop→headless-fcose, phone→tiny owned force layout if cytoscape's
            bundle (100s of KB) is too heavy (studies prove ~50 lines enough) · lazy-load cytoscape only
             where it earns weight.  foam2 makes the seam explicit (`whisperPositions` = the one line to
              swap).  Recorded processes.md item 6.  OPEN for human: headless-cytoscape bundle OK on
               phone, or default to owned light layout?  NOT a struggle — I build the skin against ANY
                provider (the decoupling working for us).
   **SEATED LABELS BUILT (foam3 · cf905ba4 · 🪧 · 2026-07-22):** the "NEXT LAYER owed" (which side to
    anchor a label from, in concert with neighbours) — DONE.  Each label seats at the cell's POLE OF
     INACCESSIBILITY (deepest interior point) and fills the horizontal chord it owns there, instead of
      centring.  A voronoi cell shares each edge with one neighbour, so the deepest point retreats from
       every SHARED edge at once (two touching cells → opposite interiors → text stops fighting the seam).
        Where poles still leave a pair closer than the old centroid spacing, a reconciliation pass eases
         the offenders toward their centroids until restored — **concert WINS the tie over roominess**
          (deliberate: the human's headline was "don't fight across a shared edge").  `[pure]` gate green
           ×21 (inside · ≥centroid clearance mean ×1.10 · owns a chord · deterministic · CONCERT
            GUARANTEED 17/17 groups worst-shrink 0.00% · probe-red when reconciliation disabled →13/17).
             Pure SKIN — independent of the layout provider, rides any whisperer.  Forked to a NEW path
              (foam2 2249bca7 kept live for centred-vs-seated compare).  Owed-next if human wants more
               polish: label at seat.y but the block extends down — could seat the whole stacked block
                (tab+name+facts) not just its top.
   **READING-VECTOR NESTING BUILT (foam4 · 8944dcf5 · 🪆 · 2026-07-22):** the BIG restructure — human
    said the C had no space of its own when its C/C are shown (children take over), text should grow
     TALLER not just wider and the whole several-chunk block should grow, labels should order like the
      snap (inners DOWN-AND-RIGHT of outers), with C-cluing vector shaping, drawn GENERICALLY at any
       angle.  ONE primitive answers all: a **reading vector u** (nesting/indent direction, default
        down-right).  Each container CARVES a header band on −u for its OWN label; children tile ONLY the
         +u inner region (carveAlong = two half-plane clips sharing the cut → header ∪ children = cell
          EXACTLY).  Children on +u ⇒ every inner seats further along u than its outer = snap indentation.
           Labels = chunks grown in BOTH dims (textLength along baseline b=⟂u · font-size along u) each
            skewX'd differently.  Written in u ⇒ ROTATION-EQUIVARIANT (carve∘rotate=rotate∘carve) = "draw
             at any angle"; ←/→ swings u live (re-carve+re-lay+render).  `[pure]` gate green ×10 ×2
              (tiling 0.00% · header 5–50% seat · snap-cascade 74/74 · clouds-open+artists-fold FLOOR 105
               · Type×N dip · equivariant <1e-6 · props hold at 78°&58°) · probe-red (bypass carve →
                tiling 25.9%).  LENS DROPPED to isolate the mechanic (pure view, composes back later).
                 Four studies live: foam2 2249bca7 🔍 (lens) · foam3 cf905ba4 🪧 (seated) · foam4
                  8944dcf5 🪆 (reading-vector nesting).  Owed-next: fold the lens back onto foam4 · finer
                   skew/vector-clue tuning (needs the human's eye — pixels don't round-trip a node check).
   **foam4 REFINED (2026-07-22, human feedback · same url 8944dcf5):** root(Library) label was thrown OFF
    its cell (header name stretched hW*0.72 on the root's huge diagonal band + tab offset ±hW*0.34 → off
     canvas) → FIX cap fs≤22 + nameFill=min(hW*0.5,…) + tab above along −u.  Overlap: nat*5.5→2.4 stretch,
      seated fontH≤20 at h*0.7 (gap).  "Less deep info at top · more spacious" = REGISTERS-BY-ZOOM: seated
       rows in register order (name→dip→veins→facts) CLIPPED to room (maxRows=round(uExt/26)) + FLOOR
        105→175 so overview folds ONE shallow layer (clouds open · artists+genres → counts) · descend for
         depth.  Gate green ×10 ×2 + probe.  HUMAN SIGNAL: "how realistic are these demos · start Vytoing
          it up soon" — the demos are FAITHFUL to the C model + geometry but STANDALONE (stand-in force
           layout not real Cyto · hand-authored TREE not the live library · no lens on foam4 yet).  Path to
            real: whisper = headless-cytoscape/owned provider on REAL C** positions · drawer = this skin as
             a Vyto face · feed live records.  Display side = the human's Vytui turf (constrained); I can do
              the wire/model side + the granted Vyto_fold tenancy.
   **NO-SPILL FIT (foam4 · same url · 2026-07-22):** human "utterly nail text placement · still spilling."
    Root cause = placement ESTIMATED not checked.  Fix = moved label layout into [pure] (headerChunks/
     seatedChunks) so check verifies the drawn numbers; each chunk's true footprint = 4 glyph corners
      through skewX+b-rotation; fitInside SHRINKS tl+fontH until every corner ≥2.2px inside the polygon,
       drops below MIN_FONT rather than clip → a label can NEVER spill.  Gate green ×12 ×2 (189 chunks ·
        worst clearance 2.20px · holds 78°&58°) + probe.  LESSON re-learned: sed-revert of a `/* */` probe
         SILENTLY no-ops (left file broken+green-looking-red) → USE Edit for probe/revert, verify with grep.
   **foam4 v2 — ALIGNED KEY · SNAP SLASH · MOBILE LOD (2026-07-26 live · same url 8944dcf5 · gate ×12→×18):**
    (1) every CONTAINER cell now LEADS with its mainkey tag (Cloud·Genre·Artist), faithful verbatim, so type
     reads as a rhythm; scoped to containers on purpose (a leaf layer is one uniform type — tagging each
      Track speck only repeats · one-line flip if the human wants leaf tags).  (2) a folded container reads
       like the snap: `Artist`/`Voxhall` then a dim `/` then only a GESTURE of contents (dip→vein→facts at
        0.6 opacity, pushed by a gap row) — identity separated from compression.  (3) MOBILE LOD: VIEWSCALE=
         clientWidth/SPACEW stamped each frame (resize re-renders); FLOOR is now a RENDERED-px floor
          (memberDim*VIEWSCALE<FLOOR) + register depth round(uExt*VIEWSCALE/26) → a phone (~0.36×) folds a
           whole layer (clouds→3 crests) and drops detail rows instead of smudging.  GOTCHA (structural fix):
            seating the tag at the extreme −u top starved it below MIN_FONT (dropped, name won) → seat the
             NAME on the roomy pole, hug the KEY just above it, gesture cascades DOWN (sheds from bottom);
              split wgt(spacing) from fwgt(font) so the small tag stays legible → 44/44 containers lead.
               No-spill STILL holds (183 chunks·2.21px).  New checks H(key leads·verbatim·containers-only)
                I(snap `/`) J(LOD folds+drops at phone scale) · probe drop `*VIEWSCALE`→J red→revert green.
                 Regression all 6 studies 157/157.  Republish an earlier-conversation artifact via `url=` on
                  the Artifact tool (compaction can sever publish lineage → same file_path would mint a NEW
                   url; pass the url to hit the SAME one).
   **SUNFLOWER/phi idea — TESTED, redirected (2026-07-26):** human clarified "phi" = a golden-angle SUNFLOWER
    lattice of label seed-points, to SEE disconnected things packed together as a rhythm/angle discrepancy.
     PROBE (scratchpad /tmp/sunflower_probe.mjs) DISCONFIRMS the naive form: the lattice rhythm is real
      (nearest-neighbour index offsets cluster on Fibonacci 8·13·21·34 = the parastichy arms) BUT it is
       ANTI-clustering by design — spatial neighbours sit at index ±13/±21, never ±1, so packing a group as
        a contiguous block SCATTERS it (interior same-group frac 0.33 · intruders 0.50 OVERLAP interior 0.17
         → NOT separable).  The idea's REAL home is the per-cell signature-ANGLE (the "meander" I pitched · =
          "which we could kinda do with the angling"): probe on foam4's actual layout (angle_probe.mjs) shows
           adjacent siblings that SHARE meaning sit 40° apart on a signature-angle vs 69° when they share
            NOTHING = a 29° visible discrepancy (sharpens if the angle keys on the dominant shared vein, not
             a whole-sig hash).  So: sunflower packing = beautiful+even but wrong tool for "who clumps"; the
              force layout already clusters by meaning (foam4 check A) and a signature-angle would surface
               intruders.  Human AMBIVALENT · leaning "get back to the Vyto algorithm" — do NOT build the
                angle-meander or the Vyto bridge without their word.
   **foam4 v3 — SIGNATURE LEAN · CELLULAR MEMBRANE · ZOOM (2026-07-26 live · same url 8944dcf5 · gate ×18→×21):**
    human dropped the morphing-phi-grid ("too complicated") · "the angle is much better informing" · wanted a
     CELLULAR graphic not a `/` text label · "zoom in a bit (part of what we're not demoing)".  BUILT all three.
      (1) SIGNATURE LEAN = the 29° idea shipped: each cell's LABEL block bends off the global u by
       bendOf(node)=hash(sorted signature)→[−22°,+22°]; cells that distil the SAME stuffing lean IDENTICALLY, a
        stranger tilts off.  MEASURED green: kin 3.9° apart vs stranger 15.8° (Moonlit's tracks lean by YEAR-
         group — 2019 all −4.6° · 2007 all −13.7°).  Carve/tiling stay on GLOBAL u (structure untouched); only
          the label leans, so no-spill still holds against the LEANED vecs.  (2) CELLULAR MEMBRANE replaces the
           `/`: a dashed bowed cell-wall along b + a nucleus dot, cell-hue, shrunk until both ends sit inside
            (gated — the wall never spills).  (3) ZOOM: a persistent camera `<g>` scales+translates onto the
             focused cell (rAF tween 520ms) AND folds into VIEWSCALE (base×camZT, cap 3.2) so diving RAISES
              rendered size past FLOOR → the focused cloud's artists OPEN = "zoom reveals detail" not re-crush.
               New checks I(membrane present+contained) K(lean informs: kin<stranger) + zoom-reveal.  Probe = key
                the lean on node.NAME not signature → K RED (kin 14°≈str 12°) → reverted green.  Regression all
                 6 = 160/160.  Full-script PARSE-checked (pure+dom via `new Function(body)`) — the browser DOM
                  can't run in-container, so parse is the syntax gate for the [dom] half.  Republished same url
                   via `url=`.  HUMAN SEQUENCE (load-bearing): "one more reply then put me on the LONG Vyto build
                    while out" — the NEXT genuine reply is the trigger to START the Vyto bridge (provider seam +
                     Vyto face + a Book proving carve/seat/lean/membrane/no-spill on LIVE library data).  Do NOT
                      start before that reply.  Owed by the human's eye: does the lean read as informing or noise
                       · membrane graphic enough · zoom the right amount.

**foam5 (2026-07-26 · NEW url 59dff932 · the START reply FIRED and REDIRECTED the build):** the human's
 trigger was "do it, keep going for hours · there's NO 'live library data' that I know of · keep working to
  VARIOUS SHAPES of C** · SET THE QUANTITY OF OUTPUT for the size of the screen — phone users hardly see many
   cells but the UI pieces should be COHERENT still."  So the live-data Book plan above is SUPERSEDED — there
    is no live library; the "Vyto build" is now (i) exercise the skin on many C** TOPOLOGIES and (ii) make the
     visible-cell COUNT a function of the GLASS.  foam5 = new path (foam4 kept as reference), foam4 core reused
      VERBATIM, + two proven claims: **SHAPE-BLINDNESS** (five deterministic seeded fixtures — library/balanced
       3×3×3/fan-10-wide/deep-spine/ragged — the SAME carve/seat/lean/membrane draws each; study-four invariants
        run PER SHAPE and hold at 78° AND 58°) and a **SCREEN BUDGET** (`budgetFor(glassArea)=area/QUANTUM²×PACK`
         → target cell count phone 9 / tablet 15 / desktop 35; `foldFor` greedily crushes the tightest frontier
          family first, never focus/ancestors, until it fits; COH=44px backstop kills any residual speck).
           DEMOTED the world-relative FLOOR=175 as the folder (it folded identically on every screen so quantity
            did NOT track the glass) — the BUDGET is the knob now.  Measured: library phone 9 cells (min 78px) vs
             desktop 30; every shape phone<desktop; every drawn phone cell shows its name ≥ MIN_FONT above the
              30px draw floor = "coherent still".  Gate ×53 GREEN; probes bit (QUANTUM 118→22 + COH 44→2 → no
               fold → phone==desktop + real specks; mainkey .toUpperCase() → aligned-key 0/N) then reverted.
                **Phase 2 (LANDED 2026-07-26 · gate ×53→×68):** leaf BUCKETING built + a genuinely FLAT fan
                 fixture (30 leaves direct off root).  Fold LADDER per family now OPEN→BUCKETED→CRUSHED: a wide
                  leaf-family partitions by `bucketKeyOf` (the sc key that best splits it — year/genre) into
                   `node.buckets`, laid as a coarser power-diagram over the SAME inner region; when leaves would
                    be specks OR the family is the shielded FOCUS (a flat fan can't be crushed), it shows K
                     aggregate bucket cells ("year 2019 ×8") not N specks / not one collapse.  Greedy PREFERS
                      crush-to-crest (clean foam4 look); bucketing is the fallback → desktop stays crisp (0
                       buckets), only the flat fan on a phone aggregates (30→4; desktop shows 30).  Bucket cells
                        wear a dashed wall + faithful Type×N dip.  Probe bucketKeyOf→null → 6 flat reds →
                         reverted.  Standing: keep going for hours · NO check-in · NO commit.

                **Phase 3 (LANDED 2026-07-26 · gate ×68→×81):** THE LAYOUT-PROVIDER SEAM — the skin is now
                 shown LAYOUT-BLIND as well as shape-blind.  `whisperPositions` went from `const = forceGroup`
                  to a swappable `PROVIDER` behind `setProvider(name)`, four shipped: force · sunflower (the
                   golden-angle phyllotaxis — the human's dropped sunflower idea, back as an OPT-IN provider,
                    not an imposed layout) · rings · rows.  A provider only returns a seed per child in the
                     region; carve/power-cell/fit are blind to it, so tiling · cascade · no-spill hold for
                      EVERY provider (proven per-provider) while only sibling packing changes.  This IS the
                       "provider seam" from the original Vyto-bridge plan — buildable with NO live data.  Gate
                        +13 (incl. "providers genuinely differ" >20px, guarding a no-op seam); probe
                         setProvider→always-force → differ red → reverted.  foam5 now proves ONE skin is
                          shape-blind (8 shapes — 7 topologies incl. a pure deep single-child spine + a mixed-
                           mainkey/no-vein SEMANTIC adversary) × layout-blind (4 providers) × screen-budgeted +
                            flat-fan bucketing · gate ×109 · url 59dff932.
   **2026-07-26 human-steer round (same url):** (1) PRESENCE — cells were reading "20% opacity"; brightened
    typeFill + strokes + open/header fills, softened the vignette (dropped mix-blend multiply), lifted gesture
     text 0.6→0.85.  (2) MECHANICAL KEYS — each fact "key: value" now splits (DOM-only, in drawSpec) into a
      mono .fk KEY tspan (--key) + proportional .fv VALUE tspan (--val), so the sc-key label is a distinct
       mechanical layer.  (3) THE ANCESTOR CRACK (drawCrack) — on dive, ancestors aren't repeated as flat
        breadcrumb text; they twist onto a jagged fault-line up the focus cell's left edge to its top-left
         corner (viewBox-space #crack layer above camG, redrawn per tween frame — NO relayout), each landing a
          hue-tinted clickable mk+name leaned -78°.  All DOM-half → pure 109 stayed byte-identical.  Can't
           screenshot an Artifact, so added foam5_crack_smoke.mjs (headless DOM shim, 72 green, finite-coord
            gate probed→red→reverted).
   **2026-07-26 cont (same url):** crack REDESIGNED to a TOP-LEFT STAIRCASE (his steer: "more left-right and
    top-bottom, gathered top-left leading into the cell; labels were aligned + overlapped a lot") — ancestors
     step RIGHT+DOWN (Manhattan treads/risers) into the focus corner, each on its OWN step so horizontal labels
      never pile into one column; anti-overlap gate added (step moves right+down by > a label height; probed).
       Then the FOCUS WARP (his "warp surrounding cells a lot better → gathering occurs naturally"): opt-in
        `warp` chip, DEFAULT OFF; magOf(child,foc) scales the child's power-radius — branch-to-focus ×2.6,
         off-focus siblings ×0.4 — at every container on the path, so focus swells + context compresses, ALL
          through powerCells (tiling exact, labels re-seat normally). layoutTree gained optional `foc` (argless
           = base, so 109-check untouched); camera self-compensates. Pure gate foam5_warp_check.mjs 8 green
            (focus swells / sibling compresses / area conserved / reversible / symmetric), probed→red→reverted.
             MECHANIC proven; AESTHETIC is the human's to judge (can't screenshot an Artifact). DEEPEN if he
              likes it: re-lay at K magnifications ACROSS the tween (his keyframe idea) so the reflow animates;
               trade camera-zoom for warp. Knobs: WARP_BOOST/WARP_SHRINK. Gates: check(109)+crack(133)+warp(8).
 **foam5 cont (2026-07-26 · same url 59dff932):** human steered live — zoom TIGHTER (setCamTarget 0.6→0.9/cap
  3→5.5) + RESERVE a depth-sized top-left band and seat focus in the free area (down-right) so the ancestor
   stack is ALWAYS fully on-glass (the camera makes the room, not a clamp); CRACK v3 = left-aligned KEY-column
    (every mainkey at one x — his "mainkeys aligned"), rows clamped above the corner, fault line juts in (dropped
     the right+down staircase); WARP now DEFAULT-ON + stronger (3.2/0.34), chip toggles off; TEARDROP skin =
      terminal cells pinch a spout toward the parent (teardropPoly LERPs parent-facing verts toward CENTROID ⇒
       provably inside the tile, reversible; open containers keep their frame), `Skin→teardrop` chip default-on,
        knob TEARDROP=0.6. ALL DOM-half ⇒ pure 109 BYTE-IDENTICAL. Gates: check(109)+crack(151)+warp(8)+skin(43,
         NEW foam5_skin_check.mjs); GOTCHA fixed — clearance() returns 0 for OUTSIDE points so a no-spill gate
          must use pointInConvex not clearance≥0 (a spill probe waved through until I switched). Each new gate
           probed→red→reverted. DEFERRED (his eyes): open-container teardrop (header sits on the pinched side) ·
            protruding spout into the inset gap · the keyframe warp animation.
 **foam5 cont2 (2026-07-26 · same url 59dff932):** human live — (a) intro paragraph squished the demo → trimmed
  to 2 sentences + tighter header; (b) drilling into a %Track dropped detail. DIAGNOSED (diag harness under the DOM
   shim): the real bug wasn't fonts — focusing a leaf could BUCKET its parent family and HIDE the focus leaf
    (threshold-sensitive on COH, so which track you click decided if it vanished). FIX = foldFor never buckets a
     STRICT ANCESTOR of the focus (ancOfFocus guard, coherence loop + while bucket move) — a PURE change but a
      NO-OP for focus===TREE ⇒ 109 byte-identical (probe-verified). Plus seatedChunks `present` mode (focus leaf
       only, optional param ⇒ pure identical): all rows + relaxed floor + stack compressed into the pole disk so
        nothing runs into the taper. (c) PRONOUNCED SPOUT: teardropPoly now inserts a real TIP vertex at 0.97·reach
         toward the parent (rayHit ray→boundary; FIXED a Cramer's sign slip), bounded by the TRUE tile (convex ⇒ no
          spill), cells rounded ×1.7 (less triangular). Gates: check109+crack151+warp8+skin44; tail gate = "spout
           tip present" (honest — the tail is a thin sliver so centroid-shift/sharp-perp/overshoot metrics were all
            wrong). GOTCHA: the single convex tile caps a no-overlap spout at ~14% of reach — DRAMATIC teardrops
             need the human's NEXT idea, THE FAT-LIMB WEAVE: fold a limb into a raft of nodes+edges (cytoscape-per-
              limb) necking to the parent, not one voronoi cell. Not tile-bound. NOT yet built — held for his read.

**Build order after rulings:** VytoBreathe (④ tenancy + Express inversion) → floor-law TENANCY
 (mechanics ALREADY PROVEN — the **Floorlaw** Book, Voronation.g 2026-07-21, green ×3 + red-gated:
  crush-to-crest via Stuff_distil · one-directional anti-flutter AT the exact re-admission point ·
   across-pass un-crush by frame = the surf kernel AND by importance) → VytoNest (solve recursion
    into cell polygons — port the structure artifact's numeric checks) → VytoSurf (reframe as a
     shift) → runner_shot demos + a Credence visual sub-What.  Key fixed-point argument (J1):
      fold/crush only LOWERS demand within a pass so S rises monotonically — un-fold only across
       passes under hysteresis; Typescale AND Floorlaw both proved this loop green.  Artifacts: structure https://claude.ai/code/artifact/cdccea4b-020b-4238-8767-a1c140a0f79c ·
      rosette/vein https://claude.ai/code/artifact/87eb6234-e2d7-495e-9d57-af1a57d73153.
       Related: [[stuffing-stho-reexpression]], [[g-authoring-gotchas]], [[vyto-first-tenant-gated]].
