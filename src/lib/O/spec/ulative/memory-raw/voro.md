---
name: voro
description: "Voro (the legacy voronoi glass, being superseded by Vyto) — fold is a viewer imposed at snap-time, Book Voro-blind, the model layer + %Se phase-provenance, ▦ face 6 = the gem, VY=1 retiring"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2f2e32df-77de-4509-9ea4-3454f9ecd5a7
---

Voro is the legacy voronoi-glass fold engine (`Ghost/V/Voro.g` + `Cytui.svelte`), **being superseded by Vyto** (the human's thread: "that becomes useVyto"). Durable facts (chronology shed):

**Fold ("crush") is a VIEWER, imposed from above like Cyto/Matstyle — the Book contributes ZERO lines.** Switch = `useVoroCyto` (a Story toc Opt, rides the Cyto commission). Mechanism = impose at **SNAP time** (Phase 3 snap_step/snap_H), NOT inline `.g` calls, NOT Cyto's wave (too late). A Phase-1 witness can only read a Phase-1 fold — that timing is why the fold once looked self-selected. **Book is Voro-blind**: no fold-`%see`; diagnostics live in `w:Voronoiology`; `dontSnapVoronoiology` prunes that world from the snap. The ◈ button (`e_Cyto_crush`) is a LIVE explore lens on non-opted views only.

**The MODEL layer** (`Voro_model` in Voro.g, crush tail on census-authoring passes) = the semantics a consumer reads instead of re-deriving from pixels. Off-snap tree on `w.c.voro_model`; snapped distillation in Voronoiology. **HONEST LANGUAGE = MAINKEY IS PROVENANCE**: rows `Family:<name>,n,kind,order_by,axis,from,to` + `Loud:<key>[,v]` children (loudest-first = rank) + an `Se:drift` child ONLY on a beat movement was reported (absence IS the quiet reading). **`%Se` is worn ONLY by Selection-derived readings, at PHASE level not per-field** (`input|scape|census|drift|attention`) — computed census wears plain names. `Family` = the FOLD UNIT (a gang explodes to its members), not the grasp region. Semantic order from the DATA (`Voro_model_axis` = widest-spread fact-key). "The model" = ALL Voro data representation; Voronoiology = its snapped face; the %Seems are its MEMORY.

**The drift-Seem** (per-family neu/gone via `%Seem:model` over the member layer): PERSIST the Seem across the per-beat drop-and-rebuild (spare `mainkey!=='Seem'`) — its D-sphere is the baseline; SUPPRESS the first resolve (fresh Seem = baseline beat); ATTRIBUTE by stamping `fam` on the member's own sc (resolve hands back a detached D-clone, `c.up` null); resolve matches members by POSITION not anchor value.

**The glass faces gallery**: the ▦ sub-graph pane tessellates each fold/gang cell into sub-cells (bamboo v2). Modes evolved through several passes; the human named **face 6 "the gem" (a cell-wall around its nucleus)** — all saved in `spec/voro_modes/` (revive any via `git show <commit>:src/lib/O/Cytui.svelte`). One grammar rule-set recurses at every level (key=vein hue, colon lilac iff value follows, mainkey=kind badge, ×N=violet superscript). **`runner_shot.mjs --svg`** ships the live glass as greppable standalone SVG — the geometry verification instrument. **VoroTest** (`Voronation.g`) unit-harnesses the guts with NO Cyto (`Voro_crush_scan(w,quiet,fixed)` pins the level); each `%Example` snaps as (input world, output world) side by side.

**VY=1 / first-tenant**: the `?VY=1` boot param is WRONG-SHAPED and retiring — migrate per-Book from within the app, not via a boot flag. Related: [[voronoi-cells-render]], [[fight-back-on-core-changes]], [[host-commits-midsession]], [[vyto-refactor-avoid-display]], [[snap-data-not-judgement]].
