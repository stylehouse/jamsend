---
name: tuples-face-snap-notation
description: "the ▦ sub-cell has THREE faces via the vsub_face PARAMETER (not a button): 'tuples' (DEFAULT — snap-notation rows in the cell) | 'star' (radial gem) | 'phi' (PART 9, 2026-07-17 — the GOLDEN LETTERING LAYER: one groove spiral over the WHOLE glass, every label claims an exact arc interval so text NEVER obscures text; misses fold to +N chips, click = 3× reach; glass beneath = underpainting; lattice = flat reference the warps read against); flip via runner_shot --arm --face=vsub:star|phi. AWAKE PUNCH-LIST (2026-07-15, human hard-reloaded + seeing it live, SUPERSEDES the P5/J box work): kind chip FULLY DE-BOXED → a plain colour-coded word ('standard'); BOTH bottom badges gone (/*N dip = one-way Vtuff_pop trap; +N hid unpainted but kept as spell DATA); cradle RIM = container tint (was invisible when hues matched); header gets norot on pane_rows (was drifting into a slanted corner); spell floor 11→12 + ceiling 2.4→3.0 (bloat-up); spell FREEZES while vdrag (drag-blink). R<40 crater gate HELD. K-ROUND (PART 8, 2026-07-15): crater REDRAWN as explicit NESTED BASINS after 'no, nothing like the Cradle I'd like' — Artist basin = 2 stepped inset rim contours (vsub-abasin/-abasin2 around c.inset, fill:none) + carved-lip shadow; the Track nucleus (old coag) nests INSIDE = basin-in-basin draws Artist⊃Track, no longer leans on the cell background. VoroScape given Track/What/What depth (Tide→the-spot→cymbal) via LocalGen (editor down). Coprosma-LIE ORDER FIX LANDED (PAINT-ONLY, no fixture drift): root cause was Cytui tuple_rows REVERSING the distiller's honest order — Vtuff_default emits title→members→keyrows but tuple_rows led with facts, so facets read ABOVE the species as genus content; FIX = a homo gang's bare members flow directly under the identity, their pooled traits AFTER ('across these'); cross-kind folds untouched (members in a lead tuple, members[] empty). DEEPER per-member-facts-together (sub-basin per species) still OWED = crater/C-spaces path. Full detail = Voro_todo §0 PART 8. C-cradle = Artist tint wraps a member-tint nucleus on 3 sides opening right; the GROWTH SPELL grows starved cells"
metadata:
  node_type: memory
  type: project
  originSessionId: 12f2682d-1c3e-4f5e-ba2c-7e09ee65b139
---

2026-07-17 PART 9 — ϕ THE GOLDEN LETTERING LAYER (face 'phi') + zone_seat (full detail Voro_todo §0 PART 9). The human: "cast a phi sunflower grid onto the cells, assign continuous spaces across the grids with another bit of give+take basically ignoring cell boundaries, then we completely obscure what we have drawn so far with this other… labels stretched onto a phi spiral! with connective tissue showing expandy regions" + the rationale "phi is easy to see kinks in but not overly griddy — communicate a warping data-landscape". BUILT (`phi_build`, Cytui): ONE Archimedean groove spiral (PHI_G=17) over the whole glass; labels per cell off the SAME Vtuffing descs; each claims a contiguous arc — ledger = total-spiral-angle intervals → same-groove overlap is an EXACT check, fs capped below pitch → **text never obscures text by construction** (the human's "we must KNOW if we're obscuring"). No-fit-within-budget → +N chip at the cell, CLICK = phi_expand → 3× reach (the expandy region). Dashed ties thread displaced labels home; the faint lattice = the flat reference. Under ϕ: panes build but stats BLANK (walls/craters = underpainting), spell GATED OFF (blanked stats would read as global starvation → storm), layer FROZEN mid-drag. Census on the vsub vlog (`phi:{labels,placed,folded,boosted}`). Same shift, pre-ϕ steer: `zone_seat` REPLACED pane_rows in tuple_pane + crater_pane seats (territory-first: height divides into weight zones, text sized to FILL its band; member lists split into 5s) + gates opened (crater R<24 was 40, mem≥1 was 2, degrade area<900 was √<88) — 'tuples' face UNSEEN live since that swap. UNVERIFIED-BY-SHOT both; verify = hard-reload + `runner_shot --arm --face=vsub:phi` + `--why`.

2026-07-14: the human's steer — "WHAT I REALLY WANT is ALL the info, arranged so I can read the notation that looks similar to snap but with multiple rows squished together … some venn diagramming or similar visual tricks (like treeing — it's VERY BASIC!)" and "a parameter (not in the UI because we'll probably take all this away to another UI) where we can make it this gem-like star formations, or the new default: somewhat more like a C structure, tuples … using something like but possibly not exactly cs to make those tuples aligned and hang in the same direction across places, but they should mostly favour the shape of their cell, to align text with the biggest top-left-est cell wall".

BUILT (Cytui.svelte, render-only): `let vsub_face: 'tuples' | 'star' = 'tuples'` — a PARAMETER, not a bar toggle (the human: it's prototyping, the real UI comes later). `subgraph_build` branches: `total<2||tiny` → nucleus_pane; `vsub_face==='tuples'` → `tuple_pane`; else the radial `star` gem (unchanged). Flip live via `runner_shot --arm --face=vsub:star` (op:'face' takes `vsub`), or the `Cyto_vsub_face` stash.

`tuple_pane`: says the distiller's tree as SNAP-LIKE STACKED ROWS — title (kind badge · name · ×N), then keyed rows in GLOBAL vein order (`vein_slot` — same key at the same RANK in every pane = "hang in the same direction across places"), each `key:` + value chips, then members flowing; wraps with a one-em TREEING indent; over-wide atoms shrink to a 7px floor, the rest folds to `+N` (never clipped `…`). Density beats the 14pt floor here (rows 9–15px) — the notation wants ALL the info. `tuple_frame(poly)` picks the cell's biggest top-left wall (len × top-left-weight × readability, upright-normalised, quantised 15°) and the whole pane's baseline rotates to it — "favour the shape of their cell". Each atom is its own VStat (member chips keep their click/pop).

LIVE-PROVEN on VoroMitosis (`/tmp/nightshift_gallery/tuples_clean.svg`): 13 panes, 141 labels, 0 ellipsis, rotations −45…+30. Reads e.g. `Metrosideros ×11 / habit: shrub ×3 tree vine ×4 / woodystem ×3 / propinqua rhamnoides grandifolia …`.

2026-07-14 FIRST REAL-WORLD READ (Artists|Tracks) — the human caught the FLATTENING LIES ("this new graphic is great! but…"): rows forgot WHICH C they came from. (1) %Track kind lost — homo list Vbits carry v: only, no tag (the "tag rides ALWAYS" honesty is the MIXED branch; a fold's title tag is the CONTAINER's mainkey 'Artist'); (2) %title key lost — Voro.g DOES emit `row:'list', k:'title'` but Cytui subgraph_tuples' list branch DROPS d.key; (3) sc ORDER lie — glass sorts by global vein rank + localeCompare, not mainkey→title→year|live|remaster; (4) title ×N lie — reads as count-of-such-Cs, honest mark = /*N at the SUB-CELL tail, `/` open `/*4` closed; (5) /*N click spills-to-graph + collapses to bare title, lands SLOWLY (suspect: Story finished → beliefs() quiesced → re-crush waits on a random think; needs a think-nudge), and NO reopen affordance. THE RESOLUTION: **bamboo v2 = C-SPACES** — the human shelved bamboo v1 2026-07-11 with the redirect on the lab door (`Vtuff_bamboo_on()` hard-false "until bamboo v2: hierarchy expressed as sub-cells|sub-graph, not text stalks") and today's steer IS that spec: encapsulated per-member-C sub-regions, voronoi'd within the cell, ANCHORED AT ACUTE CORNERS, NOT cytoscape inner layout (headless cy workers a maybe), each sub-cell its own mini tuples pane in its OWN sc order with its own /*N tail; retires the display-side namekey by architecture. Full phased plan (B0 honesty → B1 C-spaces → B2 surf lifecycle → B3 identity floor) = Voro_todo.md §0.

2026-07-14 B0 HONESTY LANDED + LIVE-PROVEN (VoroClinic, useVoroCyto=1, clean toc, seeds Artist{Alpha}⊃4×Track{title}). All four lies fixed: B0.1 Voro.g Vtuff_member_rows threads `src` and tags the homo list with the member kind when it differs from the container title kind (mk!==ck); B0.2 Cytui vtuff_rows captures d.key for `list` rows (was fact|spread only — the title key died at the read); B0.3 subgraph_tuples routes a TAGGED list (cross-kind fold) to a keyed LEAD tuple {tag,lead:true} while a same-kind list (flora genus) stays bare trailing members (gate on d.tag NOT d.key — d.key gates too broad regressed flora into a redundant 'Genus:' row), and tuple_pane orders keyed by lead-first stable partition (desc==sc order) not vein sort (vein now hue-only); B0.4 title text = name only, ×N dropped (redundant with the /*N dip, same root.sc.n) so it never reads as "N such". Live shot read: `⟨Artist⟩ Artist name: Alpha / ⟨Track⟩ Track title: a1 a2 a3 a4 / tail /*4`. GEN: ba725387. Flora (VoroMitosis same-kind) unregressed. Cross-kind live-verify Book = VoroClinic (NOT VoroTest — useVoroCyto=0, builds data but no voronoi glass; NOT VoroScape — dirty toc, checkout would destroy the human's re-record).

2026-07-14 B1 C-SPACE CRATERS BUILT + LIVE-PROVEN (VoroClinic). A big cross-kind FOLD cell (%Artist⊃%Track) divides into per-member sub-cells so you SEE where each C is. Cytui: `pane_rows(poly,cx,cy,rows,opts)` = the tuples flow extracted to run on ANY convex polygon (rotates to the poly's OWN best wall, line_span chord-intersection, inflate); `tuple_rows`/`member_rows` build the atoms; `tuple_pane` = pane_rows on c.inset; `crater_pane` = CARD layout — a full-width HEADER BAND placed BELOW the cell's WIDEST chord (a pointy top would drown the identity — the acute-corner starve), members grid-split the body via `power_cells(bodyPoly,…)` (aspect-aware cols, even — a phi-spiral of few seeds gave 29px beside 7px), each member its OWN mini pane in its OWN sc order + wall. SELF-GATES: if the header title won't seat ≥11px, return null → caller falls to flat tuples (small cells read better flat). Wired into subgraph_build tuples branch (craters first, tuple_pane fallback). A/B via `runner_shot --face=craters:0|1` (vsub_craters flag + Cyto_craters stash). Live: Beta (big cell) craters — 28px header + 4 Track sub-cells; Alpha (small) falls back flat 28px. Reuses power_cells + clip_halfplane (programmatic, NOT a 2nd cytoscape — the human's ruling). GEN unchanged (Cytui only). OPEN taste (the human's eyes on real BIG-artist data): member evenness still wobbles (b1 9px vs b2-4 22px); shadows-for-depth + tunnels-between are the human's noted north-stars (not built). Verify Book = VoroClinic (VoroTest useVoroCyto=0; VoroScape dirty toc).

2026-07-14 B2 PROBE (code, no headless-click path): the human's "/*N collapses really slowly — has Story shut off beliefs()?" CONFIRMED + pinned. Vtuff_pop stamps c.popped then `await cyto_update_wave(w)` (fires on the click), BUT cyto_update_wave (Cyto.svelte:230) re-crushes ONLY `if(scan.c.crush_wanted)` — armed each Book step, NOT reliably after Story finishes → the wave re-SCANS but doesn't re-CRUSH → the spill (popped members→cells) waits for the next think that re-arms crush_wanted. Candidate fix: set w.c.Scannable.c.crush_wanted=1 in Vtuff_pop before the wave — but arm-then-verify with a LIVE click, DON'T ship blind into the human's live pop. B1 in-glass toggle sidesteps it (no spill). B3 identity floor = LANDED as the crater self-gate (header <11px → flat fallback). Lloyd relaxation for crater evenness REVERTED (sound but unverifiable-benefit on VoroClinic's small noisy cells + 3× power_cells cost) — noted as the lever to try on real big cells with pixels.

Second steer same day refined B1: frame = the EXISTING Stuffusion→Stuffziad→Stuffziado series (Stuff.svelte.ts, `innered`/`inner_sizing` recursion hooks; Voro_vtuffing.md already calls Vtuff_default "that compression RE-SAID AS ROWS" — B1 says it as SPACES), RULED programmatic geometry not pinned-cy-nodes (reuse phi-spiral splitter + poly_chord), VERIFIED model has Artist+Track while cy nodes only Artist (no_further='stuffed'), and THE SPILL LEVER exists = c.popped/c.popped_open (Vtuff_pop stamps, crush_walk/Voro_crushable read) — today binary folded↔spilled; B1 adds the middle state open-in-glass, so /*N toggles closed↔in-glass and spill-to-graph becomes a deliberate separate gesture.

2026-07-14 THREE TASTE-FIXES landed (human: "yay … now it just needs inflating into its potential space … a line obscured by the cellwall above it, and a 150° down angle"): (1) INFLATE — `tuple_pane` is now two-pass: `layout(zoom)` lays out fresh, measured at 1× for natural stack height `used`, then re-run at `zoom = min(2.4, 1+(availH/used−1)·0.7)` to fill empty vertical space, kept only if the bigger pass still seats everything (`up.hid===0`). Sparse 1-member cells now 22–38px, dense stay 12px. (2) NO WALL-CLIP — `line_span(ytop,lh)` = INTERSECTION of the chord under the box top and over its bottom (was centreline-only, so ascenders poked under a sloping top wall); a line steps down until its whole box seats inside both walls. (3) NO STEEP BASELINE — `tuple_frame` now REJECTS any wall >45° (`Math.abs(th)>Math.PI/4` → continue; was only soft cos²-penalised, letting a +60° wall = the "150°-down" through); no qualifying wall ⇒ horizontal. Verified `tuples_v2.svg`: only 0/−15/+30°, 0 labels over 45°.

2026-07-15 AWAKE PUNCH-LIST (LATEST — the human hard-reloaded, cache cleared, seeing it LIVE; SUPERSEDES P5/J where they disagree; all Cytui paint-only, gen unchanged; full detail Voro_todo §0 PART 7):
- **KIND CHIP FULLY DE-BOXED** — the human's last word walked the whole J-round home: "the soft tinted field … I don't want it, just the regular colour-coded key visual … standard!". Dropped `box:true` at both mint sites, the `vsub-kindbox` rect, the `vsub-kindlabel`/`vsub-kindbox` CSS, and `box`'s atom.len reserve. The kind is now a plain word coloured by inline `kind_tint` — reads like any key by its hue. The whole box saga (J2 pill → P5.1 soft field → gone) ended at "no box at all".
- **BOTH BOTTOM BADGES REMOVED** ("the +1 and /*3 … I'm not wanting"). `/*N` dip: `pane.dip` no longer assigned — it looked like a fold-OPEN control but `Vtuff_pop`'s stamp is ONE-WAY ("opens, no way back") = a trap. `+N` hid: no longer PAINTED, but `sp.hid` still computed as DATA — the growth spell reads it to keep swelling the cell until everything seats (heal by GROWING, not by confessing a count). Dead `.vsub-dip`/`.vsub-hid` CSS removed.
- **CRADLE MADE TO READ** ("the C-cradle — I don't see at all … supposed to be around every C?"). The C is drawn by the CELL BACKGROUND (arm) around the nucleus fill — invisible when the container hue ≈ member hue. FIX: rim the nucleus in the CONTAINER tint (the Artist), opacity .5→.85 / width 1.3→2. The wrapping level now draws its own wall regardless of hue family.
- **HEADER NO LONGER CORNER-DRIFTS** ("the Artist … renders nowhere / clipped / drifted into a sharp corner"). `pane_rows` rotated the identity baseline to the biggest wall → tilted into a slanted slice's point. FIX: new `norot` opt (th=0), set for the header arm only — identity reads LEVEL; nucleus body still follows the wall.
- **TEXT BLOAT-UP** ("just a bit more"): spell legible floor 11→12, ceiling 2.4→3.0 (also floats more cells past the crater gate → more cradles surface).
- **DRAG-BLINK FROZEN** ("only occasionally … during dragging it blinks"): `spell_update` early-returns while `vdrag` — a grow/shrink verdict can't land on a drag repaint frame.
- **STILL OWED, human's eyes only (don't blind-tune):** (a) is the cradle on ENOUGH C's? — I HELD `R<40`; if their cells are just small, that's the knob to drop (→~32) AFTER they OK the rim+header on the ones that crater. (b) did rim+norot actually kill the corner-drift? — reasoned not shot-verified. (c) the text-in-polygon "should we import help?" is a real fork (our pane_rows is a hand-rolled chord-seat+inflate solver; a constrained-layout lib is a genuine option), parked not a bug.

2026-07-15 LIVE SESSION (the human at the desk, firing steers over HMR — READ THIS OVER the FINAL/P5 steer where they disagree; all Cytui paint-only):
- **DRAG THE ARTIST** ("make the Artist draggable, wire the drag to the cytoscape node underneath that cell"). The crater IDENTITY (`vsub-ntitle` text) is now a grab handle (cursor:grab; opts back into the pointer-events:none layer). `vsub_grab/move/drop` set `cy.getElementById(sp.id).position()` + `pan_zoom_motion()` (live re-tessellate, same path as the gravity brush `brush_wheel`); WINDOW listeners survive the re-render the drag triggers; yields to a running layout (`live_layout`) like the brush. Grabbing sp.id moves the RIGHT node (a voronoi seed sits anywhere in its cell, so a pass-through canvas grab is ambiguous). This is the manual way to free a TRAPPED cell — the human's own alt to an auto-repulsion spell (parked).
- **THE C-CRADLE crater** (the human picked it over "artist-bowl" and "nested craters"): the crater is a literal C — the coagulate is a NUCLEUS inset to the centre-right (`clip_halfplane` ×3: y≥yTop, y≤yBot, x≥xLeft), and the cell's OWN Artist tint wraps it on THREE sides (TOP arm holds the identity, LEFT spine + BOTTOM lip close the C, opens right). "A cell-wall around its nucleus." Replaced the H-round header-band split + the I-round chord huddle (the identity sits at the top BY CONSTRUCTION now, so same-kind cells huddle for free). ahTop/ahBot/aw are cell fractions so the C scales with the spell. R<40 (a C-nucleus insets 3 sides → needs a bit more cell than a band).
- **THE BLINK WAS `wants_crater`** — my "grow-then-crater" flagged every readable flat fold to grow → a zero-sum re-paint STORM (grow A shrinks B, B begs, shrinks A…), and each re-paint flipped cells crater↔flat → toggled the Stuffing overlay opacity (3538-3541) = "very blinky … makes the overlay disappear". REVERTED wants_crater. KEPT the htitle<11 legibility gate (small folds stay FLAT-and-readable, not cratered-tiny — the "text is very small" fix) + the existing `tiny()` spell (grows a genuinely-tiny flat fold until it can crater). LESSON: never add an always-hungry class to the spell (it can't grow everything — zero-sum area).
- **SPELL HYSTERESIS** ("turn itself off only once the cell becomes even bigger than the size — it was oscillating on+off"): the grow/release gates were too close, so a cell that grew to fit read as roomy and shrank back into starvation → flicker. Now: GROW while starved, HOLD once it seats, RELEASE only when the cell is clearly oversized (`roomy` in tuple_pane+crater_pane = used < avail*0.42, was 0.6); gentle steps (grow ×1.15 / shrink ×0.95); a cell that grew this pass is never shrunk in it. Wide hold band = no oscillation.
- **HOVER-ESCAPE** ("the hover effect can get stuck on … a catch to escape it"): a mid-hover re-tessellation strands the correspondence reveal — `corr_hot` lights members by STABLE particle ref and `ribbon_pane` by cell id, so a lost mouseleave (the element was destroyed under the pointer) sticks. `corr_clear(all?)` drops the transient reveal (all=also the pin); wired to **Escape** (a top-level `<svelte:window>`, NOT nested — svelte errors otherwise), to **leaving the graph** (wrap `onpointerleave`), and to **drag-start** (the biggest strander).
- **CANARY**: appended `?` to every kind label (`{s.tag}?` in the vstat snippet) as a propagation test while the cache is stale — REVERT once "Artist?" is confirmed visible.
- STILL can't shot: all runner tabs (49dee91d, 20e3476b, 3c5238c6, 77d26228) are on a STALE bundle — but Vite serves my code FINE (node-fetched the bundle: vsub_grab/CRADLE present, wants_crater=0). It's a CLIENT cache pin (`runner_ask reload` is a soft reload → can't bust it). Fix is human-side: HARD reload / unregister SW / restart vite. See [[hmr-socket-dead-tell]] for the full diagnosis ladder. Everything this session is UNVERIFIED-BY-SHOT — review on hard-reload (verify list in Voro_todo §0 PART 6).

2026-07-15 FINAL steer (walks the J-round back — READ THIS OVER J2/J3; all Cytui paint-only, gen unchanged):
- **P5.1 de-button the kind chip** ("the button-looking mainkey is more confusing and visual noisey, though that that's the start of a C (eg the inner Stuffing Track) is clear"). J2's pill was filled+STROKED+pill-rounded with a `textLength` glyph-STRETCH — that chrome IS the button. Now `vsub-kindbox` = **no stroke**, fill-opacity .13, small radius (`fs*0.16`), hugging the word; `vsub-kindlabel` drops the stretch+tracking, natural width, weight 500. Still "a C begins here" (tinted field = the TYPE), no button. J3's stretch is REMOVED (the "Michaelangelo stretch text into place" want lives on for row justification / the polygon-fit research, NOT the kind label). `box` atom.len reserve 2.2→1.0.
- **P5.2 the name-drop** ("sometimes Artist only appears, no name:Riverine"). The identity title is two atoms `[kind-chip][name]`; in a ~1-line header band the name WRAPS then hits the vertical floor and gets DROPPED (`flow` → `dry`, counts to hid) = a lonely kind. Fix, two mitigations: the de-buttoned chip is narrower (wraps less) AND the crater header floor rises **26→42px** so a wrapped two-line identity ('Track' / 'name: Riverine') has a landing (honours the H2 wrap).
- **P5.3 crater ALWAYS for a fold** ("the crater should always be there! that'll communicate it better") — reverses the I9 "flat fallback is a fine outcome" stance. `R<60`→`R<34` (sliver-only) and the header `htitle<9 → flat` self-gate REMOVED. A cross-kind fold KEEPS its crater even a beat cramped; the growth SPELL (tiny<11) swells it legible over beats — communicating the fold beats waiting for room. `mem.length<2` (no fold structure) + pure-geometry gates (`!hres`/`coag<3`/`!bres`) stay (un-drawable → flat, and the spell still grows a flat cell toward a future crater). VERIFY: **LIVE SHOT STILL OWED (blocked)** — the one idle machine-runner (20e3476b) is wedged on a STALE cached bundle (`location.reload` won't cache-bust → `no cy_face hook — old Cytui`), and the only fresh tab is the human's ★editor (HMR-live, off-limits to a shot). Type-clean, paint-only/fixture-safe.

2026-07-15 J-ROUND (the human, after "hover behaviour is really nice" — three asks; all Cytui-only, paint-only, gen unchanged):
- **J1 the gap** ("Tracktitle: is what it looks like, they need a gap. this is in the inline mode"). The gkey atom held tag+key in ONE centred atom and the separator was a bare `<tspan> </tspan>` which SVG WHITESPACE-COLLAPSES → "Tracktitle:". Fixed two ways: the kind is now its OWN atom (J2) so the flow spaces it from the key, AND the inline separator is a non-breaking space ` ` (defensive, for the members-row/star paths where tag+val still share an atom).
- **J2 the kind in its own subcell** ("always put eg Artist/Track in a subcell — they look like properties on the Artist otherwise"). The kind label (header Artist via tuple_rows, gkey Track via keyed_rows) is split into a separate `box:true` atom; the vstat snippet draws a tinted rounded-rect PILL (`vsub-kindbox`, fill-opacity .16 + tinted stroke) behind it and renders the label full-size (`vsub-kindlabel` 86%, weight 600) instead of the tiny inline `vsub-tag` (76% grey). So the kind reads as the TYPE/container, not a property. The DEDUP still holds (only the first row of a tag run mints the boxed kind). `box` adds +2.2 to atom.len so the flow reserves the pill's padding.
- **J3 Michaelangelo (first cut)** ("we still need to Michaelangelo up something about stretching text into place"). The boxed kind label STRETCHES via `textLength`/`lengthAdjust=spacingAndGlyphs` to fill a min-width pill (`max(tag.length+2.2, 5.5)` chars) so kinds align at a consistent width — poster-lettering feel (the Wes-Wilson north star). Fuller row/value justification is the CONTINUING direction, not done. VERIFY: idle runner fleet vanished mid-round (only the human's ★editor up), so no shot yet — the edits HMR into the human's live tab (how they iterate); type-clean, paint-only so fixture-safe. LIVE SHOT OWED when a runner frees.

2026-07-15 I-ROUND (the human at the desk, live steer over the H-round craters — READ THIS OVER THE H-ROUND; all Cytui-only, gen unchanged):
- **I1 cell hue = what the cell IS.** `cell_color` preferred `c.fold_kind` for EVERY fold, so an Artist folding Tracks painted Track-pink ("Artist says it's supposed to be Teal tho, and it's pink like Track"). Now a GANG rep wears fold_kind (its members ARE its identity) but a REAL container wears its OWN mainkey — the header speaks that level, the members' tint lives on the coagulate inside. Same split in the descmap `tint` (~3192) that drives the header badge tagtint (ckind = gang ? fold_kind : own). LIVE: VoroScape Artist cells `#298699` teal, Track coag `#bc2476` pink.
- **I2 inner shadow.** Second linearGradient over the coag (`gid+'s'`, black o0 0.32 → o1 0 over 34px below the seam) — the parent-facing edge reads as sunk-in, not laid-on. lgrad type gained `o0/o1` overrides (the depth grad stays 0→0.24).
- **I3 badge dedup.** keyed_rows tracks `ptag`: the kind badge shows on the FIRST row of a tag run, the rest bare ("we say Track,title:… then also year:… the laters should just be eg year:"). LIVE: `Track title:` badge, then `year:` `remaster` `live` bare.
- **I4 central unfoldment.** pane_rows inflate ceiling 1.35 → 2.0 + a CENTERING pass (re-layout dropped by half the spare height, kept only if every atom still seats) — text fills its space and sits centred, not jammed at the top ("things inline-text-looking up against the ceiling"). pane_rows now returns `avail` too.
- **I5 huddle.** crater header seats at the HIGHEST chord that holds the title at ~full width (tw*0.9), else the WIDEST chord (ywide fallback) — same-kind cells align their container level at one end WITHOUT starving a narrow top ("collaborate to huddle the Artist part up one end").
- **I6 the GROWTH SPELL** (the human: "put a spell on the cell to grow it, until the cyto changes enough that it gets more than enough space and we can remove the spell"). `vspell: Map<id,mult>` scales the seed's content box in voronoi_layout → the power walls yield room → no cytoscape touch (stream keeps flowing). `spell_update` (end of paint): STARVED (degraded | +N | tiny <11px load-bearing atom) → ×1.18/beat cap 2.4; ROOMY (seated, used<60% avail) → ×0.93 decay to lift; SNUG → hold. Dead band = no flutter; terminates on saturate/lift; chain cap 14; re-paint via rAF (visible) / queueMicrotask (hidden tab — NOT inline, the outer paint would clobber). LIVE: the 7px `name: Voxhall` blemish → 32px.
- **I8 ribbons hover-gated.** H5 hourglass ribbons moved out of `walls` into `sp.ribbons`; render only when `ribbon_pane===sp.id` (enter/leave on the coag path, vsub-hour pointer-events:none); louder now (0.16/0.28) since occasional ("not there until you mouse over the cell they're in … one more layer of stuff"). LIVE: Scape resting glass paths 52→14, 0 ribbons at rest.
- **I9 self-gate on the MIN identity atom** (not the first): the badge can seat fat while the wrapped name floors to 7px — gate on min ntitle fs, floor 9 (borderline keeps its crater, the spell grows it ≥11), below 9 → flat fallback. GATE (4 runs on 20e3476b): Clinic/Scape/flora all green, teal/pink split + badge-dedup + ribbons-0-at-rest + flora-0-coags hold EVERY run. **DON'T chase crater count or exact px through static shots — they're ENTROPY-VARIABLE**: Scape's cell composition itself shifted 4-teal↔5-teal run-to-run (the fuzz moves the graph), text landed 8–16px min across runs as the spell settled against a different layout, crater count wobbled 1–2. The flat fallback reads every row legibly (banded + deduped), so flat is a FINE outcome not a failure; crater-vs-flat is eyes-gated on the human's live tab. GOTCHA: a shallower header/body split (ytop+hh*0.5) THINS the header and shrinks the wrapped identity — keep ytop+hh (deeper header, body still ≥42%).

2026-07-14 H-ROUND (the human's correction of the G-round — READ THIS OVER THE G-ROUND WHERE THEY DISAGREE): the per-member ORBS were a MISREAD — "I didn't intend a crater per C (ie tuple), they were going to be an entire Stuffing+Stuffusion … just ONE in each cell … conserve Track,title:" → a crater = header strip (container level) + ONE smooth coagulate holding the DISTILLED member band (keyed_rows: lead list + tagged facts/spreads — compression conserved, never per-member repetition). The "black blurs" were NEVER static absence rows — they belong to the HOVER/CLICK correspondence: "mouseover|click handler all over the tuple, any one of the v we click on shows what other $v and $k are involved in it" → Vtuff_keyrows stamps c.members on every fact/spread chip (c-side, fixture-safe); hover a claim chip → carriers + one-hop sibling claims light (corr-lit), hover a member → claims it LACKS take the blur (corr-dim), click pins (member-click stays the pop). Plus: HOURGLASS ribbons (corr_ribbons — claim chip ↔ member chip, pinched waist, faint, capped 14, crater-only); TITLE WRAPS at its grammar seam ("you can wrap Artist\nname:Palegold" — the title is TWO atoms so the flow breaks between badge and name at full size); UNIFORMITY ("Artist and Track fairly uniform size … all w/C/* similar") = inflate zoom ceiling 2.4→1.35; the coagulate fill = linearGradient transparent on the PARENT side, tint deepening away (the human's depth ask). Also the human's meta-note: explain SOLIDLY how the machinery works — they felt under-informed; give mechanism, not adjectives.

2026-07-14 G-ROUND (the human's read of the live craters; the ORBS + static-blur parts SUPERSEDED by the H-round above): (G1) "a year:2007 look like its part of %Artist,name:Palegold, but those ARE strictly part of the Tracks … two different C/C levels cannot share data descriptions" — Vtuff_keyrows now stamps per-key carrier-kind tags (all carriers one kind ≠ container kind → tag, B0.1's rule generalised); paint bands flat panes by grasp level and a crater HEADER carries container-level rows ONLY (member facts live inside the craters). (G2) "Artist,name bits … must be the biggest!" — pane_rows lead cap (identity row's landed fs ×0.92 ceilings every later row) + craters cap members at header ×0.9. (G3) "each C coagulate should have quite a smooth shape … overlapping orbs" — bodyPoly → chaikin coagulate (STRONG boundary, member-kind tint); each power cell → orb (poly_grow +4.5 = overlap past the gap, chaikin ×2, clip_to coag); text flows in the ORB (rounder corners beat acute power corners); separator rule dropped. (G4/G5) absence BLURS: crater builds the fold's key UNIVERSE in global vein rank (same key same row across orbs AND cells — "C/n/* and C/m/* can think about alignment … across the boundary"); a lacking member renders the key word as a black smudge (vsub-blur, blur(1.6px)). Hover-highlight (the "or" branch) NOT built. WEDGE: the editor's compile dock for Ghost/Story/Voronation.g hangs on ANY real change (byte-identical no-ops fine, Voro.g fine interleaved — bisected); the VoroClinic seed enrichment (VoroScape_track sprinkle → year/live/remaster, needed to SEE G1/G4 on the Clinic) is authored with gen UNLANDED + Clinic re-record owed behind it; cure = the human reopens/reloads their editor tab.

**Why:** the "venn" (∀-shared facts said once, spreads showing variance with counts) was ALREADY the distiller's job (the typed-claims cut [[see-assertion-layer]] era); the tuples face is just the LAYOUT that renders it as readable notation. It also OBSOLETES the display-side namekey cut: you don't distill one name when you SHOW the whole tuple — kind:name + all keys read structurally, so the "fallible region|axis|name analytics" (parked) aren't needed for the glass.

**How to apply:** the `∿` button (was `cs`/`▧`) = the family washes + I/C/S/O arc-RIVERS, now PARKED OFF (the human: the squiggles "aren't earning their ink"; the cross-cell harmony they gestured at now lives in the tuples face's shared baselines). All render-only, byte-identical when off, eyes-gate only (NEVER headless — pixels don't round-trip). Verify via `runner_shot --svg` after courting the runner; ALWAYS `git checkout wormhole/Story/VoroMitosis/` after (a committed Book run clobbers its toc). See [[raf-throttle-blank-subcells]] for the blank-glass gotcha the build tripped.
