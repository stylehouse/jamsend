---
name: vyto-foam
description: "The foam voronoi-glass study → PRODUCTION port (vyto_foam.ts + Gang/Relate/Focus organs; VytoWeb/Fold/Bunch/Foam green×2); then the §9 SIZING ④+⑤ tenancy VytoBreathe green×2 (a relation lifts SIZE — opt-in priced commission; frame-share was VACUOUS for cells); Stuff_distil re-say; spout=isolated feature, weave PARKED; Vyto_fold landing stays the display refactor's"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2f2e32df-77de-4509-9ea4-3454f9ecd5a7
---

"foam" = the geometry half of Vyto (`spec/vyto_workingouts/processes.md`). Study → production, 2026-07-27, uncommitted (the human commits).

**The study (Artifact foam5, url 59dff932) as a shape STUDY is essentially done**: shape-blind nesting skin, screen-budget/fold, reading-vector nesting (each container carves a header band on −u, children tile +u ⇒ snap indentation, rotation-equivariant "draw at any angle"), dive/zoom/ancestor-crack (top-left key-aligned staircase), focus WARP (default-on, magOf scales power-radius through powerCells so tiling stays exact), per-cell SPOUT/teardrop, layout-provider seam (`setProvider`: force/sunflower/rings/rows — carve/fit blind to it). Proven claims: SHAPE-BLINDNESS (8 topologies incl. deep-spine + mixed-mainkey adversary) × LAYOUT-BLINDNESS (4 providers) × SCREEN-BUDGET (`budgetFor(glassArea)`→ phone 9 / tablet 15 / desktop 35 cells; `foldFor` greedily crushes the tightest frontier family, PREFERS crush-to-crest, buckets a wide leaf-family only as fallback) + no-spill (fitInside shrinks until every glyph corner ≥2.2px inside the polygon — a label can NEVER spill). Gate ×109+ `[pure]`, each check probed→red→reverted.

**Human RULINGS (durable):** the per-cell SPOUT is a tiny ISOLATED feature (a narrow nub on the single parent-facing edge, other vertices untouched) — NOT a whole-cell teardrop warp ("whack beziers"). The FAT-LIMB WEAVE (cytoscape-a-raft-per-limb for dramatic tile-unbound tails) is PARKED ("way too complicated"). A single convex tile caps a no-spill spout at ~14% of reach — subtle-and-clean chosen over dramatic-and-complex. REPRESENT C FAITHFULLY (never case-munge a mainkey; leaf type DERIVED by walking down; label = `Type×N`).

**PRODUCTION PORT (LANDED):** `src/lib/O/vyto_foam.ts` = pure sibling of `vyto_geometry.ts` (`sig_of`/`group_edges`, `bucket_key_of`, `FOCUS_BOOST 3.2`/`FOCUS_SHRINK 0.34`, `budget_for`+`fold_ladder`, `pull_step`). Vyto.g stubs FILLED: `Vyto_gang` (crowded>6 root → gang_key), `Vyto_relate` (`%Flow` rows on detached `w.c.relations`; joins + mainkey NEVER count as kinship), `Vyto_focus`+`e_Vyto_focus` (proposal door → `focus_tok`; no focus ⇒ mag 1 ⇒ old fixtures byte-still). **`Vyto_fold`'s wiring stays the DISPLAY refactor's** (the ladder is Book-ready but UNCALLED). Books green×2 on the live runner: **VytoWeb** (gang/relate/focus, 5 sworn), **VytoFold** (pure fold engine direct — the surface VytoWeb can't reach), **VytoBunch** (solver honours Relate edges as attraction — A/B differential: sever a genus ⇒ edge drops ⇒ joined<free), **VytoFoam** (the 7 tiny study Books MERGED into one 4-beat name-scoped world in Voronation.g).

**The contract is `%sworn` not `%see`** — a toc `%Assertion` row is credited ONLY against `this.story_swear(w, sentence)`, byte-identical; %see alone reds every assertion ABSENT with `sworn 0`.

**Stuff_distil re-say** (`Stuff_distil`/`Stuff_keyrows`/`Stuff_veinrows` in Voronation.g `//#region Stuff`): a faithful re-say of `Vtuff_keyrows` emitting the SAME `%Vtuffing/%Vrow/%Vbit` tree (KEEP-the-format; `Vtuff_build` stays the oracle). RULED: **the vein SUPERSEDES by default** ("say more with less"), knob `coexist` restores the per-key facts; vein is `wgt:1` like facts (loudness comes from shared-ness in the live stack, not a bump). Owed: re-home `Stuff_distil` into `Vyto_fold`'s tenant.

**THE FACE RAIL — LANDED 2026-07-27 (`src/lib/O/Vytui.svelte`) — the headline: Vyto renders Radio's REAL UI now.** A three-agent survey found the #1 blocker to "Radio builds its UI on Vyto like Cyto+Voro" was that Vytui drew only SVG cells + one ident label — NO faces (Radio's UI IS ten face components). Now each cell whose mirror row wears `sc.face:'X'` (worn) or whose mainkey ∈ `FACE_MAINKEYS` (imposed: Heist·Musu*→Crate) mounts the SAME glass component Cyto does (`glass_kinds.ts`), props `{ n: row.c.source_n, H }` — the LIVE source particle, not the scalar mirror row. Rendered as an HTML overlay molded to each cell in viewBox PERCENTAGES (no pixel measurement → tracks the responsive SVG, no overlay-sync bug class = the "better than Voro" win: real DOM faces). `<svelte:boundary>` per face so a throw degrades to a label not a white-screen. Faceless rows keep the ident label ⇒ Books byte-identical. Compile-proven. **REVIEW: `/BigSoundland?VY=1`** (Sounditron commissions Vyto on 9 organs). **HONEST GAP: NO Vyto screenshot path exists — runner_shot is Cyto-only (`cy.png()`), so the render is verified by compile + reuse-of-Cyto-faces + the human's eyes, NOT pixels.** Sizing/legibility in small cells + crew-tuck + kind chrome owed. (For Sounditron's per-organ grapple no crush/descent-suppression needed — each organ is one faced cell drawing its own internals.)

**LIVE CRUSH tenanted — `VytoCrush` (Vytonation.g): CODE-COMPLETE + compiled + gated OFF, bake PENDING (contention).** `Vyto_fold` no longer a stub: a `w.c.folded` opt-in (6th arg to `Vyto_commission_on`) glass crushes a crowded scope — `budget_for` sets the budget, `bucket_key_of` elects the key, each ≥2 group distils to ONE crest cell (`Vyto_distil`) with its counted dip; crushed members wear `.c.folded` + lose `.c.T` (Solve skips them). Voro's rosette matched. Touches only Fold+Solve — crests survive Scan's sweep via a `seen_at=scan_gen` stamp, Scan's CORE untouched. **GOTCHA that cost a cycle: `budget_for` must be ADDED to Vyto.g's vyto_foam import — LocalGen compiles the .g without resolving identifiers, so a missing import is a RUNTIME ReferenceError that CRASHES the run (state shows `run:null`, not a normal fail).** Ran to done:4 after the import fix; accept/verify blocked by contention. NEST-SOLVE `VytoNest` now GREEN×2 (baked once the runner freed). Fold/Solve/commission all gated so the fleet is byte-identical.

**SIZING ④+⑤ TENANTED — `VytoBreathe` (2026-07-27, Vytonation.g, GREEN×2 + full Vyto* regression green).** The `Vyto_sizing_todo §9` global type-scale wired into `Vyto_express`, OPT-IN via a PRICED commission (`w.c.priced`, 4th arg to `Vyto_commission_on`; default off ⇒ every existing Book byte-identical). **Key reframe the build FORCED (durable):** the §9-draft *frame-share* (`env_area=frame·imp/Σimp`, S=√(frame/Σw)) is **VACUOUS for CELLS** — power diagrams read only RELATIVE radii, so for `importance=1+dose` the frame-share = the absolute dose box geometrically (and normalising even DEGRADES the ordering, since r² are additive weights). "Push S until frame spent" is a TEXT idea (cells always tile). So the honest ④ = §3 graph-GLOBAL importance: `Vyto_importance` = `1+dose` + a **kinship lift** (sum of Relate `%Flow` edge weights on the row, §9③ "a relation lifts both endpoints") on the ABSOLUTE `2400·imp` scale. Shared meaning ⇒ a BIGGER cell everywhere — the thing raw dose can't say. Proven by a 2-D DIFFERENTIAL (needs BOTH pricing AND kinship): kin oaks outsize a lone pine at equal dose · unprice→equal · sever kinship→equal. Owed: shared-ness direct from mirror (not only via edges) · intra-cell rank · taper ⑥ untouched · preen the 0.5 coeff.

**NEST-SOLVE (J4) tenanted — `VytoNest` (2026-07-27, Vytonation.g): CODE-COMPLETE + compiled + gated OFF, but UNRECORDED** (runner monopolised by another client the whole record window — fixtures not baked, toc still dige:lie). `Vyto_solve` recurses into every scope when `w.c.nested` (opt-in, 5th arg to `Vyto_commission_on`; default off ⇒ flat/priced byte-identical): after the flat top cut, each cell becomes the frame for its children via `Vyto_solve_scope` (gap=0 → a scope FILLS its parent; stamps `parent.c.misfit`=|parent−Σchildren|), to any depth; Express grows `Vyto_express_rows` (tree-walk sizes nested rows). THE "cluster of UI bits" SPINE (a cluster = a scope whose children solve inside its cell). Additive+gated so safe-by-construction, BUT no fresh regression re-run with its gen yet. Finish (≈5 min free runner): fire-and-poll record cycle (below). Book asserts root-tiles + deep-tiles (misfit~0 at 2 depths) + all-seated + flat-control (unnested ⇒ children get no T).

**Shared-runner BLEED lesson (cost most of this session):** `--watch` HANGS/false-deads on a contended runner. Robust record cycle = **FIRE-AND-POLL**: `runner_ask run <Book>` (NO --watch) returns after dispatch → poll `runner_ask state` until `phase:done|failed` → `accept` (patient 150s+, NEVER kill — it re-runs ALL beats) → poll `ping` to `running:null` → fire-run again → poll `state` to `done`. Verify reds SOLO. the ★claude runner (`49dee91d61a9de64`) is SHARED and was heavily contended — regression runs got "refused (busy)"→"no reply"→a false "went RED" banner (the run never ENGAGED). ALL such reds were bleed, not real; every Book greened on a clean SOLO retry. Tells: intermittent "running:{book:...}" you didn't launch · reload itself timing out · "insisting 1/5..5/5". Also: killing an `accept` mid-flight (it re-runs ALL beats + waits — needs 150s+, don't kill at 60s) leaves server-side work that collides with the retry and wedges the runner (heal=reload). Verify reds SOLO; don't thrash a contended runner.

**RESIDENT MOUNT — the "no glass yet" bug, and the SEAM that fixes it (2026-07-27, LIVE-CONFIRM pending):** the human's `/BigSoundland?VY=1` never showed Vyto — badge stuck at "no glass yet". Vyto_commission is a **deferred elvis** (`i_elvisto` lands a tick later); a world OFF the pump never processes it ⇒ `e_Vyto_commission` never runs ⇒ no `UI:'Vyto'`. First fix tried (hand-stamp `c.up` on A:Vyto/w:Vyto) DID NOT work — c.up isn't the lever. **ROOT: the SEAM.** Sounditron_glass stood the glass on `this.c.up`; the green Vyto Books stand on **`this.up ?? this.top_House()`** and PUMP — and Vytonation §"the seams" is explicit: **`.c.up` is the un-pumped resident seam, `.up` is the property subHouse() sets that the pump follows.** FIX: stand A:Vyto on `this.up` (BESIDE the run = snap-blind, so recorded fixtures 002-007 never see it), latch `glass_done` only AFTER dispatch. ALSO this round (human directive): **?VY RETIRED — Sounditron commissions Vyto UNCONDITIONALLY** (no boot_param gate), **Cyto KILLED** (removed the Sounditron_glass Cyto branch + the 4 toc Opt flags useCyto/dontSnapCyto/useVoroCyto/useFaces). BigSoundland went **glass-agnostic**: mounts `ob({UI:'Vyto'})[0] ?? ob({UI:'Cyto'})[0]` (Vyto preferred), badge+trace key on `!Vyto-mounted`. toc.snap is LIVE-SAVED by the runner — a hand-edit to Opt can be clobbered by an auto-save unless the human reloads so the runner loads the new toc. [[nested-req-needs-cup-stamped]]

**RESIDENT CPU BURN — DEFER THE REACTION OUT OF THE EFFECT (2026-07-27, LIVE-CONFIRM pending):** once Vyto rendered resident, it PINNED A CORE — Vytui's render `$effect` re-fired at ATIME frequency and ran the heavy `adopt` synchronously each time. **The flush gate did NOT fix it** — I first tried driving off `void H.ave.vers` (reactivity_docs §"Enrolling and reading" sells this as one-re-run-per-settled-cycle, à la Cytui's `H.graph`), but the human's verdict: *"they aren't so well mapped as it might indicate in that docs"* — `H.ave.vers` AND `vyto_worlds()`'s own `H.ob`/`A.ob` reads still LEAK Atime-frequency bumps. Chasing a perfectly-gated signal is a losing game. **REAL FIX = throw the reaction OUT of the reactive context with a trailing-edge `setTimeout` latch:** the `$effect` body stays trivial (subscribe broadly, then `react_soon()` which arms a timer if not `react_pending`); the heavy `adopt(vyto_worlds())` runs in the `setTimeout` callback — so its reads take NO subscription (can't re-arm = no feedback) and a burst of N bumps folds into ONE adopt per window (`REACT_MS=120` ≈8Hz; the rAF spring loop runs 60fps BETWEEN windows so motion stays smooth). Hidden `?B=` runners stay SYNCHRONOUS (`document.hidden` → inline adopt; setTimeout is ~1s-throttled in a bg tab + Book determinism wants it inline + rAF frozen there anyway); teardown `clearTimeout`s the pending. Second, independent lever: **gate `paint_tick++` on actual geometry change** (`moved||removed||parked||hidden`) — an unconditional bump re-pulled `viewport_cells` + re-diffed all 9 real-DOM faces every heartbeat on a STANDING picture (no dose ⇒ constant radii ⇒ the cut is a FIXED POINT). Faces stay live regardless (each reads its own `source_n`, off paint_tick). LESSON: don't trust the Atime→UItime gate to throttle a live resident glass — DEFER the heavy reaction out of the effect (the `Vyto_stir_soon` idiom, render side); a Book world quiesces so it never bit here. [[otro-h-effect-no-read-H]]

**BigSoundland is now a MICRO-INSTRUMENT (my domain now — human handed it over):** header **glass badge** (`◇ VYTO` amber / `◈ CYTO` blue — the two voronoi glasses look alike, so which-glass was pure guess) + a **VY commission trace** in the diagnostic (`stood?` A:Vyto world exists · `landed?` w.c.commission set = elvis processed · error/rebuff rows) — turns "still Voro+Cyto" into a definitive readout. The badge/trace paid for itself first reload. Reuse on any page hosting Vyto mid-migration.

**Vyto CLIENT DOC surfaced — `spec/Vyto_client.md` (2026-07-27, unpreened):** the "how Radio builds its UI on Vyto like Cyto+Voro" front door the human said we'd "done before but something'd it up". Content existed buried in `vyto_workingouts/client.md` (Jul 20) but had DRIFTED: §1 addr `elvisto('V/Vyto')` WRONG (live=`i_elvisto('Vyto/Vyto')`), §9 caps stale (said no faces/nesting/gangs — all landed since). New doc = current+correct, face-rail-aware, grounded in live code; client.md got a supersession note (deep machinery there still holds). Readiness verdict for the human: ENGINE ready, resident DELIVERY was the gap (this fix) — no human had EVER seen a Vyto frame; still NO pixel proof path (runner_shot is Cyto `cy.png()` only).

**Reload gotcha:** a .ts MODULE re-fetches on reload but a changed .g METHOD body may NOT re-mix — put adversarial probe toggles in the module, not the method. A changed **gen .go** (e.g. after LocalGen) needs a HARD reload for a runner to re-load CREDULER_GHOSTS. To RETIRE a Book you must DELETE THE DIR (a Book dispatches by its `wormhole/Story/<Name>/` dir + toc, not just the method). Paint/faces WERE the human's reserved Vytui refactor — but the explicit "Radio builds its UI on this" directive + the BigSoundland handover SUPERSEDE that for the face-rail/mount work. See [[vyto-refactor-avoid-display]], [[localgen-browserless-compile]], [[g-authoring-gotchas]], [[new-book-cli-record-recipe]].
