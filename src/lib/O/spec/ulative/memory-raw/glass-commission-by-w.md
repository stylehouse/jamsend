---
name: glass-commission-by-w
description: "A WORLD commissions its own Cyto/Voro glass (no toc useCyto, no step-time coupling); CytoStep archive + e_Cyto_seek = the Yore time-travel seed"
metadata: 
  node_type: memory
  type: project
  originSessionId: 334456e9-f1e5-4e61-a0c4-7b0aaa37eec7
---

Built 2026-07-18 (uncommitted): `Sounditron_glass` — the world itself issues
 `Cyto_commission` (`{Scannable: RunHouse, Styles, client_w, useVoroCyto:1}`, NO wave/animation
  waits) so the glass forms continuously while Story snaps stay pure H. Cyto was ALREADY
   step-time-free once commissioned: it watch_c's the Scannable and rescans on any version bump;
    `useVoroCyto` on the commission arms the crusher Cyto-side (Voro.g). This is the demo idiom
     generalised — deletion-shaped, no new engine.

**Why:** the human: "Voro commissioned by w:Sounditron itself rather than bound to the Story's
 sense of step-time."

**How to apply:** the PROVEN rail is toc `useCyto+dontSnapCyto+useVoroCyto` (live glass, pure-H
 snaps — Cytui registration rides the Cyto world's own beat via Story_settingoff); the w-side
  commission is the fallback for tocless contexts and must STAND DOWN when the rail commissioned
   (two commissions fight over wave flags → snap-wait wedge). MODEL-AS-UI: the glass content =
    the world's particles — readable text goes in MAINKEY VALUES (comma-sanitized), facets stay
     quiet; MANY+homogeneous rows crush behind a husk by design. SEE it browserless:
      `runner_shot.mjs <out> --runner=…` (first bare arg = filename, NO 'shot' word) — canvas
       PNG shows bare nodes; the glass is the `--svg` overlay, greppable. TIME TRAVEL IS HALF-BUILT:
   Cyto archives `CytoStep{step_n, C}` per scan and `e_Cyto_seek{open_at}` re-waves any archived
    step (supports_seek retains the series; without it latest-only). The Yore cut = auto-number
     archives when step_n is undefined + bump moments on meaningful changes — design in
      [[Sounditron_todo]] §4. See [[sounditron-wild-book]].
