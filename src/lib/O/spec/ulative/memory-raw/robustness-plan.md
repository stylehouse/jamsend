---
name: robustness-plan
description: "hardening pass: one disease (assert-vs-derive, trust-vs-confirm) in 5 organs, born from the remoteWormhole grant saga. FOLDED into Cluster_spec.md (§homes); FINAL sublation 2026-07-07: last organ (Organ 3 authoritative-absence) → Cluster_spec.md §2.0a, Robustness_plan.md FULLY RETIRED to src/lib/O/spec/history/"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7d992ead-6ef2-4f82-85d7-85669ab9454f
---

**`src/lib/O/spec/Robustness_plan.md`** is the plan of record for the codebase-hardening work the owner greenlit 2026-07-04 ("we'll do whatever it takes"). Written from 4 parallel audits after the remoteWormhole grant saga ([[remotewormhole-grant-honest-durable]]) turned out to be a catalogue of the same few mistakes.

**CLOSEOUT DONE (2026-07-06, doc-reduction).** The fold happened: `Runner_network.md`, `Runner_quality_handover.md`, `Runner_talk_TODO.md`, `Cluster_runner_handover.md` were folded into **`Cluster_spec.md`** and DELETED; the 5 hardening organs distributed to their §homes (disease→§2 head principle; Organ 1 latch→§3.2b; Organ 2 ack→§1; Organ 4 identity→§3.2a DONE+verified; Organ 5 nav-contract→§3.8). **FINAL FOLD 2026-07-07:** Organ 3 (authoritative-absence at `land_good`) graduated to `Cluster_spec.md` **§2.0a**, so `Robustness_plan.md` is now FULLY ABSORBED and retired to `src/lib/O/spec/history/` — the WHOLE doc moved with a historicity notice atop, NO forwarding stub (missing `spec/*` → check `spec/history/*`, a convention now in CLAUDE.md's "Docs" section). Full specimen catalogues live in git history + the archived copy. `Cluster_spec.md` is the single home for trust · runner-flock · transport · identity. Step-0 verification that gated the fold: Organ-4 routing VERIFIED live; needsFSA "takes" VERIFIED; still owed = retry-fires (mid-run reload) + bin_write-over-proxy + P0/P1 forced-condition. See [[reconnect-epoch-seq-collision]], [[needsfsa-dispatch-gate]], [[musureplica-crush]].

**One disease:** state is ASSERTED instead of DERIVED, and boundaries are TRUSTED instead of CONFIRMED. Five organs, each with concrete `file:line` specimens + a cure:
1. **Latched flags never reconciled** — `w.c.channel_up`/`transport_up` (`LiesLies.svelte:319,346`, set-once-cleared-nowhere) ARE the "relay down" wedge; `Runner.sc.granted_wormhole` is the badge bug one hop over.
2. **Silent success over dropped work** — Peeroleum acks an unregistered type as handled (`Peeroleum.g:538`), a transport drop still returns a "sent" seq (`:393`), async handlers acked sync (`LiesLies.svelte:291-312`).
3. **No single source of truth + empty-overwrites-real** — the P0. `land_good` (`LiesStore.svelte:930`) conflates absent vs couldn't-fetch; the Waft-provision loop (`Lies.svelte:772-829`) lacks the toc's `notfound_once` guard → a transient not_found wipes Cluster/Keep/Library. Fix = tri-state read (present|absent|unavailable) + authoritative-absence at the one choke point.
4. **Identity model: 9 tiers → 2** (the deep fix) — root of the routing drop + grant for-flap + duplicate rows. ✅ SHIPPED 2026-07-05, canonical home `Cluster_spec.md §3.2a` (see the LANDED note below). grant `for` is now the FULL pub with the address derived at send (was a frozen 16-hex prepub).
5. **Lying errors & partial interfaces** — the "granted" badge, "no writable share" (really no `bin_write`), `RemoteWormholeNav` is a partial nav contract.

**Priority order:** P0 authoritative-absence at `land_good` (data-loss, the original wipe) → P1 Peeroleum-never-acks-undelivered + clear channel_up/transport_up latches → P2 identity collapse 9→2 + wormhole `bin_write`/nav-precedence.

**LANDED 2026-07-04** (all parse/compile-clean, `:9091`-UNVERIFIED — verify each on a live runner):
- ✅ **P0 land_good** (`LiesStore.svelte`): re-ask ONCE before trusting a `not_found`, deduped per-req (`req.c.nf_counted`) across the Phase-2 + read_good land sites; `notfound_rounds<2` keeps content `undefined` (re-dispatches). Docs (`text/Doc`) EXEMPT (new-blank-file + one-shot cold-subscribe would hang). `Auto.svelte` Library same re-confirm on its `rw` queue.
- ✅ **P1 latches** (`LiesLies.svelte`): `Lies_channel_up`/`_transport_up` clear the latch when `Socket_real` VANISHED (remix the auto-reconnect can't fix) → re-standup; a merely-disconnected ws does NOT flap it.
- ⚠️ **P1 ack-surface** (`Peeroleum.g` req_unemit → gen recompiled via LocalGen CHECK-then-write): unregistered frame type now WARNS loudly. DEFERRED the "mark faulty/don't-ack" — would retx-wedge conditionally-registered handlers (`test_*`/`repli_*`/`audiochunk`/`stream_offer`); needs live proof. drop-returns-seq already covered (channel_live gate + retx).
- ✅ **P2 nav-precedence** (`LiesFunk.svelte` install+reconcile, `Rundar.svelte` badge): PREFER a granted local share (`A.c.DL`) over the editor proxy — heartbeat no longer clobbers `WormholeNav(DL)`; new `local` badge state; unlocks `bin_write` via local FSA for free.
- ✅ **Organ 4 / H5 assertion** (`Lies_self`): warns when advertised prepub ≠ signing-key prepub (diagnostic only).

**LANDED 2026-07-05 — wormhole `bin_write` option-b** (P2, was deferred): a headless `&remoteWormhole=1` runner (NO local share) can now write binary over the wormhole. `RemoteWormholeNav.bin_write` + a buffer-carrying `send` + `Lies_send_binary_consumer` (runner→editor CONSUMER twin of `Lies_send_binary_to`, bytes off-snap on `frame.buffer`) + `Lies_wormhole_req_recv` reads meta header-or-top-level + an `op:'bin_write'` serve branch (`nav.bin_write` on the editor's disk, JSON `{ok}` reply). Reuses the proven symmetric binary path (editor's frozen pinned_stable spine already body_hash-verifies inbound binaries). NO .g change (all live O/ modules). `:9091`-UNVERIFIED — the live runner was mid-FAIL on MusuGenerateTestsMusic; needs runner tab + editor tab RELOADED (stale nav instance has no bin_write) then `runner_ask run MusuGenerateTestsMusic --watch`. This is what unblocks [[always-register-book-in-credence]]'s MusuGenerateTestsMusic (a %brand_new never-green Book).

**LANDED 2026-07-05 — the identity collapse 9→2, the WHOLE set** (Organ 4 parts 1-3, no longer staged; svelte-check clean, `:9091`-UNVERIFIED). Canonical writeup MIGRATED to **`Cluster_spec.md §3.2a`** (Robustness_plan Organ 4 is now a DONE-banner + the H1-H6 diagnostic archive). What shipped: (1) `Lies_self` is a PURE DERIVATION `prepubOf(Lies_cluster_idento.pub)` — the `Clustation_self ?? legacy` fork is deleted, so advertised prepub ≡ hello-bound prepub BY CONSTRUCTION (old H5 warn retired, can't diverge); boot auto-`Clustation_adopt`s a bare `.stashed` key into a first-class `%Identity` (`Auto.svelte`). (2) The H5 warn graduated to a FATAL invariant at `Lies_channel_up`'s hello — on divergence it screams + rings the Relay Brink + sets `w.c.identity_diverged` so `Lies_advertise` REFUSES to publish an unreachable address (NOT a hard throw). (3) `grant.for` = grantee's FULL pub: the advertise beacon now carries `pub` beside `from`, editor stores it on the `%Runner`/`%HostedIdentity` row + grants against it (FEATURE-DETECTED — old beacons still get a prepub grant), and routing derives `to = prepubOf(claim.for)` at SEND (kills the H1/`replyBIN` residue). SAFE because `prepubOf = pub.slice(0,16)` is a pure prefix so `prepubOf(prepub) === prepub` — idempotent across a mixed fleet. Files: `LiesLies.svelte` (Lies_self, channel_up, advertise/_recv, runner_roster), `LiesFunk.svelte` (grant_wormhole, grant_offer_recv, wormhole_req_recv), `Auto.svelte`, `Grant.ts` (mint_grant ALREADY took full pub — the bug was the CALLER). **Residue retired same day:** the corr-broadcast itself was falsified live (relay role addr = ONE socket; a 2nd runner tab ate every role-addressed reply/pong/offer) — all editor→runner frames now ADDRESSED, see [[roles-divide-addresses-deliver]]. **VERIFIED live 2026-07-05:** editor reloaded → `ping` gives `self===clustation_self` (routing intact) → MusuReco flipped `failed 0/11` (starved) → `done 11/11 100%` (2 fuzz-ok caveats), i.e. %Good now lands + the wormhole-read chain works addressed. STILL OWED: the bin_write path specifically (`runner_ask run MusuGenerateTestsMusic --watch` — may need a local-FSA runner, needsFSA).
