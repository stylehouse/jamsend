---
name: rung0-per-chunk-cid
description: Radio_spec §5A rung 0 COMPLETE — per-chunk cid=sha256 (2026-07-15) + [RUNG7-WIRE] origin-signature WIRED both seams (2026-07-17 green ×2); all 4 chunk-Books + MusuBreach(10) green; owed = a prod signer-setter + swarm routing
metadata: 
  node_type: memory
  type: project
  originSessionId: 0da3cb3b-d094-4c32-8e27-daa138a39d1f
---

**Radio_spec §5A rung 0 — per-chunk content-addressing — BUILT + LIVE-PROVEN (2026-07-15).**
Every audio chunk now carries a durable **`cid`** = full sha256 hex of its bytes:
- minted at all three chunk-mint sites — `Ra_record_from` (%Preview), `Ra_chunk_mint` (%Stream, incl. the tail-concat recompute), `Heist_census` (%Body);
- carried in the `.jam` header as a **`cids[]`** manifest parallel to `sizes[]` (filled sync in `Ra_pack` — noble `sha256_hex` is sync);
- **verified per-chunk in `Heist_land`** (both the `bin_append` stream path and the whole-buffer fallback) as a localized breach that names the corrupt seq, ahead of the existing whole-file `body_hash` gate (which still stands).

Files: `Ghost/M/Ra.g` (added an `IMPORT() import { sha256_hex } from "$lib/O/Hashly.ts"`), `Ghost/M/Heist.g`. Gen written via **LocalGen** (`GFILES="..." node_modules/.bin/vitest run -c scripts/Story_cli.vitest.config.mjs scripts/LocalGen.spec.ts`; `CHECK=1` = compile-only diff, no write) → `src/lib/gen/M/{Ra,Heist}.go`. Compiles clean through the real in-app translator.

**Live proof:** `runner_ask run MusuHeist` minted a `cid` on every `%Body` (39 in step-2's snap), zero step errors, phase machine ran to `deny`. It went RED **only** because the recorded fixtures lack the new `cid` rows (additive-snap diff, not a logic failure). Per-chunk verify is a **no-op on honest bytes** (census stamps `cid=sha256_hex(slice)`, identical bytes flow to land → hash always matches); it fires only on real corruption the wire/`body_hash` gates already exclude.

**RE-RECORD — DONE (2026-07-15 night shift):** `run → accept → re-run` = MusuHeist **22/22 GREEN** (caveat 20 = benign AudibleEntropy ≈). Fixtures `002.snap`–`022.snap` carry the `cid:` rows, step count stable, no collapse, no encode smells. GOTCHA re-learned the hard way: the fleet had FOUR runners advertising and an **un-addressed** `run`/`steps`/`state` round-robins across them — a `steps` query lands on a runner that never ran the Book (stale `done:6`, reads as a truncated false result). **Pin the WHOLE sequence to ONE runner: `--runner=<self-id>` on run AND steps AND state** (`runner_ask runners` lists them; `★claude` favourite is safe). See [[pere-books-total-1]].

**BREACH BOOK — DONE (2026-07-15 night shift): `MusuBreach` LIVE-GREEN ×2 (6/6, caveat 0).** Adversarial twin of MusuHeist in `Ghost/Story/Heistation.g` (sibling Book, dispatched by `w.sc.w`; registered in `wormhole/Credence` needsFSA:1,needMusic:1). Censuses two artists → lands one HONEST record clean (the control, proves the gate DISCRIMINATES) → flips one byte of a MIDDLE chunk LEAVING its cid → proves the gate FIRES: `poison,seq:2,breached,no_new_land,gone,retained,gate_named`. RIGOR UPGRADE: since a whole-file `body_hash` would ALSO catch this, `Heist_land` now records `job.sc.breach_seq` (the code finally does what its comment claimed — "names the seq") and the Book asserts the GATE's own report matched the poisoned seq — that localization is the per-chunk gate's value body_hash can't give. MusuHeist re-verified 22/22 green after the Heist.g change (breach_seq is inert on honest bytes).

**ORIGIN-SIGNATURE (rung 7) — PROVEN IN ISOLATION same night (2026-07-15).** MusuBreach grew a 7th step (`vouch`, green ×2): the cid catches corruption but NOT a lying peer who recomputes a cid over bad bytes, so the ORIGIN SIGNS the cids manifest (ed25519 over `id | cid0.cid1…` via the `Idento` primitive from `$lib/Y.svelte.ts`, the same one Swarm.g uses — DETERMINISTIC given seeded keys, so the vouch snaps stable: `vouch,by:434ced…,vouched,forgery_caught,imposter_caught`). Three probes: honest vouch verifies; FORGED manifest (swap a cid) fails; IMPOSTER (wrong key) rejected. Helpers `MusuBreach_sign/verify/manifest` in Heistation.g (IMPORT `{ Idento }`). **GOTCHA: the Idento sign method is `.sig(msg)` NOT `.signhex()`; verify is `.ver(sig,msg)`; `.freeze().pub` is the full pubkey; `.pretty_pubkey()` the 16-hex prepub.** MusuBreach now proves BOTH trust gates: cid=corruption, signature=forgery.

**RA-PATH cids — DONE (2026-07-15): `MusuRaStream`(40) + `MusuRaChase`(56) + `MusuBuddy`(14) re-recorded GREEN ×2 — the WHOLE rung-0 fixture debt (all 4 chunk-Books incl. MusuHeist) is CLEARED.** The %Preview/%Stream chunks carry cids in the fixtures (ride the snap round-trip = the resurrect proof). **BIG FINDING:** Ra-path cids ride the ENTROPY BAND (37-38/40 steps caveated ≈) because the transcode is NOT bit-reproducible — same source encoded twice → slightly different bytes → different cids. Heist-path cids (original file bytes) are EXACT; Ra-path cids are per-transcode. CONSEQUENCE for rung 7: two peers independently transcoding the same track get DIFFERENT cids → different manifests → different signatures, so a swarm can't dedup/verify across independent transcodes. The signature must vouch for the ORIGINAL MASTER's cids (deterministic, the `%Original` of §2.4 rung 3), not each grade's. MusuBuddy re-recorded green ×2 later the same night once the fleet stabilized — the earlier stalls were the RUNNER wedging under load (I ran ~15 Books), NOT MusuBuddy; a two-probe stability gate (run a 7-step Book twice, both green) before the heavy run is the fix. Fleet heals on tab reload (human's side); expect flaky routing under sustained load (runners drop out of the cluster toc mid-dispatch).

**`[RUNG7-WIRE]` DONE — the origin-signature is WIRED (2026-07-17, green ×2).** The helpers are promoted to `Ghost/M/Ra.g //#region trust` as `Ra_manifest`/`Ra_sign`/`Ra_verify` (the ONE impl; `MusuBreach_sign/verify/manifest` now DELEGATE — steps 2-6 diges byte-identical, the delegation was transparent). **Seam A — `.jam` at rest:** `Ra_vouch_header(info,bufs,signer)` stamps `by`(full pubkey)+`sig` over the cids manifest before `Ra_pack`; `Ra_stock_standing` REFUSES a header that claims a `by` but fails verify; unsigned headers pass untouched (old jams load). Signer threads off `lib.c.signer` (absent for every current Book → byte-identical, zero fixture churn). **Seam B — the offer door (keystone):** `Heist_offer_all(...,signer)`→`Heist_offer_vouch` stamps `by`/`vouch_sig`/`vouch_cids` on the RECORD HEAD (three scalar keys — they ride the chunkless husk; the `%Body` cids do NOT cross a husk, so the manifest MUST ride the head, dot-joined). `Heist_beat` verifies (`Heist_vouch_ok`) BEFORE `Ra_pull_beat`: a signed-but-failing offer is REFUSED (zero wants, zero lands, `job.sc.unvouched`+`unvouched,tune:` marker names it); unsigned passes. Gate ONLY at the Heist call — generic Repli path untouched (MusuHeist 22/22 stays GREEN, caveat 20 benign). **Proof: MusuBreach 7→10 steps** (steps 7-10 = the wire; two new `%see`), green ×2 on `49dee91d`. GOTCHAS re-learned: `Heist_beat` walks the WHOLE mirror (one honest beat lands ALL records) → give each wire scene its OWN single-record mirror (`MusuBreach_trim_to_one`); a husk skips binary-bearing children so cids can't ride the head as `%Body` — carry a dot-joined `vouch_cids` scalar instead. **Still owed (rung 7, NOT this wire):** an APP path that actually SETS a signer in prod (today only a Book would); swarm ROUTING (have-bitmap beacon, `Radio_multicast_todo.md`).

**NEW-BOOK BOOTSTRAP (learned this night):** a brand-new Book runs only 1 step until its `wormhole/Story/<Book>/toc.snap` carries `step=2..N,dige:lie*` lines (hand-add them, run → RED against the lies, `accept` records fresh, re-run GREEN). AND: a **wedged runner ACCEPTS `become_book` but never steps** (`run:null,outcome:null` forever, `steps`→book:null) — this looks identical to a toc bug but isn't; `runner_ask runners` then run a KNOWN Book to find a healthy one, ALWAYS `--runner=<self-id>`. 2 of 4 runners wedged this night; heals on tab reload (don't reload unattended). See [[pere-books-total-1]] [[frozen-boot-empty-first-run]].

**Two operational lessons (this session):**
- **Running a Book MUTATES its toc.snap on disk** (TimeSpool timings + the Credulation run-record, incl. `~g1` ghost-dige bumps that confirm the runner loaded my edited ghosts) even without `accept`. A plain `run` is NOT read-only. Revert benign run-churn with `git checkout -- wormhole/Story/<Book>/` if the Book's toc was clean at session start.
- **Vite HMR hot-loads fresh gen into a LIVE runner** with NO reload and even with the **editor down** — after LocalGen writes `gen/*.go`, the running tab picks it up (proven: cids appeared without any reload). So a live read-only-ish probe of a compiled change is possible editor-down; do NOT reload a runner unattended (it may fail to re-acquire and not come back).

Related: [[musurastream-real-streaming]] (the chunk-particle model), [[localgen-browserless-compile]], [[verify-via-live-runner]], [[force-clean-rerecord]], [[toc-collapse-orphaned-save]], [[cluster-trust]] (the signing layer rung 0's swarm-trust needs).
