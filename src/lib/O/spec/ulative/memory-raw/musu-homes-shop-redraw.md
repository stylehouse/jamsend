---
name: musu-homes-shop-redraw
description: "Radio_spec §2 Musu homes — LARGELY BUILT 2026-07-17: %Library DISSOLVED into MusuSelf|MusuThem/stock (15 Books), shop/ + Heist re-homed, soft %Heist→%Lead→condense (MusuSoft), bay+%Heistlet travelling ask (MusuBay), ogg128 export, cascade GC, RUNG7-WIRE — all green ×2"
metadata: 
  node_type: memory
  type: project
  originSessionId: 0da3cb3b-d094-4c32-8e27-daa138a39d1f
---

**Radio_spec §2 RE-DRAWN with the human (2026-07-17) — the Musu homes.** The shapes live in spec §2.2–§2.4;
 this is the pointer + what's NOT in the spec. `%Library` + the `Ray` dissolve into `%MusuSelf,pub:<me>` |
 `%MusuThem,pub:<them>` homes (correlate to Peering/Pier, shallow on purpose), each with three shelves:
 `radiostocking/` (ephemeral draw Mags — GC fodder) | `the/` (durable Mags — written about, never dropped) |
 `shop/` (the LOADING ZONE — mid-transfer only; the human: "the whole loading here and there zone") |
 `stock/` (the SETTLED shelf — ruled on license). Settled trio on stock/: `%Original/%Chunk` = the master,
 held where it is GIVEN as such, never travels; `%Record/%Preview|Stream` stands BESIDE (never under) an
 %Original — a pull lands a Record, derived, never a master; `%Blob,id,grade:ogg128` = whole-file export
 grades. `%Grasp` = the chosen handful of ANY remote Waft|Repli data (wishlist; also the abuse-report
 shape — "what of their share is naughty"); `%Heist` = a Grasp's actively-downloading leg — and it starts
 SOFT (a wish — a SEARCH locating a Pier who can fulfil it). `Mag|Grasp|Heist` are Waft-based (`What/`
 anywhere). The stock shelf is FLAT: bytes in ONE place, every meaning a Mag|Grasp referring in by id;
 GC roots = the Mags|Grasps.

**BUILT the same night (second 2h window, all green ×2 on runner 49dee91d):**
- **r3 container dissolve** — `%Library,pier:` GONE from code AND fixtures. Doors `Ra_home_self|them(w,pub)`
  → the home's `stock,pub` shelf (`Ra_home_shelf(w,home,pub,name)` shared; `Ra_library` = deprecated alias).
  ~35 sites converted by side-judgment (origin→Self, mir→Them); containers do NOT ride the Repli wire
  (`['Record','id']` locates — MusuVend proved it). 15 Books re-recorded green ×2. The shelf carries
  `sc.pub` (not pier) — `Ra_stock_one`/`Ra_card` read `.sc.pub` now.
- **shop/ + Heist re-home** — `Ra_home_shop(w,pub)`; `Heist_job(...,opts.home)` + `Heist_wish` home under
  the ASKER's shop; `Heist_flatten` removes from `job.c.up || w`. Zero Heists float on w (rung 1 [done]
  for the Heist).
- **soft %Heist** — Heist.g `//#region soft`: `Heist_wish` (`%Heist,wish:` = wish + no `at`) → `Heist_ask`
  (chunkless husk) → `Heist_match` (literal word-match vs Musica_cards → `%Lead,pier,id,tune`; no match =
  no Lead — silence is honest) → `Heist_condense` (at:+chose:+filing; FEEDS the untouched pull). Book
  **MusuSoft** 6/6 ×2: one wish→the right Lead; no-match→zero Leads; condense pulls ONLY the wanted card
  (decoys stay unspent). `Heist_words` = the seam the Stemdex/%Seem by-meaning rung replaces.
- **bay + %Heistlet travelling ask** (same day, Book **MusuBay** 9/9 ×2) — `Ra_home_bay(w,me,them)` mints
  `bay,pub:<them>` under MY shop; `Heist_let_mint/ask/answer/adopt` (Heist.g soft region) run the round
  trip: `%Heistlet,of:<hid>,pier:` + scalar `ask,id:` children → Repli_offer over (BOTH directions must be
  granted) → far side stamps `have:1`|`held:1`|NOTHING in place → annotated copy back → ADOPT onto my bay
  original (mirror = landing zone, never home). `%Heist` gained optional `opts.hid` ident (the `of:` join).
  Multi-source Lead fan-in proven (one wish → two piers → Leads from both). GOTCHA: frames settle BETWEEN
  beats — send and answer must be separate steps.

- **Mag shelves** (same day, 12 Books re-recorded ×2) — `Ra_home_radiostocking/the(w,pub)`; every world-side
  `%Mag` re-homed onto the OWNER's radiostocking/ (all sites = SELF; mirror copies untouched — `loc:['Mag']`
  is container-blind). MusuCursor/MusuHeal Dogear spines re-rooted at the shelf. ALL FOUR §2.2 shelves real;
  `the/` = door only until a written zine exists. Control MusuReplica green with no re-record.

**STILL OWED:** wet quarantine UNDER the %Heist (design fork posed to the human: bytes-under-Heist re-plumb
 vs thin referring wet particle — breached/took rows on the job may already BE the observable half); the
 `%Original` re-shape (census `%Record/%Body` → `%Original/%Chunk`) + `%Blob` re-home (BOTH eyes-on);
 Heistlet `cursor · backoff` leg fields + a true two-runner return leg; prod signer-setter + prod `hid`
 derivation; Jam session era-archive; Repli goner-delete consent gap (fix underway — surgical guard in
 Musica_recast_offer + delete-after-revoke Book; the core-seam choice stays the human's). Ruled on license: Grasp PRUNES / %Grab ENDURES; the zine reading of Mag (issues =
 curated Clouds; `%Spotlight,id` cover; bylines via rung-7 signature) is spec-prose only.

**Why:** the human kept asking "why would a Blob or Original just be sitting there?" — the settled answer
 (their delegation, my call, flagged for appraisal) is the referring-particle law applied to arrangements.

**How to apply:** any new music-culture particle homes under a Musu home, not flat on `w` (rung 1); a
 signature keys on the MASTER's cids (Ra-path transcode is not bit-reproducible — see [[rung0-per-chunk-cid]]).

**The night's three builds (all live-green ×2 on runner 49dee91d, all uncommitted→human banked some):**
- **ogg128 export** — `Ghost/M/Orig.g` (new ghost, enrolled in CREDULER_GHOSTS via LiesLies.svelte):
  `Orig_ogg_export` muxes a Record's Preview|Stream packets into a real RFC-7845 Ogg/Opus file (Androids
  play .ogg happier than .opus) + `Orig_ogg_parse` structural reader. Book **MusuOgg** (6/6 ×2): decode
  round-trip 78.47s vs 78s card (opus end-pad; granule = exact 78). GOTCHA: `bin_read` returns an
  ArrayBuffer not a Uint8Array. Never snap ogg bytes/hash/size (transcode non-reproducible); snap structure.
- **radiostock cascade GC** — `Ra_stock_cascade(nav,pub,gone,keep)` (Ra.g) + `Musica_forget(nav,mag,cutoff,pub)`
  now reaps the derived .jamsend_radiostock files of a forgotten era, BIAS-TO-KEEP (any surviving card
  spares the enid; stock is a re-derivable cache). Book **MusuReap** (4/4 ×2); MusuVend blast-clean 11/11.
  Still owed on forget: the WIRE goner (fold Musica_recast_offer's op:delete into the Berth path — M4).
- **[RUNG7-WIRE]** — see [[rung0-per-chunk-cid]] (both seams, MusuBreach 7→10 steps).

**FLAG for the human (unresolved):** the LIVE editor's Credence re-snaps flattened the caps flags off the
 disk toc mid-session — `MusuBreach` and `MusuReap` lines in `wormhole/Credence/toc.snap` now lack
 `needsFSA:1,needMusic:1` (MusuOgg kept its; MusuBuddy untouched). Without needsFSA the dispatch gate may
 route these disk-heavy Books to a proxy runner (refused/red on run-all). Fix from the EDITOR side (or a
 disk edit once the editor is quiet) — a disk edit while the editor lives just gets clobbered again.
