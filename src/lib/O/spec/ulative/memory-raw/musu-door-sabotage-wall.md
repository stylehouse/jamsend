---
name: musu-door-sabotage-wall
description: "MusuDoor = D1 part b, the anti-klepto sabotage wall (LIVE-GREEN x2 f76b3d7e); a wire-grafted %req is inert because reqdo pumps only w's IMMEDIATE reqs; canary must be DYNAMIC not immediate-child (false-green lesson)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2d6954a9-460d-42bd-afcf-4431dd33d952
---

**MusuDoor (built 2026-07-13, `Ghost/Story/Heistation.g`, fork of MusuVend) = D1 part b — the §12 anti-klepto SABOTAGE wall.** A malicious origin hand-crafts a magazine card carrying a grafted `%req:sabotage,eternal,arm:pwn` — a standing request dressed as catalog data. It rides the husk (`Repli_lines_of` ships every non-chunk child), merges deep under the follower mirror via plain `i()` (NOT `oai`, so no req-machine fires), and lands **INERT**. WHY inert: `reqdo`/`reqdo_sweep` (Housing.svelte.ts ~1017) pump only reqs that are **IMMEDIATE children of a `w:` world** (`w.o({req:1})` — immediate-only, `do()` reads `this.o({req:1})`), and merged content lands strictly BELOW the mirror lib → never enumerated → never pumped. The step-9 snap SHOWS the trojan sitting harmless in both origin mag and follower mirror. **LIVE-GREEN ×2** on runner 49de (9/9, caveat:0, 3 sees: wall + gate-refusal + containment), commit `f76b3d7e`. Deterministic (no FSA/audio/Berth/entropy → any runner). Registered Credence + Ality.

**THE FALSE-GREEN LESSON (an adversarial review caught it — [[adversarial-test-agent]]).** My first draft asserted "the grafted req is not among `w.o({req:1})`" (an immediate-child check). VACUOUS: (1) it watched the wrong shape — the attack is `req:'sabotage'` (mainkey `req`, VALUE 'sabotage'), NOT a `sabotage` KEY, so the control tested a decoy the real trojan never wears; (2) even fixed, `w.o({req:1})` is immediate-only, so a **deep-walking-sweep** regression (the exact one the §12.1 BOMB names) pumps the buried req WITHOUT making it immediate → the check stays green. FIX = a **DYNAMIC canary**: install a `req_sabotage` handler (`do_fn_for` resolves `H.req_<value>` for a bare req — Housing ~1222/1230; called `handler(req)` — Stuff ~674) that walks to the req's first `w:`-ancestor and flips `w.c.pwned` IF EVER PUMPED. See #1 asserts `!w.c.pwned`. Now BOTH a promoting merge AND a deep-walking sweep fire the handler → RED. Non-vacuity: a positive CONTROL pumps an identical `req:sabotage` through a THROWAWAY holder world's own `.do()` (avoids re-entrancy on the beat's own `w.do()`) and watches `holder.c.pwned` flip → notes `control_fired` (a gate on see #1). **Reusable pattern: to pin "foreign data cannot run", canary the DYNAMIC side-effect a pump WOULD cause (install a would-be handler), not a structural proxy; prove the canary CAN fire via a controlled pump.** `w.c.pwned` is on `.c` so it never snaps — a red regression shows only as the see-note VANISHING from got_snap → dige mismatch. So a live recording (real diges, not `dige:lie`) is what makes it gate at all.

**NEXT = D1 part a (OWED, the crypto door):** swap MusuDoor's Book-owned `w.c.grants` toggle for the live `Swarm_pier_live` verdict (MusuHeist's `repli_allow` shape) — reintroduces seal wall-clock → needs an EntropyProfile + a warming re-accept, so best done ATTENDED. The sabotage wall does NOT depend on WHO the peer is, so part b landed cleanly without the crypto. See [[musu-ra-magazine-m2]], [[magazine-cloud-shape]], [[see-assertion-layer]], [[verify-via-live-runner]], [[oai-req-mainkey-only]].
