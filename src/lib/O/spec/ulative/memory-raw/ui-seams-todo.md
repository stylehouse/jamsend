---
name: ui-seams-todo
description: seams where the data model ran ahead of the glass (Mag/Heist/provenance/roster); the 3-render-surface fact
metadata: 
  node_type: memory
  type: project
  originSessionId: 2f2e32df-77de-4509-9ea4-3454f9ecd5a7
---

`src/lib/O/spec/UI_seams_todo.md` — the well-defined list of "seams waiting on UI" (written 2026-07-27, verified against live code).

**The one non-obvious fact that reframes every "UI too busy" complaint:** `BigSoundland.svelte` picks ONE of THREE render surfaces — the **clean glass** (resident, faced organs only, `:230-235`), the **diagnostic fallback** (shown while the glass hasn't drawn, mounts `run_uis` incl. **Storui** with raw `%desc`/`%sworn`/`%see`, `:236-261`), and the **▦ sprawl** (`:211-229`). The clean glass ALREADY self-tucks `%desc`/`%sworn`/`%see`/`%log`/`%Machine`/`%req` under the `system` crew (allowlist inversion `Cyto.svelte:444-452`, default mute `{system:1}` `Cyto.svelte:393`) — they still SNAP (proof intact), they just don't draw.

**S1 CORRECTED 2026-07-27 (the doc's first draft was wrong twice — don't repeat it):** (a) The raw `%desc`/Storui is NOT everyday clutter — `run_uis`/Storui mounts ONLY in the glass-STALL `{:else}` branch (`BigSoundland.svelte:236-261`); once `cyto` draws it's gone. There is **NO `humdinger` boolean** to gate on — EVERY BigSoundland is role `'sound'` (`:46`); a real gate is an opt-in (`?diag`), not `!humdinger`. (b) The everyday leaks on the *drawn* glass are just two dressed faces carrying a non-`system` crew: the **Stoker** (literal `crew:'Radio'`, `Ghost/M/Radio.g:621` — fix = change to `'system'`) and the **posed Heist** (`Ghost/Story/Sounditron.g:411`, `{Heist:'…',posed:1}` — it has NO `sc.crew`; it draws because `cyto_crew` `Cyto.svelte:759` falls to face-kind `FACE_MAINKEYS[Heist]='Heist'` `glass_faces.ts:9` — fix = ADD `crew:'system'`). Both change the Sounditron snap → re-record on ★claude, NOT while the human uses the live glass.

The seams: **S1** the two crew edits above (Appendix A ready-to-paste, verified); **S2** the Mag/Cloud layer has NO face (`CrateFace` flattens `Ra_recs` into a `slice(0,18)` chip-pile — human wants per-Pier collapsible magazines) — a CLEAN display build, NOT blocked: the nest-depth question (preen §4) is RULED+BUILT 2026-07-26 — `Ra_recs`/`Ra_rec_find` now recurse via `Ra_recs_deep`/`Ra_rec_find_deep` (Ra.g:679/697), deeper rows FOUND not dropped, so a face may lean on the census at any depth; **S3** the Heist engine is built+green (`Ghost/M/Heist.g` `Heist_wish/job/beat/land`) but has **NO live caller** (exhaustively re-verified: only live `Heist_*` ref is `HeistFace.svelte:43` `Heist_condense`, the take button, gated `soft = sc.wish && !sc.at` `:32`, dead since nothing mints a live wish) — missing piece is a live `Heist_beat` driver (analogue of `Swarm_share_loop`); PROVISIONAL-until-M2 per [[heist-rulings]], needs a UX steer; **S4** now-playing provenance dropped (`radio.sc.by` via `Radio_open` `Radio.g:314-318` — line drifts, verify; `RadioFace` ignores it though `LineupFace.who()` `:26-32` already resolves it; wording is **"via" <Pier>** — the CARRIER this copy came through, NOT "from" which implies authorship, human ruling 2026-07-27); **S5** roster thin/split; **S6** legacy `#`-invite Dexie→`%Idzeug` migrator owed.

**Display edits are the human's Vyto-refactor zone** — [[vyto-refactor-avoid-display]]. Wire-side (`.g`, the S3 driver, S1 crew words) is mine on green-light; faces (S4/S2/S5) wait for Vyto glass integration. Appendix A (S1 crew edits) + Appendix B (S4 provenance) are verified ready-to-paste; Radio.g line numbers drift with its uncommitted edits — re-grep the anchor text before pasting. Related: [[radio-brain-glass-faces]], [[mag]], [[glass-allowlist-inversion]], [[heist-rulings]], [[sounditron]].
