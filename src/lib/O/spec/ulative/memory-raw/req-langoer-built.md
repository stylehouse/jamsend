---
name: req-langoer-built
description: "req:Langoer (the %Lango receiver/focus arbiter) BUILT beside the old path, live-runner green; the consumer that makes a Lango \"become something\""
metadata: 
  node_type: memory
  type: project
  originSessionId: f0479bcc-815c-423c-b7b3-65406dfb41f5
---

The consumer half of the %Lango channel — `req:Langoer`, `req:Keeping`'s receiver hat — BUILT 2026-06-30 (Backbone_plan P3 Move 3), proven productive on the LIVE runner. Answers the owner's critique "which Lango became anything to Lang" — now a Cursor Lango becomes an observable focus verdict.

Pieces (all .svelte, HMR'd into the live runner — no .g/ghost-compile):
- `H.lango` (LiesFunk.svelte) gains **`seq`**: per-`w` monotonic counter off-snap on `w.c.lango_seq`, value SNAPPED on each Lango. The cross-carrier "globally-newest wins" tiebreak (out-compete only orders WITHIN one carrier). Munged in fixtures via a new `{sc_has:{Lango:1}}`→mung-`seq` rule in `Story.svelte` `story_matching`.
- **`Lies_langoer_focus(w)`** (Lies.svelte, beside `Lies_focus_waft`) — PURE read, THREE-TIER policy: (1) equip never wins (background-never-steals); (2) a DELIBERATE (non-cold) Cursor outranks any `cold` resume/boot Cursor regardless of seq — THE boomerang rule; (3) newest seq within a kind. On boot only cold Cursors exist → newest cold wins → resume lands; mid-session a deliberate move outranks a later cold re-open → no boomerang. `H.lango` learned `cold` (snapped flag, set from the want kind by the future feed). Reads only snapped fields → survives reload. NB a naive "cold yields to any active foreground" guard would BREAK boot-resume (acquire sets active to arbitrary wafts[0] before the cold resume) — the deliberate-vs-cold distinction is why this lives in the arbiter, proven, before any live cut.
- **`req_Langoer(req)`** (Lies.svelte) — string-req → `req_Langoer` name-convention do_fn (`do_fn_for` Housing.svelte.ts:1147); records winner as snapped `req:Langoer,focus:<waft>`. **Does NOT write `.sc.active`** — stands beside the old want-resolver claim, observing only, zero live-focus risk. Seed with `{w}` so `upto_w` resolves (mirrors req:workon).

Gate: `LakeLango` (Machinery.svelte `e_Lies_lango_selftest`) mutated — foregroundable `LangoFore`/`LangoFore2`/`LangoResume`(cold) + 4 receiver markers `cursor_lango_drives_focus`/`equip_source_never_focuses`/`newest_fg_cursor_wins`/`deliberate_beats_newer_cold`. GREEN via `runner_ask.mjs accept` (on-disk 001.snap byte-matches live got_snap; step dige `a692157e3cff7709`). Snap makes the divergence legible: old `req:desire` locked `Waft:LangoFore` (arbitrary first-Waft) vs `req:Langoer,focus:LangoFore2` (deliberate winner) = the boomerang in miniature, Langoer right. Foregroundable Wafts woke real focus machinery (acquire finished, timemachine, `Interest:Trail` in A:Lang). NB live runner is INTERMITTENT (dropped out mid-session several times); accept→re-run can show a transient `failed`/`outcome:null` race — confirm via `steps`/`state` not just `--watch`.

FEED + SEED BUILT 2026-06-30 (Moves 2 + auto-seed), STILL OBSERVE-ONLY: (i) the cursor feed is live — `Lies_resolve_wants` (the want-land seam every cursor move funnels through) mints a Cursor %Lango on the landed Waft via `H.lango(w, landed, {kind:'Cursor', to, cold})`, cold riding from the want kind (`kind:'cold'`→cold Cursor); (ii) req:Langoer auto-seeded on every w:Lies (`w.oai({req:'Langoer',eternal:1},{w})`) + driven right after the resolver so its verdict is fresh same-tick. Langoer still writes ONLY req:Langoer,focus, NOT .sc.active. Type-clean. LakeLango STAYS green = NO-REGRESSION not positive-proof: its synthetic w:Lies has no Keep/wants so the feed no-ops + the auto-seed merges with the selftest's manual req:Langoer (find-or-create). Plus a new GLOBAL Keep-pref store `Lies_keep_pref_get/_set` (on the Keep ROOT sc, beside the per-Waft `Lies_keep_cfg_*`) — first consumer = Langui's `expanded` (the V/Vexpandy 80vh toggle), now projected from it like Waft.svelte's `minimised` (button writes Keep, $derived re-reads); persists editor expand across reload, snap-inert for Books (no Book toggles V). (iii) THE CUT — BUILT 2026-06-30, CONSERVATIVE, in-system GREEN, LIVE-acceptance PENDING. req_Langoer now DRIVES .sc.active from its verdict via the Lies_set_active_waft chokepoint (Move 4). GUARD (not a blunt sole-writer): re-assert `win` ONLY when the current focus is Lango-backed (the cold-resume boomerang — that resume minted a cold Cursor) OR nothing focused; a focus set eagerly with NO Cursor Lango (Liesui tab-click `Liesui.svelte:137`, +Now `e_Lies_now_Waft`) is RESPECTED — else a naive sole-writer's OPPOSITE regression (yank deliberate-but-Lango-less focus toward a stray cold Lango). In-system proof = LakeLango `cut_drives_active` marker: snap now shows `Waft:LangoFore2,active` + the desire/acquire lock FOLLOWING the verdict to LangoFore2 (was frozen on arbitrary first-Waft while verdict said otherwise = boomerang-in-miniature, now resolved); re-recorded green dige `38679b1bef1225ee`. KEY: snap_H KEEPS .sc.active (session key) — `Lies_waft_save` strips it but Story got_snap does NOT, so the cut DOES move snaps. LIVE-CONFIRMED 2026-06-30 (owner, :9091): focus moved into Musuation.g from a later Waft STAYS — no boomerang. THE FOCUS BOOMERANG IS FIXED. SEPARATE pre-existing issue STILL OPEN: the within-Waft CURSOR-POSITION resume ("cursor still don't resume" — lands on first/click, not last spot). NOT my arc (never touched `e_Lies_foreground_waft` or resolve_wants want-ordering); it's a want-RACE — the cold resume want (`Lies.svelte:229`, via Lies_keep_resume_what) must out-rank the deliberate land-on-first/navigation want, and currently the deliberate move wins. Distinct layer from focus(.sc.active). RESIDUAL (full sole-writer): make Liesui-tab/+Now emit a `click` want → mint deliberate Lango → verdict and live focus never diverge → guard drops to unconditional. OWED: feed shifts every REAL-editor w:Lies snap → re-record Lake*/Editron (the accepted "we only break snaps" cost); positive feed proof + re-record both need the runner free of the StoryTimes sweep (was cycling MusuCrate/Radiola.g, overriding become_book; w:MusuCrate has NO w:Lies so Musu* reds are unrelated WIP). Type-clean, uncommitted. See [[verify-via-live-runner]], the `accept` op in [[storyrun-run-record]]'s runner_ask family.

P7 DESIGN (drawn together in `spec/Lies_handover.md`, 2026-07-02): **LE = LiesEnd** (module renamed→LangHold in D7; design in `spec/LiesEnd_spec.md`) = the **accept-tour hold** — two %Seem (origin=remote %What, working=editable clone), vote via `U%unaccepted`, push replaces the %What back. `LE` stays a tag (the `D`=Demonstrations / `U`=Understandable / `LE` letter-tag family; renaming = ~40-snap re-record for no gain — the smell was it was *undefined*, not short). THE ONE MOVE: give every attention-Waft its own Interest (`Interest:Trail` giver, `Interest:Aside` scratch, **`Interest:Ting`** trail) ⇒ one LE scheme `{LE:<waft_key>}`; the global singletons (`%Spotlight` cursor, `{LE:Undertaking}` trail, `{LE:1}` singleton, `'checkout'` fallback) exist ONLY for want of an Interest and all dissolve. `Interest:Ting` (a background Interest, twin of Aside — holds an LE without stealing .sc.active) evicts the word **Undertaking** (the trail-LE that reads Waft:Ting globules for the minimap/DocTing glow); costs a LakeTiles re-record (7 snaps carry `LE:Undertaking`).

UPDATE 2026-07-01 — Backbone_plan **P4 COMPLETE, live-verified** (owner, :9091, "all good"). The CUT is now UNCONDITIONAL (the Lango-backed-only guard dropped): `req_Langoer` is the SOLE `.sc.active` writer via the `Lies_set_active_waft` chokepoint. RETIRED: the `req:desire`/`req:acquire` boot wrappers **and** `req:timemachine`'s per-tick `Lies_desire_land_cursor` playback (the drag-back engine) — timemachine is now play/step transport only. Boot focus = `Lies_keep_boot` (editor-only); live focus = `.sc.active`+Langoer; cold-start land = `LiesCurse`. Deleting the per-tick playback exposed a **LakeSurfer/LakeFunk-only regression**: `LiesCurse` emitted its cold want via `Lies_first_point_doc` (first Doc-with-*direct*-Point), which DISAGREES with the retired `Waft_cursor_first` (first leaf `%What`-with-direct-Points) when a Waft's Points hang off the `%What` (siblings of its Doc, not under it) → cursor landed a loose point-`%Doc` (`what:?`, raw `bm_` bookmark) instead of `%What:itis`. The old per-tick land hid it by re-asserting every tick. FIXED (LiesCurse.svelte): prefer `Waft_cursor_candidates(w)[0]` (the shared leaf-What collector `Waft_cursor_first` already uses) before the point-Doc fallback — identical target for the normal `%What→%Doc→%Point` shape (accepted Books don't shift), restores the What for the What-child-Point shape. Whole Lake fleet re-recorded green. D7 renames landed (LiesHold→**LangHold**, Keep methods extracted to **LiesKeep**). Corpus-spayer future-direction (conserve the shared cross-Book delta so ONE Accept covers a refactor, residual = the real signal) recorded in `spec/Story_next_level_spec.md §4.4`. Commit point reached; uncommitted (human commits). Remaining Backbone phase = P7 (dissolve `%Spotlight` into the Cursor Lango / Keep cursor).
