---
name: live-share-wired
description: "LIVE music sharing WIRED 2026-07-19: Swarm_share_up/beat/loop arms Repli on w:Swarm — stock husk-casts to Music-granted friends, per-friend %MusuThem crates mint in the RADIO world (repli_mirror_by_from + repli_mirror_w, opt-in), Radio_dial_pool joins them to the dial; fingers-proof owed"
metadata:
  type: project
  originSessionId: 334456e9-f1e5-4e61-a0c4-7b0aaa37eec7
---

Built 2026-07-19 (uncommitted), Books green post-change (Swarmation 1.0, MusuRaStream 40/40,
 Peregrination 1/1; Sounditron 7/7 steps run, RED on dige until the human re-records):

- **Swarm_share_up(w_swarm, ident)** (Swarm.g share region; InvitePanel arms it after station
  standup): Repli_arm + `repli_mirror_pier = my prepub` (the pull's from-address) +
  `repli_mirror_by_from = 1` (per-friend crates keyed by the caster) + `repli_mirror_w =
  top.c.radio_w` (crates mint in the RADIO world so glass/Riffle/Radio see them) + `census_w`
  (the boast counts the real shelf) + grant gate `Swarm_share_granted` (Swarm_pier_live at
  use) + presence gate `Swarm_share_present` (ra_source_live).
- **Swarm_share_beat** (600ms era-guarded post_do loop — the carrier's own mutex seam): enroll
  casters/rx per granted friend (idempotent), husk-offer the whole stock when
  `station_era:peer_era:stock_count` mark moves (presence-gated), drain Peering reqs
  (awaitbufs/parked), Ra_transcode_pump, Ra_restock_beat(budget 2) per friend crate.
- **top.c.radio_w** — Stoker_ensure stamps the world where the stoker shelves; the share and
  the faces resolve shelves through it.  BUG FIXED under everything: Radio_pub read
  `c.keys.prepub` (ALWAYS undefined — keys={pub,key}); now `sc.prepub`, so the live stock
  shelf keys by identity (%MusuSelf,pub:<prepub> — proven in the live snap).
- **Radio_dial_pool**: friend-crate records with map[0] landed join the dial (coin-flip vs my
  fresh pick).  RadioFace: "plays the pool — N of yours + M from friends".
- **Riffle rework**: %Riff cards wear face:'Riff' (RiffFace — own cells, the display system);
  my crate walks the share BY FOLDER on the nav contract (dir cards cap 12 + 6-track hands,
  paths on .c — commas); `Riffle_tune` stocks a bare path on demand (Riffle_base_split keeps
  the stoker's (base,rest) discipline so enids agree).  UI says "deal", never "blat".
- **Musica_pop / Radio_mag_pop**: append-only hand-curated %Card into the 'Faves' Berth mag
  (%Cloud,randomic:'chosen'); ★ on RadioFace + RiffFace.

**Second wave (same day):** FULL-LENGTH leg in Swarm_share_beat — a live keep-ahead riding the
 radio's REAL playhead (never Ra_term_stream_beat, the Books' simulated cursor): wants the next
  missing pages ~32s ahead past the preview, parking at the caster ignites its transcode.
   VOUCHER (spoof answered — relay routes on to only, from unchecked): per-era identity-signed
    {prepub, pub, era} attached in Swarm_deliver AND Swarm_hi_one, verified at the funnel
     against the SEALED pier's imported pub before heard_at, sealed-only + station-gated so
      Books/handshake untouched.  MEANDER FIX (Crate.g): local audio = ONE branch beside each
       subdir (uniform draw) — a root with tracks no longer starves the whole subtree below;
        noise dirs never draw.  %Zine cell + ZineFace (Faves Berth listed in the glass, ▶ by
         enid, ★ live-refreshes; Musica_pop/Musica_zine_* in Heist.g).  ONE BootGate (proactive
          on BigSoundland — kid-friendly "one tap to open the music", never names AC/FSA, picker
           only when disk_gated, keep_awake_acquire rides the tap).  System tuck: CREW_MAINKEYS
            += Dogear/Storyrun/Sent_Tree/Crush_Tree; a fresh tuner defaults {system:1} muted;
             TunerFace renders NOTHING when bare and says "show / hide" not "tuner".

**Book-proven (2026-07-19, delegated agent):** SwarmShare green ×3 — per-friend mirror keying
 (%MusuThem by caster prepub, never merged), %Suggest store-and-forward (offline stands un-got,
  hi drains the queue, got retires), era-change hi → reset_handshake keeping %Ud.  PereReborn
   green ×3 — the reused-seq collision re-ack + epoch reset teeth on the spine.

**Owed:** two-tab fingers-proof (seal → husks in friend crate → preview plays → pool dials →
 full track); voucher replay window (per-frame header-sign at the Peeroleum seam = full cure);
  FSA names-only expand for huge dirs.  See Radio_todo §0.

**How to apply:** live glue arms on w:Swarm but SHELVES live in top.c.radio_w's world; all
 Repli extensions are opt-in .c flags so Books stay byte-identical — extend that way, never
  by changing a default the fixtures see.
