---
name: sounditron
description: "Sounditron = /BigSoundland's resident real-environment diagnostic Book (event-paced beats, Opt/wild DEAD, checks like any Book); the mega-vague entropy regime (rows=proof, values=environment); idle-CPU diagnosis (two stacked drivers, both fix-surfaces off-limits)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 2f2e32df-77de-4509-9ea4-3454f9ecd5a7
---

`Ghost/Story/Sounditron.g` — the Editron-twin diagnostic Book that /BigSoundland boots by default. Probes the REAL boot: machine → relay → possibilities-of-peers census → peer → sound → report. Beats are **event-paced** (`expecting()`/ttlilt holds until meaningful state changes). Guts = `%Machine/%Relay/%Possibility/%Census/%Audio/%Session` referring particles.

**`Opt/wild` (record-not-check) is DEAD** — Sounditron fixture-checks like any Book (both assertions AND fixture tracking, the human "I wanted both"). Declared `%Assertion` gaps RED the run; undeclared `%sworn` latch opportunistically. Witness swears via `this.story_swear(w, sentence, subject?)` (idempotent). **NEVER add Sounditron to run-all** (unusual: real-environment). A foreign environment reds the fixtures — that's ITS tell; the assertion contract is the portable verdict.

**THE MUTEX LAW (a 4× hard tab deadlock taught it):** a beat fn must NEVER await an unbounded promise — it runs under the beliefs mutex (`await ac.resume()` on a gestureless tab pends forever; symptom: no beliefs(), ~10% CPU, Storui dead, no exception). Anything pendable runs DETACHED in an `expecting()` and stamps `w.c` for the witness. Grants are OBSERVED from storage (`%Pier/%Grant` under my `%Peering`), never re-set-up.

**The mega-vague entropy regime (SAVED green×2), one sentence: rows are the proof, values are the environment's** — the snap keeps every organ standing; what the environment fills them with is forgiven. Authored as local Entcases in `wormhole/Story/Sounditron/toc.snap` (hand-authoring round-trips fine): `means,dontSnap` folds a subtree keeping its line (the raw `stock` listing, `w:Voronoiology`, `Mag:Lineup` = random %Card rows every run); `means,drop` kills a row entirely (`Found`/`Possibility`/`Friend`/`ttlilt` — random/environment/settle drift; dropped rows still MINT, only the snap forgets them); wide spays `re:<Mainkey>(.*),tol:any` on organ rows. **Author `X(.*)` NEVER `X.(.*)`** — the dot demands a character so a BARE row fails its own spay. The loop: run live → read RED diff → fold/drop/spay → accept → green×2. Fixture-side rows can never be forgiven into existence — a new fold means a fresh accept. Wait ceilings: `stoker_wait` 30s, `peer_wait` 12s (patience for anything beyond ourselves).

**Idle CPU (20-30% on the tabs), two stacked drivers — BOTH fix-surfaces off-limits:** (A) `story_drive`'s `poll_step` re-arms at 50ms with NO visibility gate → ~20 belief passes/sec while `run.c.driving`; PERMANENT if an `expecting()`/ttlilt never settles (`Sounditron.g:54-58` Run-republished ttlilt outlives its dropped req). Fix = a `document.hidden` gate on `poll_step` — CORE-SEAM, needs the human's blessing. (B) the Voronoi glass re-tessellates continuously: each cell FACE ticks a 0.5-1s clock → ResizeObserver → `voronoi_soon()` fires UNCONDITIONALLY (`Cytui.svelte:739`); runs at true idle. Fix = gate on a node-size-delta — DISPLAY SIDE, the human's Vyto refactor zone. Disambiguate on `run.c.driving`. See [[ttlilt-rides-finishing-req]], [[vyto-refactor-avoid-display]], [[fight-back-on-core-changes]], [[entropy-samples-fuzzok]], [[ui-seams-todo]].
