---
name: sworn-assertioning-rulings
description: "2026-07-18 rulings + PRIMITIVE BUILT live-proven: story_swear(w,sentence,subject?) sync latch; contract = The/step=N/%Assertion (by_n dies); evidence → ave/%Assertioning shelf reset per run; runner_ask assertions op; NEVER say roster"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: c04d18c2-95b5-4c6f-9e9e-1966a05f5bee
---

The human's rulings on the [[see-assertion-layer]]/Seen_split redesign (full block:
 `spec/Seen_split_todo.md §0`, dated 2026-07-18):

- **`%sworn`** replaces `%seen` (greppability — `seen` collides with see/seed/chosen in prose+fixtures).
- **Contract:** `The/Steps/step=N/%Assertion:slug,sentence:…` — the hosting step IS the by-when;
  `by_n` field dies. **Evidence:** beat mints `i %sworn` on w, a harvest seam (story_harvest_desc
   sibling, pre-encode) moves it to `ave/%Assertioning,Story:<book>` beside This, stamping `n:`.
   Sworn lines leave fixture bytes forever after one last churn (SwarmStaple/Steal/Wire, MusuBerth).
- **Births entirely coded;** unmatched %sworn = "unclaimed evidence"; the human's Accept harvests it
  into the contract, after which absence complains. NO universal dumper/tunable matcher (that's a
   second EntropyArrest — EntropyArrest owns wobbling values, Assertions own presence).
- **NEVER the word "roster"** for The/Assertions — collides with the Cluster runner roster. Say
  contract / evidence.
- **Microsnaps:** at latch the sworn captures an encoded microsnap of its SUBJECT (what the
  assertion points at), under the House mutex (async swear legs must acquire it); the swear idiom
   grows an optional subject param — bare `i %sworn` stays legal, microsnap-less.

**BUILT 2026-07-18 (the primitive + the CLI op), live-proven green ×2 + sabotage red ×1:**
 `story_swear(w, sentence, subject?)` in Story.svelte (SYNC — witnesses call it without await;
  microsnap = sync bounded enLine walk, depth-cap 3, stored on `.c`, never encoded);
   `story_assertioning(_reset)` shelf, reset in Story_settingoff; `story_harvest_sworn` beside
    story_harvest_desc; `Cred_assertion_gaps` reads per-step contract + shelf with a CHURN BRIDGE
     (flat bucket + `seen:` snap text still count until SwarmStaple/Steal/Wire + MusuBerth convert
      — then DELETE the bridge); gap shape now `{slug,sentence,n}`; toc %step skip-rule keeps
       Assertion-only steps; `runner_ask assertions` op live (✓/✗ contract, ◇ achievements,
        ⌖ microsnaps, exit 1 on gaps). Sounditron converted. A subject not yet minted at go-off →
         bare swear, honest no-microsnap.

**CHURN DONE + BRIDGE DELETED (2026-07-19):** whole fleet converted + live-Accepted
 (SwarmSteal 2 / SwarmStaple 10 / SwarmWire 5 contract per-step; SwarmDoor + MusuBerth
  achievements-only); fixtures carry ZERO assertion bytes; the flat-bucket/`seen:`-text bridge is
   GONE from Cred_assertion_gaps, Cred_report_wild, the assertions op, the CLI. Phantom-gap probe
    proven on BOTH regimes (wild + dige). `%seen` no longer exists anywhere — grep `sworn`.
     Churn swears are subject-less; retrofit microsnap subjects freely (never a fixture byte).

**STORUI LAYER BUILT (2026-07-19) + the human's corrections:** hover-desc layer (hovered pip's
 %desc alone, absolute, instant); diamond assertion marks in sr-flags (pending hollow→latched
  green→overdue amber→red pulse at run end — leniency purely structural); `sworn N/M` button
   ON THE RUN BAR between the step counter and the verdict (the human's placement) / `[s]`
    (works with no panel — standalone explorer) swaps diff↔explorer (mutex); missing assertion
     = red dashed GHOST LINE in show-diff at the end of the `w:<book>` subtree, click-through.
      Explorer sections: `declared %Assertions` and `undeclared %Assertions` — undeclared is
       NOT ok (amber ◇ "wants declaring"; never "achievements"/"unclaimed"); latch labels read
        "sworn at NNN" (never bare `n=`).  All from The + the ave shelf via `H.ave.ob` — no new
         state, no fixture bytes.  Build order DONE.  **The declare door BUILT:** `declare ↑`
          button per undeclared row (+ CLI `runner_ask declare '<sentence>'`) → `e_story_declare`
           mints `The/step=N/%Assertion` (N = the shelf n:, slug = head-clause kebab) + story_save;
            absence complains from the next run; undo = delete the toc line; code NEVER
             self-promotes.

**Why:** assertions were flat latches amongst the world guts, joined to their declaration by
 byte-identical prose; the human wants them off to the side, grouped, time-domained, and pointable.

**How to apply:** UI = hover layer shows ONLY the hovered pip's %desc; assertion marks in sr-flags
 (loudest broken); explorer mutexes show-diff; absent assertion = red GHOST LINE in show-diff where
  the evidence would stand (click-through). Leniency: pending→overdue(amber)→red-at-end, from
   structure. **Accept doctrine revised:** live test+Accept authorized for these re-records — the
    gap-gate now prevents Accept eating a declared proof (headless ACCEPT=1 stays banned). See
     [[see-assertion-layer]], [[sounditron-wild-book]], [[desc-on-steps]].
