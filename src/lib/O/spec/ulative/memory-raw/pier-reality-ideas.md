---
name: pier-reality-ideas
description: "the §9 \"Pier reality\" idea set in spec/Radio_todo.md — ten seams for taking Repli from the demo loopback to real Piers, plus their What map in the Ality Waft"
metadata: 
  node_type: memory
  type: project
  originSessionId: a060c31b-3f6c-4aa7-b1a5-fa5fe9c87f36
---

**Set up 2026-07-03** (owner: "set up a bunch of Pier reality ideas"). Canonical home:
 `src/lib/O/spec/Radio_todo.md` **§9** (ten ideas, each grounded in an EXISTING seam so a session can
  pick one up and go) + a navigable `What:Pier reality` section in `wormhole/Ghost/Music/Ality/toc.snap`
   Pointing at each seam.

The ten, telegraphically: **9.1** real library off /music via the FSA share gate (MusuCrate's decode
 seam; a Se over the folder tree — its neus|goners are files); **9.2** `%Share,label:` Selections
  sendable to Piers (the ancient AI-note at the top of `src/lib/mostly/Selection.svelte.ts` finally
   executable — unshare = goner in the Share, not the library); **9.3** the pull rides the playhead
    (req_streamability's want_left shape → replication rate = listening rate); **9.4** catalog gossip
     over `to:@channel` multicast (late Pier gets baseline + live neus); **9.5** a %Sent_Tree PER PEER
      = the availability map Mesh_route routes wants by (multi-source pulls); **9.6** wear makes the
       mirror a cache (got regresses on reap — Se continuity shows it as history); **9.7** the Keep
        gates what enters (grants before wants — tracks [[cluster-first-class-object]]'s
         Backbone_plan, don't fork); **9.8** the tree is the resume (want from:have on reconnect —
          the [[inseq-reload-baseline]] shape); **9.9** retire as a first-class social act (op:delete
           per-Share vs per-library); **9.10** the audio-proof cherry — B PLAYS its replica (the demo
            that IS the app).

Suggested order: 9.1 → 9.10's spine (one real file end-to-end) → 9.2 → 9.4 → then 9.5-9.8 as the
 swarm grows. See [[repli-protocol]] for the machinery all of this stands on.

**HEIST DESIGN DECIDED 2026-07-11 (owner Q&A) — canonical capture Radio_todo.md §0.** Payload =
 ORIGINAL file bytes (transcoded-send with IDv3+albumart deferred); job = `%Heist,at:<pier>`+match
  (klepto = match-all; %TreasureMap speculative micro-blog sibling); Pier|TreasureMap|Heist live
   briefly then FLATTEN OFF; landing = STRAIGHT into the collection (merge decided at heist-creation
    via the old believe/disbelieve %place layers — prior art `src/lib/ghost/Pirating.svelte` steps
     2+4); probation = `.jamsend/newlyadded` METADATA log (never bytes; feelings; week-two listening
      diet → koha list or drop); provenance NOT persisted (dedup by catalog identity + upgrade-to-flac
       policy). Home = NEW `Ghost/M/Heist.g` riding Repli. First Book = rung 1: two Piers share the
        existing `testsounds/` FSA dir (8 flat WAVs), test mode DIVIDES ARTISTS between them (whittled
         census so each seems to hold different music), each files under DIFFERENT genre categories,
          per-Pier `.jamsend/test-marrauding-of-<runid>` override for clean deletion (one rm -r each).

**§10 added 2026-07-07 (owner ask): KLEPTO MODE** — the heist points at the PIER, not a Record:
 mirror-everything at heist rate (not playhead rate). Three moves: (1) many kleptos, ONE disk read —
  host broadcasts one library sweep on a heist @channel via `Peeroleum_offer_stream`, latecomers ride
   the bow wave (MusuReco's shape) + backfill 1:1; (2) the cafe tree — same-public-IP detection AT THE
    RELAY stamps LAN groups → cheap edges → `Mesh_broadcast_stretch`/`Mesh_cafe_spec` routes one WAN
     copy; (3) consent intact — heist takes what's OFFERED (grants, 9.7). Rungs: v1 loopback Book →
      cohort (FORCES the 2-runner real-Pier milestone — mock is by-reference, flatters shared-delivery
       claims) → cafe. §10.1 = honest wire ledger: real protocol, tame wire (`porta.partner = portb`).
