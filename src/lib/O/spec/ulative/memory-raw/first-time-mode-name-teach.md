---
name: first-time-mode-name-teach
description: "First-time-mode BUILT: Clustation_friendly = persisted chosen name (prepub + role Thang tags, DoorFace ✎ editor); every face teaches its empty state; remaining user-story seam = live Repli pull filling %MusuThem after a seal"
metadata:
  type: project
  originSessionId: 334456e9-f1e5-4e61-a0c4-7b0aaa37eec7
---

Built + COMMITTED 2026-07-19 dawn session (03b6eb96): **first-time-mode**.

- `Clustation_friendly(name)` (Auto.svelte) — the chosen name, stamped on the active %Identity
  AND its %Peering page (what [[invite-claim-pin-autojoin]] invites carry), persisted into the
  identities Thang under BOTH the prepub tag and the page's role tag (a role-resumed boot must
  find it too).  Rehydrates in Clustation_concrete; Clustation_pin carries it.  Set from the
  DoorFace ✎ editor.  `nick` stays the derived cosmetic fallback ([[cluster-identity-nick]]).
- TEACH empty-states, all face-side (.c/sc reads only — no snap furniture): Door (newborn →
  "the QR is how a friend joins"; unnamed → name nudge), Radio ("▶ plays your music — N records
  stand ready" off the stoker census, honest), Stoker, Riffle (only-crate → invite pointer;
  empty friend crate → QUOTES their %IveGot boast), InvitePanel (born-today + no friends →
  the whole trick in one line).

**The one remaining user-story seam:** nothing LIVE starts the Repli pull that fills
 %MusuThem after a seal — proven on the Book wire (Radiation), never wired to the live
  station.  Next rung: seal|grant → Repli session over the relay → %MusuThem/stock fills →
   Riffle friend crate live → Radio_tune their track.

**How to apply:** first-visit detection = born-today + empty-shelf checks, never a stored
 flag.  Teach lines live in faces, not the machine.  The dawn session's 8 commits
  (9636136f..03b6eb96) were human-authorized — the never-commit rule otherwise stands.
