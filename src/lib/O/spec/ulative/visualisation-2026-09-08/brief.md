# Brief for the visualisation agent — jamsend, 2026-09-08

You are the third of three long-running agents: metaphysics|code (running on `ulative/fallen-out-of-mind-2026-09-07/metaphysics_brief.md`), social-infra-and-app (the interactive session), and YOU — visualisation. Repo `/app`. Docs `src/lib/O/spec/`. Read `/app/CLAUDE.md` first (the C-object model, mainkeys, snaps, the one bet), then `spec/Homethink_todo.md` for posture.

## The owner's steer, verbatim (2026-09-08)

- *"I don't like any of those laws, really... the testing ethic is alright I guess... but everything about Vyto was quite muddled. we almost got to a usable C\*\* expression, but I couldn't rely on it to be absolutely legible, and not randomly leave something off... it needs to be as truthy as snap, which is maybe what that language is failing to say. it's like an advertisement for a bit of reality, and standards bind us to stating everything that matters there to quite some degree, unless the context is a squish zone that can tolerate lying for artistic effect."*
- *"I think it's a cytoscape collapsing into voro cells I want to explore again|more. the same thing. it was just hard to get it to fold at the meaning, in the right way."*
- Earlier rulings still standing: *"ideally this whole program looks like a child's pasta and paint artwork — we can see what the player is plugged into in the Mag, tiny ants moving buffer into the Record there"* (`Vyto_todo.md §0.0`); cells are the scarce resource — new understanding arrives as RELATIONS drawn and as MOTION, never as new boxes of text.

The session's restatement the owner accepted: **the render is a snap** — same truth standard, different medium. A view has two regimes: the CLAIM (outside a squish zone it shows everything the snap of that subtree would show; omission is a failure with a name) and the SQUISH ZONE (declared, not implied; may lie about size and emphasis, never about presence). Presence is the invariant; everything else is squish. The five LAWs at `Vyto_todo.md:2333-2351` are process rules the owner does not want; the testing ethic (pixels witnessed, adversarial proof) is "alright".

## What is known (from a code census this morning — verify anything you build on)

Built and Book-gated (20 Vyto Books, fixtures complete): cells as region/boundary (`vyto_geometry.ts power_cells`), focus-as-geometry (`vyto_foam.ts focus_mag`, VytoWeb), importance lifting size by relation (`Vyto_importance`, VytoBreathe), morph-never-blink (`Vyto_scan_walk/sweep`, VytoStaple, VytoMitosis), fold/crush/crest (`Vyto_fold_scope`, `Vyto_distil`, VytoCrush/Crest/Fold), nesting (`Vyto_solve_scope`, VytoNest/Depth), faces as DOM, settle+holds (`Vyto_calm_held`). Half-built: only the area Express channel; Relate draws vines but never factors a shared crest; spool has the o-mark, nothing mints a blessing. Wishes: pelt, Slope, Mesh, recipe commission, Situations. `fold_ladder` (path-to-focus never folded) is proven pure and UNWIRED in the glass. The moult never finished: `Cyto.svelte`/`Cytui.svelte` still live beside Vyto. The render is snap-blind by construction (`Vytonation.g:27-33`): the fleet stayed green while the tessellation was destroyed at 82% coverage (`Vyto_todo.md:2369`, `scripts/vyto_see.mjs`).

## Your question

**What does it mean to fold at the meaning, and why did it not?** "A cytoscape collapsing into voro cells" — a graph of particles whose subtrees collapse into cells, where the collapse boundary is drawn by MEANING (mainkey families, holdings vs referrers, `of:` relations, what the group can SEE/PROVE/REWRITE) rather than by crowding, dose, or a budget.

Work it from three sides:

1. **What the fold keys on today.** Read `Ghost/V/Voro.g` (Fold, Gang, the crush, the nucleus per parent, "region = widest-SHARED key, axis = widest-SPREAD key"), `Ghost/V/Vyto.g` (`Vyto_fold`, `Vyto_fold_scope`, `Vyto_distil`, `Vyto_importance`, the `w.c.folded` opt-in), `src/lib/O/vyto_foam.ts` (`fold_ladder`, `budget_for`), and Cyto's own collapse (`src/lib/O/Cyto.svelte` `cytyle_classify` skip|invisible|compound, `Matstyle.svelte`). State, per mechanism, the QUESTION it answers when it decides a subtree becomes one cell. Most will be "is it crowded" or "is it far from focus" — say which are about meaning at all.
2. **What "meaning" is, in this codebase's own words.** The C-object law (one holding, many referrers; `of:` for many:1; mainkey = what a thing IS); the `%see`/`%sworn` sentences (the system's own account of itself, ~570 of them — sample `grep -rn "story_swear\|%see:" Ghost/`); Homethink's SEE/PROVE/REWRITE test; `spec/Cello_synthesis_todo.md` (ink ∝ surprise, §R.8 the context-keyed prior), `spec/InkSurprise_todo.md`, `spec/Seemables_todo.md`, `spec/Cstructures_todo.md`, `spec/Statemap_todo.md`. Also the snap encoder itself (`src/lib/O/Text.svelte` `enWaft`, Travel + mainkey; the common-prefix factoring the spec's "bunching" wanted to transfigure) — the snap already decides what is one line and what nests; a fold-at-the-meaning may simply be the snap's own grouping made spatial.
3. **A proposal, in particles first, pictures second.** What is a "meaning cell"? Candidates to test against each other: a mainkey family under one parent; a holding plus its referrers; a Seem's `D//U` pair; a req pile and its owner; a sworn sentence and the particles it is about. For each: does it stay legible (the claim regime — nothing of the snap lost), where is its squish zone, and what happens to the path-to-focus. Then say how the existing organs would be re-keyed, and which Books (existing VytoFold/Crush/Crest, or new) would gate it. Write the `%sworn` sentences for the gate BEFORE any code — that is the one piece of the old laws worth keeping.

## Rules

- No code changes. No commits. Write to `src/lib/O/spec/ulative/visualisation-2026-09-08/` only: `findings.md` (your report — short sections, cite `file:line`, the owner's words quoted, honest about law vs ruled-but-unbuilt vs wish), and any scratch you need beside it.
- Verify with `runner_ask` only if a runner answers `node scripts/runner_ask.mjs ping`; never run `Story_cli_run.mjs`. Do not reload a runner. Read-only is enough for this job.
- Short, exact, in the corpus's register. The owner reads sentences as poetry as much as proof. "Fold at the meaning" needs a definition the code could be tested against, not a mood.
