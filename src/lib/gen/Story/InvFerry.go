<script lang="ts">
    // GENERATED by Lang compile — do not edit by hand.
    import { TheC } from "$lib/data/Stuff.svelte"
    import { onMount } from "svelte"

    let { H } = $props()

    onMount(async () => {
    await H.eatfunc({

    Ghostmeta_Ghost_Story_InvFerry(): string { return '05187e96640aa421~g1' },

// InvFerry.g — the SECOND Inv* Book: the WHOLE ferry exchange end to end (Inv_ferry_todo.md §3).
//  InvSeal proves ONE rung (the seal-seam warmth gate — cold refuses / warm parks the consent).
//   InvFerry proves the exchange as a state machine: an invite is MINTED (a serial off the %Idzeug
//    issuer) → CARRIED (the ?Iz token pulled back out of the URL) → VERIFIED at the door (the presig
//     regenerates — only the issuer can wear the MAC) → CLAIMED (the serial ticks the spend ledger)
//      → both sides hold reciprocal %Grants → the account CROSSES the sealed MyCave pier (#fc code
//       off the URL fragment) → and a re-scan does NOT un-spend.  Plus the failure edges worth a
//        beat: a DOUBLE-SPEND refuses; a FORGED presig refuses locally at the door.
//
//  MODELLED TIGHTLY on SwarmSpread beat 5 (Swarm_ferry_send/Swarm_ferry_heard + the MyCave pier
//   recipe) and SwarmFerry (the seal→unseal→import CROSSING claims) — the two PASSING ferry Books.
//    Same skeleton: fixed keys seeded off the name (ed25519 signs byte-repeatably), a pinned clock
//     stepped per beat, note rows of BOOLEANS ONLY (the crypto-random secret + sealed blob ride
//      `.c`/stashed, never sc), witness via this.story_swear (Assertion: lines under the toc steps).
//
//  BOOK-INERTNESS: this Book crosses the account over the SEND path (no humdinger raised), exactly
//   as SwarmSpread beat 5 does — Book piers carry no heard_at, so the humdinger-gated PARK branch is
//    never reached here (InvSeal owns the park proof).  So no consent frame / no heard_at leaks into
//     the fixture; the crossing is deterministic.
//
//  ⚠ DRAFT 2026-08-30 — NOT yet compiled or run; authored WITHOUT editor/runner access.  Before real
//   (Inv_ferry_todo §0): register 'Ghost/Story/InvFerry.g' in CREDULER_GHOSTS (~:56); LocalGen compile
//    (GFILES must name it); author the Plan (6 steps) in the editor (a missing Plan → hollow 1-step
//     green); verify on the LIVE runner only (runner_ask run InvFerry --watch), N>=5.  Some beats lean
//      on the door verbs (Swarm_redeem/Swarm_hello/Swarm_ferry_send) whose exact seal path must be
//       WATCHED on the runner: if the crossing needs a settle beat for the frame cascade (the
//        transport-frames-ride-post_do rule — SwarmDoor beat 5), split beat 4 into knock+settle.
//  CONVENTION (Swarm*): the world MUST be named InvFerry (do_fn_for dispatches by w.sc.w — the usual bomb).

InvFerry(A,w) {
    w.doai({req: "wrangle", eternal: 1})?.(async (req) => {
        await this.InvFerry_drive(w,req)
        req.sc.ok = 1

    })
},
InvFerry_T(w) {
    let t = w.o({ testing: 1 })[0]
    if (!t) { t = w.i({ testing: 1 }); t.c.up = w }
    return t

},
InvFerry_note(w, sc) {
    let t = this.InvFerry_T(w)
    let n = t.i(sc)
    n.c.up = t
    return n

},
// the named person's identity (test-side lookup, by the Book's own of: tag) — the SwarmStaple idiom.
InvFerry_ident(w, name) {
    return w.o({ Account: 1, of: name })[0]?.o({ Identity: 1 })[0]

},
async InvFerry_drive(w, req) {
    let n = (this.c.run)?.c.step_n
    if (n != null && n !== req.c.did_step) {
        req.c.did_step = n
        if (n === 2) { await this.InvFerry_stand(w) }
        if (n === 3) { await this.InvFerry_mint(w) }
        if (n === 4) { await this.InvFerry_seal(w) }
        if (n === 5) { await this.InvFerry_cross(w) }
        if (n === 6) { await this.InvFerry_teeth(w) }
    }
    await this.InvFerry_pump(w)
    this.InvFerry_witness(w)
    await this.InvFerry_order(w)

},
// deliverance — every account's undone mail is handled each pass (the SwarmStaple pump), so a
//  pier_hello sent this beat is heard and answered before the beat's snap.
async InvFerry_pump(w) {
    for (const acct of w.o({ Account: 1 })) {
        for (const ident of acct.o({ Identity: 1 })) { await this.Swarm_pump(w, ident) }
    }

},
// beat 2 — the parties stand.  Alice the Linkor (a full soul: keys seeded off the name, one self-issued
//  %Idzeug of CONTENT the ferry must preserve — the SwarmFerry recipe) and Cavey the blank Cave-to-be
//   (only a body key — the SwarmSpread Ebox recipe).  Pinned clock; both online.
async InvFerry_stand(w) {
    w.i({reached: "step_2"})
    w.sc.now = 1756500000
    let aacct = w.oai({ Account: 1, of: 'Alice' })
    aacct.c.up = w
    let akeys = await this.Swarm_mint_keys('InvFerry-Alice')
    let alice = this.Swarm_identity(aacct, akeys, 'Alice')
    w.c.alice = alice
    this.Swarm_online(alice, true)
    // one self-issued Idzeug so Alice's account carries content the crossing must preserve byte-for-byte
    await this.Swarm_mint_idzeug(w, alice, { Music: 1, genre: 'Jazz' }, 'invferry_content')
    let cacct = w.oai({ Account: 1, of: 'Cavey' })
    cacct.c.up = w
    let ckeys = await this.Swarm_mint_keys('InvFerry-Cavey-body')
    w.c.ckeys = ckeys
    let cavey = this.Swarm_identity(cacct, ckeys, 'Cavey')
    w.c.cavey = cavey
    this.Swarm_online(cavey, true)
    this.InvFerry_note(w, { stood: 1 })

},
// beat 3 — the MINT and the CARRY.  Swarm_ferry_link mints a MyCave invite → <base>#Iz=<token>&fc=<secret>
//  (the ANCHOR form, unparked 2026-08-31 — the whole link rides the fragment, hashchange lands it reload-free).
//  The CARRY leg: pull the #Iz token back out of the URL (Swarm_iz_of_url — the boot handler's core) and
//   parse it (Swarm_token_parse) — the compact token names Alice's prepub + the serial.  Secret rides the
//    fragment (client-side); the durable twin sits in the stash.  Note booleans only.
async InvFerry_mint(w) {
    w.i({reached: "step_3"})
    w.sc.now = 1756500010
    let alice = w.c.alice
    if (!alice) { return }
    let top = this.top_House ? this.top_House() : null
    w.c.url = await this.Swarm_ferry_link(w, alice, 'https://jamsend.example/BigSoundland')
    let url = String(w.c.url || '')
    let msoul = this.Swarm_ferry_role('soul')
    let secret = msoul ? msoul.c.secret : null
    let twin = top && top.stashed && top.stashed.ferry && top.stashed.ferry.soul ? top.stashed.ferry.soul.secret : null
    let token = this.Swarm_iz_of_url(url)
    let t = token ? this.Swarm_token_parse(token) : null
    w.c.token = token
    // CAPTURE THE CODE NOW (the first run's finding, 2026-08-30): Swarm_ferry_on_seal fires at beat 4's
    //  seal seam, auto-sends the ferry AND CLEARS the live secret + twin ("cleared on a successful send") —
    //   so a beat-5 read of top.c.ferry_secret finds nothing and the cross silently skips.  The human's
    //    copy is the #fc fragment they carried; this is the Book's fragment.
    w.c.code = secret ? String(secret) : ''
    let row = { minted: 1 }
    if (url.startsWith('https://jamsend.example/BigSoundland#Iz=') && url.includes('&' + 'fc=')) { row.url_carries_both = 1 }
    if (secret && String(secret).length === 32 && url.endsWith(('&' + 'fc=') + String(secret))) { row.secret_rides_fragment = 1 }
    if (twin && String(twin) === String(secret)) { row.twin_stashed = 1 }
    // the CARRY: the token pulled back out parses to Alice's prepub and a MyCave offer.
    if (t && t.prepub === alice.sc.prepub && t.to === 'MyCave') { row.token_carries = 1 }
    this.InvFerry_note(w, row)

},
// beat 4 — the DOOR: Cavey redeems the carried token.  Swarm_redeem delivers pier_hello to Alice; her
//  door (Swarm_hello) REGENERATES the presig off her own ledger record and prefix-matches (only the
//   issuer can wear the MAC), CLAIMS the serial (ticks the spend ledger — single-use), mints the
//    %Grant:MyCave and seals; pier_accept/pier_confirm cross on this beat's pump passes → both sides
//     land a mutual %Pier with cross-signed grants.  Asserted off the SETTLED piers.
async InvFerry_seal(w) {
    w.i({reached: "step_4"})
    w.sc.now = 1756500020
    let alice = w.c.alice
    let cavey = w.c.cavey
    let token = w.c.token
    if (!alice || !cavey || !token) { return }
    await this.Swarm_redeem(w, cavey, token)

},
// beat 5 — the CROSS: the account ferries over the sealed MyCave pier.  Once the pier stands (beat 4),
//  Swarm_ferry_send exports+seals+delivers Alice's WHOLE account over it under the #fc code; Cavey reads
//   the frame off its inbox (the real wire path — the SwarmSpread beat-5 recipe) and Swarm_ferry_heard
//    unseals it with the fragment code — Cavey now holds the very same soul key, as a Cave.  Fails
//     closed: the sealed frame carries no private key hex; a WRONG code lands no account.  The seal
//      ciphertext rides `.c` (m.c.frame); the note row is booleans, so the snap stays repeatable.
async InvFerry_cross(w) {
    w.i({reached: "step_5"})
    w.sc.now = 1756500030
    let alice = w.c.alice
    let cavey = w.c.cavey
    let ckeys = w.c.ckeys
    let top = this.top_House ? this.top_House() : null
    if (!alice || !cavey || !ckeys) { return }
    let peering = this.Swarm_peering(alice)
    let pier = peering ? peering.o({ Pier: 1, pub: ckeys.prepub })[0] : null
    if (!pier) { this.InvFerry_note(w, { crossed: 1, no_pier: 1 }); return }
    // the code the human carried on the #fc fragment — the Book's copy, captured AT MINT (beat 3), because
    //  Swarm_ferry_on_seal already fired at beat 4's seal: it auto-sent the ferry over the fresh pier and
    //   CLEARED the live secret + its twin (its own "cleared on a successful send" contract).  So by here
    //    the frame is ALREADY in Cavey's mail — the frame's presence IS the sent proof — and only the
    //     carried fragment can unseal it, exactly the live shape.
    let code = w.c.code
    let keyhex = alice.c.keys ? alice.c.keys.key : null
    let m = cavey.o({ mail: 1 })[0]?.o({ frame: 'ferry' })[0]
    let frame = m ? m.c.frame : null
    let sent = frame ? true : false
    let esoul = frame && code ? await this.Swarm_ferry_heard(w, cavey, frame, code) : null
    // fails-closed — a fresh device given the same frame with a WRONG code unseals nothing.
    let fcont = w.oai({ Account: 1, of: 'Fbox' })
    fcont.c.up = w
    let fkeys = await this.Swarm_mint_keys('InvFerry-Fbox-body')
    let fproto = this.Swarm_identity(fcont, fkeys, 'Fbox')
    let badHeard = frame ? await this.Swarm_ferry_heard(w, fproto, frame, 'wrong-ferry-code-000') : null
    let ebody = esoul ? this.Swarm_body_mine(esoul) : null
    // the account's CONTENT crossed: the Idzeug Alice self-issued at beat 2 (Music, Jazz) stands on the
    //  landed soul's Peering.  (NOT a byte-identical re-export — the landing legitimately mutates the
    //   account: Swarm_body_take writes the new Cave body onto it, so `whole` was a false claim; the
    //    first run proved every other rung and refused exactly this one.)
    let eidz = esoul ? this.Swarm_peering(esoul)?.o({ Idzeug: 1 }).find((z) => z.sc.genre === 'Jazz') : null
    let row = { crossed: 1 }
    if (sent === true) { row.ferry_sent = 1 }
    if (frame && keyhex && String(frame.sealed || '').indexOf(keyhex) < 0) { row.secret_hidden = 1 }
    if (esoul && esoul.c.keys && String(esoul.c.keys.pub) === String(alice.c.keys.pub)) { row.account_crossed = 1 }
    if (eidz) { row.content_crossed = 1 }
    if (esoul && esoul.c.keys && String(esoul.c.keys.key) === String(keyhex) && !esoul.sc.key && !esoul.sc.pub) { row.keys_thawed = 1 }
    if (ebody && ebody.sc.post === 'Cave') { row.post_cave = 1 }
    if (badHeard === null) { row.wrongcode_no_body = 1 }
    this.InvFerry_note(w, row)

},
// beat 6 — the TEETH.  A DOUBLE-SPEND: a fresh scanner redeems the SAME serial — the spend ledger says
//  no, a pier_reject crosses back (surfaced as %rebuff:rejected_spent), no Pier forms.  A FORGED presig:
//   a well-formed token with the last presig hex flipped knocks — the door regenerates its OWN presig
//    and the prefix mismatches → refuse('forged') locally, nothing spends.  (The UNKNOWN-serial refusal
//     and the NAME-GATE are adjacent features landing separately — Inv_ferry_todo §4 — NOT asserted here.)
async InvFerry_teeth(w) {
    w.i({reached: "step_6"})
    w.sc.now = 1756500040
    let alice = w.c.alice
    let token = w.c.token
    if (!alice || !token) { return }
    // double-spend: Eve redeems the already-claimed serial.
    let eacct = w.oai({ Account: 1, of: 'Eve' })
    eacct.c.up = w
    let ekeys = await this.Swarm_mint_keys('InvFerry-Eve-body')
    let eve = this.Swarm_identity(eacct, ekeys, 'Eve')
    this.Swarm_online(eve, true)
    await this.Swarm_redeem(w, eve, token)
    // forged presig: flip the token's last hex — well-formed, real serial, but the door's regenerated
    //  presig no longer prefix-matches.  Mallory's redeem must refuse at the door.
    let macct = w.oai({ Account: 1, of: 'Mallory' })
    macct.c.up = w
    let mkeys = await this.Swarm_mint_keys('InvFerry-Mallory-body')
    let mallory = this.Swarm_identity(macct, mkeys, 'Mallory')
    this.Swarm_online(mallory, true)
    let tam = token.slice(0, -1) + (token.endsWith('0') ? '1' : '0')
    await this.Swarm_redeem(w, mallory, tam)
    this.InvFerry_note(w, { teethed: 1 })

},
// ── the witness — DURABLE sworn facts via this.story_swear (idempotent per run; evidence on the
//  Assertioning shelf never snap bytes; contract under the toc step lines).  Happened-FACTS of the
//   exchange, each gated on the beat's boolean note row plus the live sealed piers.  No commas — em-dashes.
InvFerry_witness(w) {
    let T = this.InvFerry_T(w)
    let alice = this.InvFerry_ident(w, 'Alice')
    let cavey = this.InvFerry_ident(w, 'Cavey')
    if (!alice || !cavey) { return }
    // beat 3: mint + carry — the URL carries the token and the off-relay fragment; the token parses back.
    let m = T.o({ minted: 1 })[0]
    if (m && +m.sc.url_carries_both === 1 && +m.sc.secret_rides_fragment === 1 && +m.sc.twin_stashed === 1 && +m.sc.token_carries === 1) { this.story_swear(w, 'the invite is minted and carried — a serial off the Idzeug issuer rides the URL and parses back to a MyCave offer while the secret rides the fragment off the relay and a durable twin stashes for reload') }
    // beat 4: the door — the presig regenerated and the serial claimed; both sides hold a mutual Pier.
    let aPier = this.Swarm_peering(alice)?.o({ Pier: 1, pub: cavey.sc.prepub })[0]
    let cPier = this.Swarm_peering(cavey)?.o({ Pier: 1, pub: alice.sc.prepub })[0]
    let aGave = aPier?.o({ Grant: 'MyCave', by: alice.c.keys?.pub })[0]
    if (aPier && cPier && aGave) { this.story_swear(w, 'the door verifies and claims — the presig regenerates off the issuer ledger and only the issuer can wear the MAC then the serial ticks the spend ledger and both sides land a mutual Pier bearing the cross-signed MyCave grant') }
    // beat 5: the account crosses whole and secret; the keys thaw to .c; a wrong code lands nothing.
    let cr = T.o({ crossed: 1 })[0]
    if (cr && +cr.sc.ferry_sent === 1 && +cr.sc.account_crossed === 1 && +cr.sc.content_crossed === 1 && +cr.sc.secret_hidden === 1 && +cr.sc.keys_thawed === 1 && +cr.sc.post_cave === 1 && +cr.sc.wrongcode_no_body === 1) { this.story_swear(w, 'the account crosses the sealed pier — the soul seals across under the fragment code and the blank device unseals it and now holds the very same soul key and its issued content as a Cave with keys on .c only while the frame hides the secret and a wrong code lands no account') }
    // beat 6: the teeth — the spent serial refuses a re-scan; a forged presig refuses at the door.
    let eve = this.InvFerry_ident(w, 'Eve')
    let mallory = this.InvFerry_ident(w, 'Mallory')
    let evePier = eve ? this.Swarm_peering(eve)?.o({ Pier: 1 })[0] : null
    let malPier = mallory ? this.Swarm_peering(mallory)?.o({ Pier: 1 })[0] : null
    if (eve && eve.o({ rebuff: 'rejected_spent' })[0] && !evePier && mallory && mallory.o({ rebuff: 'forged' })[0] && !malPier) { this.story_swear(w, 'the exchange fails closed — a re-scan of the spent serial is rebuffed and forms no Pier and a forged presig refuses at the door because only the issuer key regenerates the MAC') }

},
// InvFerry_order — float A:InvFerry to the front of H/* so the Run snap stays readable.
async InvFerry_order(w) { const H = this;
    let As = H.o({A: 1})
    if (!As.length) { return }
    let first = (a) => (a.sc.A === 'InvFerry') ? 0 : 1
    let sorted = [...As].sort((a, b) => first(a) - first(b))
    let ordered = [...sorted, ...H.o().filter(c => !c.sc.A)]
    await this.place({}, ordered)

},

    })
    })
</script>
