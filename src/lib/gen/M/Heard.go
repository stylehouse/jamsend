<script lang="ts">
    // GENERATED by Lang compile — do not edit by hand.
    import { TheC } from "$lib/Stuff.svelte"
    import { onMount } from "svelte"

    let { H } = $props()

    onMount(async () => {
    await H.eatfunc({

    Ghostmeta_Ghost_M_Heard(): string { return 'ac0c56af5e989abc~g1' },

// Heard.g — THE HEARD MAG: what I heard, of whom, and what I took (Radio_circuit_todo.md).
//  One Mag under my own identity — `%Mag:heard,pub:<me>` — holding one `%Card,id,pub` per track the
//   radio ever played me, in `%Cloud,page:N` pages, one page per sitting.  A Card has TWO STAGES and
//    nothing else:
//      heard — `id, pub, played_through`.  BARE IDS (Mag_todo §6b, ruled 2026-07-19: "listening history …
//       keep it OBLIQUE — bare ids, no titles|paths").  `played_through` counts play-throughs with a
//        person in the room.
//      taken — `hearted_at` plus the listing, cloned from the friend's answer to a describe once it lands
//       (`title, artist, dir, path, bytes, body_hash`) and `keep:<their keep-id>` naming the ORIGINAL.
//  Acting is the moment a track becomes yours to hold, so the listing arrives with the act, never before.
//
//  STAMPS, NOT FLAGS (2026-09-21, Repli's law: a key cannot be UN-set over the wire — `op:delete` is
//   whole-particle — so a flag that gets STRIPPED on every re-press or every retry is a delete pretending
//    to be a change, and the crew mirror (SoundPooling_todo §0.0 rung 3) would lose it on the first sync).
//   Every reaction and every outcome is now a TIMESTAMP that only ever gets written forward, never
//    cleared: `hearted_at` / `nayed_at` / `mehed_at` (the three reactions — `Heard_reaction` picks whichever
//     is NEWEST as the card's current word, so a later press outdating an older nay is a COMPARISON, not a
//      deletion — "a nay ends a yay" is now just "nayed_at > hearted_at"), `already_had_at` /
//       `landing_failed_at` / `offer_unsigned_at` (the three verdicts — `Heard_verdict` only counts one that
//        is NEWER than `hearted_at`, so "a later hearted_at outdates a verdict" is the same comparison),
//         `landed_at` / `carried_by`+`carried_at` (the handoff echo, §9.7), `pressed_on` (this card arrived
//          via a sibling — replaces the old `via` NAME field; the display name derives from `via_addr`,
//           already durable since rung 1), `looked_at` (replaces `unseen`'s delete-to-clear — `Heard_news`
//            derives "is there anything new" by comparing the newest event stamp against it).  The only
//             function that still DELETES anything is `Heard_untake` — the human's explicit ✕, a local
//              act with no wire-replication claim on it, unlike every writer above.
//
//  THIS MAG IS FOUR READERS AT ONCE, which is the whole reason it exists (Radio_circuit_todo §1b):
//   the DEDUP set the dial skips by (it replaces `radio.c.heard`, a 100-cap runtime bag that died on
//    every reload), the durable OBLIQUE cursor Mag_todo §8 ruled six weeks ago and never had a home,
//     `Radio_mag_cursor`'s source, and the taste ledger the heist reads.  One Mag, four readers, no `.c`.
//
//  THE HEIST IS A QUERY, NOT A STORE.  "What am I owed?" is `take` Cards whose track is not on my
//   shelf, grouped by `pub`, oldest `at` first (Heard_takes).  There is no %Jam, no %Like, no %Grab, no
//    %Caper-as-ledger: a heist in progress IS that query plus the one `%Heist` keep the beat mints from
//     it.  The keep is transient scaffolding for the byte lane; the Card is the intent and the ledger.
//
//  NOTHING HERE EVER CROSSES.  The Mag hangs under `%Identity` (or, in a Book, under the `Mine,pub`
//   HOME — never its `stock` shelf, which is the Repli unit).  A Card is not a holding and must never
//    read as one: `Repli_identity_keys` and `Swarm_graft` carry `Card: ['id','pub']` so a re-import
//     upserts rather than twinning every Card whose `played_through` moved.
//
//  THE CLOCK IS `Swarm_now` (a Book pins `w.sc.now`), never a bare Date.now — every `_at` stamp and
//   `created_at` are snapped scalars and a wall clock in a fixture is churn.  And a `played_through` tick
//    NEVER bumps: a bump on `%Identity` rewrites the whole account file inside the beliefs mutex, and a
//     play-through is not worth a disk write (Radio_circuit_todo §5).

//#region the mag — where it hangs, and the page per sitting

// Heard_now — the swarm clock, so a Book pinning `w.sc.now` gets a byte-identical snap.
Heard_now(w) {
    return this.Swarm_now ? this.Swarm_now(w) : Math.floor(Date.now() / 1000)

},
// Heard_home_find — WHERE THE MAG HANGS, found and never minted.  The live `%Identity` when this pub
//  IS the machine's own identity (so it rides the account snap and the stash for free), else the
//   `Mine,pub:<me>` HOME — the home, not its `stock` shelf: `Ra_offer_stock` walks the shelf, so a
//    Mag one level up cannot be swept into an offer by accident.  A Book pins `w.c.ra_pub` and takes
//     the second road, which is why every fixture here is world-local and crosses nothing.
Heard_home_find(w, me) {
    if (!w || !me) { return null }
    let M = this.top_House ? this.top_House() : null
    let live = (M && M.Swarm_live_self) ? M.Swarm_live_self() : null
    if (live && String(live.sc.prepub || '') === String(me)) { return live }
    return w.o({ Mine: 1, pub: String(me) })[0] || null
},
// Heard_home — the same door, find-or-CREATE.  Only a writer walks through it.
Heard_home(w, me) {
    if (!w || !me) { return null }
    let found = this.Heard_home_find(w, me)
    if (found) { return found }
    let home = w.oai({ Mine: 1, pub: String(me) })
    home.c.up = w
    return home

},
// Heard_mag_find / Heard_mag — the Mag itself.  EVERY READER TAKES THE `_find` DOOR: this is read by
//  the dial on every pump tick, and a reader built on `oai` mints by being asked — the shape that turns
//   a face into a writer ([[a-read-helper-inherits-creation]], the reason Heist_shop_find exists).
Heard_mag_find(w, me) {
    let home = this.Heard_home_find(w, me)
    return home ? (home.o({ Mag: 'heard', pub: String(me) })[0] || null) : null
},
Heard_mag(w, me) {
    return this.Heard_mag_at(this.Heard_home(w, me), me)
},
// Heard_mag_at — the Mag under ANY container, find-or-create.  The identity doors above resolve the home
//  off the live self; a hand-built shelf (a Book's, or the pool fixtures') has no identity to resolve, so
//   the container is passed in directly.  `pub` is elided when absent rather than stamped empty — an empty
//    scalar in a snap is a mint bug, not a value.
Heard_mag_at(container, pub) {
    if (!container) { return null }
    let mag = pub ? container.oai({ Mag: 'heard', pub: String(pub) }) : container.oai({ Mag: 'heard' })
    mag.c.up = container
    // the rules ride the Mag line — visible, snapped, Book-gated, no %Rules particle (§2).  Only the
    //  two TTLs in v1.0: the ambient road (played_through ⇒ take with no heart pressed) wants a screen first.
    if (!mag.sc.heard_ttl) { mag.sc.heard_ttl = '30'; mag.sc.take_ttl = '90'; mag.bump() }
    return mag
},
// Heard_mag_near — the Mag a SHELF can see: on the shelf itself, else on the container above it.  The
//  taste readers are handed a `stock` shelf (live) or a bare hand-built one (every pool fixture), and the
//   Mag hangs one level up in the first case and directly on it in the second.  Matches by VALUE only
//    (`{Mag:'heard'}`), so it finds the Mag whatever `pub` it wears.  Pure `o`, mints nothing.
Heard_mag_near(shelf) {
    if (!shelf) { return null }
    let here = shelf.o({ Mag: 'heard' })[0]
    if (here) { return here }
    let up = shelf.c ? shelf.c.up : null
    return up ? (up.o({ Mag: 'heard' })[0] || null) : null
},
// Heard_seed — mint or refresh one Card directly under a container's heard Mag, from a plain sc bag
//  ({id, pub?, played_through?, hearted_at?, keep?, title?, …any stamp}).  The door for a Book and for the
//   stash rehydrate: both have the facts in hand and no identity to resolve them through.  Idempotent by
//    (id, pub).  Generic — every key in the bag lands verbatim, so a renamed stamp needs nothing here.
Heard_seed(container, e) {
    if (!container || !e || !e.id) { return null }
    // THE MAG WEARS ITS OWNER'S PUB, THE CARD WEARS THE HOLDER'S (fixed 2026-09-05).  This keyed the Mag by
    //  the CARD's pub — `Mag:heard,pub:Cave` on an identity whose restash, wipe, rehydrate and every reader
    //   look for `Mag:heard,pub:<prepub>` — so a seeded wish sat on a Mag nobody else could find: zero rows
    //    stashed, nothing back after the wipe, and SwarmReboot's idempotence swear blocked on a count of ZERO
    //     (`idem_heard_doubled`, misnamed for what it caught).  An identity owns by prepub, a `Mine,pub` home
    //      by pub, a bare shelf by nothing.
    let me = container.sc ? String(container.sc.prepub || container.sc.pub || '') : ''
    let mag = this.Heard_mag_at(container, me)
    let card = this.Heard_find(mag, e.id, e.pub)
    if (!card) {
        let pg = this.Heard_page(mag, +(e.at || 0))
        card = pg.i(e.pub ? { Card: 1, id: String(e.id), pub: String(e.pub) } : { Card: 1, id: String(e.id) })
        card.c.up = pg
    }
    for (const k of Object.keys(e)) {
        if (k === 'id' || k === 'pub') { continue }
        if (e[k] == null || e[k] === '') { continue }
        card.sc[k] = String(e[k])
    }
    card.bump()
    return card

},
// Heard_page — THE OPEN PAGE, one per SITTING.  `mag.c.sitting` is a runtime latch, so the first touch
//  of a boot mints the next page and everything that boot hears shares it.  That makes `created_at` the
//   page's own coordinate — "Tuesday's page" — which is what a human reading the snap wants, and it is
//    what the GC ages by (§3): one clock read per page instead of one per Card.
//  No `era`, no promotion, no move: a page only ever grows at the back and leaves at the front.
Heard_page(mag, now) {
    if (!mag) { return null }
    if (mag.c.sitting) { return mag.c.sitting }
    let last = 0
    for (const p of mag.o({ Cloud: 1 })) { let n = +(p.sc.page || 0); if (n > last) { last = n } }
    let pg = mag.i({ Cloud: 1, page: '' + (last + 1) })
    pg.c.up = mag
    if (now) { pg.sc.created_at = '' + now }
    mag.c.sitting = pg
    return pg

},
// Heard_cards — every Card, across every page, in page order.  The one walk; nothing else iterates the
//  Clouds by hand.
Heard_cards(mag) {
    let out = []
    if (!mag) { return out }
    for (const pg of mag.o({ Cloud: 1 })) { for (const c of pg.o({ Card: 1 })) { out.push(c) } }
    return out

},
//#region THE MIRROR — rung 3 (SoundPooling_todo §0.0 2026-09-21): every sibling's heard Mag, echoed
//  beside my own.  `Heard_gossip_beat` (below, called off `Heard_settle`) sends MY OWN Mag's protocol
//   snap to every sibling body; `Swarm_heard_mirror` (Swarm.g) decodes an incoming one and grafts it
//    HERE — a `%TheirHeard,pub:<sibling>` home holding that sibling's OWN `%Mag:heard,pub:<sibling>`,
//     never into my own Mag (their pages are THEIR sittings and do not line up with mine).  A NEW
//      mainkey, deliberately — the music-mirror `%Theirs` (Ra.g, Radio.g, Pool.g, Heist.g, Swarm.g,
//       Repli.g all walk `o({Theirs:1})`, a presence WILDCARD that matches any stored value) would have
//        swept a heard-mag mirror into every one of those walks as a bogus music shelf with no `stock`.
//         The ~8 readers below iterate `Heard_mags`/`Heard_cards_union` instead of one Mag, so a wish
//          pressed on any one body is visible — and haulable — from every other.
// Heard_mirror_home — the container ONE sibling's mirror hangs under, found (`_find`) or find-or-create.
Heard_mirror_home_find(w, me, from) {
    let home = this.Heard_home_find(w, me)
    return home ? (home.o({ TheirHeard: 1, pub: String(from) })[0] || null) : null
},
Heard_mirror_home(w, me, from) {
    let home = this.Heard_home(w, me)
    if (!home) { return null }
    let t = home.oai({ TheirHeard: 1, pub: String(from) })
    t.c.up = home
    return t
},
// Heard_mirror_mag — the SAME door, one level down: the sibling's own Mag under its Theirs home,
//  find-or-create.  What `Swarm_heard_mirror` (Swarm.g) lands a decoded snap into.
Heard_mirror_mag(w, me, from) {
    let t = this.Heard_mirror_home(w, me, from)
    if (!t) { return null }
    let mag = t.oai({ Mag: 'heard', pub: String(from) })
    mag.c.up = t
    return mag
},
// Heard_mags — MY OWN Mag first, then every sibling's mirrored one (rung 3).  The readers below walk
//  this instead of a single `Heard_mag_find`.
Heard_mags(w, me) {
    let out = []
    let mine = this.Heard_mag_find(w, me)
    if (mine) { out.push(mine) }
    let home = this.Heard_home_find(w, me)
    if (!home) { return out }
    for (const t of home.o({ TheirHeard: 1 })) {
        let m = t.o({ Mag: 'heard' })[0]
        if (m) { out.push(m) }
    }
    return out
},
// Heard_mags_near — the `Heard_mag_near` door, unioned: the taste readers (Heard_tally, Heard_latest)
//  are handed a SHELF, not an identity, so they climb from the shelf's own container instead of
//   resolving `me`.  Mirrors land beside the Mag at the SAME level, so the same `up` climb finds them.
Heard_mags_near(shelf) {
    let out = []
    let near = this.Heard_mag_near(shelf)
    if (near) { out.push(near) }
    if (!shelf) { return out }
    let home = shelf.o({ Mag: 'heard' })[0] ? shelf : (shelf.c ? shelf.c.up : null)
    if (!home) { return out }
    for (const t of home.o({ TheirHeard: 1 })) {
        let m = t.o({ Mag: 'heard' })[0]
        if (m) { out.push(m) }
    }
    return out
},
// Heard_cards_union / Heard_cards_union_near — one entry per (id, pub) across a set of mags, picking
//  whichever copy carries the MOST outcome (landed > a verdict > carried_by > a bare take) and, among
//   ties, the newest hearted_at.  This is how "the phone's word derives from the laptop's mirrored
//    stamps" works: the phone's OWN copy of a wish it pressed is bare, but the union also holds the
//     LAPTOP's copy of that same (id, pub) — adopted and carried (Heard_adopt) — which outranks the bare
//      one and wins the read.  Two doors onto the one shared core (Heard_cards_union_of): identity-based
//       (the ~8 readers keyed by `me`) and shelf-based (Heard_tally/Heard_latest, handed a shelf instead).
Heard_card_rank(c) {
    if (c.sc.landed_at) { return 4 }
    if (this.Heard_verdict(c)) { return 3 }
    if (c.sc.carried_by) { return 2 }
    return 1
},
Heard_cards_union_of(mags) {
    let by = {}
    let order = []
    for (const mag of mags) {
        for (const c of this.Heard_cards(mag)) {
            if (!c.sc.id) { continue }
            let k = String(c.sc.id) + ' ' + String(c.sc.pub || '')
            let cur = by[k]
            if (!cur) { by[k] = c; order.push(k); continue }
            let a = this.Heard_card_rank(cur)
            let b = this.Heard_card_rank(c)
            if (b > a || (b === a && +(c.sc.hearted_at || 0) > +(cur.sc.hearted_at || 0))) { by[k] = c }
        }
    }
    return order.map((k) => by[k])
},
Heard_cards_union(w, me) {
    return this.Heard_cards_union_of(this.Heard_mags(w, me))
},
Heard_cards_union_near(shelf) {
    return this.Heard_cards_union_of(this.Heard_mags_near(shelf))
},
// Heard_adopt — A TROVE BODY CLAIMING A UNION WISH IT DOES NOT ITSELF CARRY (rung 3 — replaces the
//  whole handoff-lane relay).  Writing the outcome onto a MIRROR home would never reach the presser —
//   only MY OWN Mag re-gossips (Heard_gossip_beat) — so hauling a sibling's wish first re-stamps the
//    SAME (id, pub) onto my own Mag, taken, `pressed_on` (this was not my own press — Heard_news
//     suppresses my own nag for it), floored past anything already there.  Idempotent: a card already
//      showing `take` here is left exactly as it is.  Heard_clone_beat then finds it the ordinary way,
//       and the ordinary gossip mile (fired by Heard_settle below) carries the outcome straight back —
//        no separate ack frame, unlike the rung-1 lane this retires.
Heard_adopt(w, me, card) {
    if (!w || !me || !card || !card.sc.id) { return null }
    let mine = this.Heard_card(w, me, String(card.sc.id), String(card.sc.pub || ''))
    if (!mine) { return null }
    if (this.Heard_reaction(mine) === 'take') { return mine }
    let at = this.Heard_react_at(w, mine, ['nayed_at', 'mehed_at', 'already_had_at', 'offer_unsigned_at', 'landing_failed_at'])
    mine.sc.hearted_at = at
    mine.sc.pressed_on = at
    if (card.sc.title && !mine.sc.title) { mine.sc.title = String(card.sc.title) }
    if (card.sc.artist && !mine.sc.artist) { mine.sc.artist = String(card.sc.artist) }
    mine.bump()
    this.Heard_settle(w, me, 'heard_adopt')
    return mine
},
// Heard_mirror_stamp_keys / Heard_mirror_listing_keys — the two halves of a Card a mirror merge touches:
//  every stamp MAX-MERGES (never overwrites backward); the listing rides along only when a stamp
//   actually advanced, so a stale re-delivered frame writes nothing at all.
Heard_mirror_stamp_keys() {
    return ['hearted_at', 'nayed_at', 'mehed_at', 'played_through', 'pressed_on', 'carried_at', 'landed_at', 'already_had_at', 'landing_failed_at', 'offer_unsigned_at', 'looked_at']
},
Heard_mirror_listing_keys() {
    return ['title', 'artist', 'dir', 'path', 'bytes', 'body_hash', 'keep', 'why', 'waiting_for', 'carried_by', 'via_addr']
},
// Heard_mirror_merge — land a decoded sibling snap's Cards onto MY mirror of them.  Swarm.g's
//  `Swarm_heard_mirror` is the wire receiver; this is the pure merge it calls, kept here beside the key
//   lists it depends on.  Two DIFFERENT bodies' clocks can disagree even though Heard_react_at (rung 2)
//    already rules out a tie WITHIN one body's own writes, so a blind graft (whole-card overwrite) could
//     roll a stamp backward on a delayed or reordered frame — every stamp here takes the higher value.
Heard_mirror_merge(mag, got_C, now) {
    if (!mag || !got_C) { return 0 }
    let n = 0
    for (const pg of got_C.o({ Cloud: 1 })) {
        for (const inc of pg.o({ Card: 1 })) {
            if (!inc.sc.id) { continue }
            let card = this.Heard_mag_card(mag, String(inc.sc.id), String(inc.sc.pub || ''), now)
            if (!card) { continue }
            let advanced = 0
            for (const k of this.Heard_mirror_stamp_keys()) {
                let a = +(card.sc[k] || 0)
                let b = +(inc.sc[k] || 0)
                if (b > a) { card.sc[k] = String(b); advanced = 1 }
            }
            if (!advanced) { continue }
            for (const k of this.Heard_mirror_listing_keys()) { if (inc.sc[k] != null && inc.sc[k] !== '') { card.sc[k] = String(inc.sc[k]) } }
            card.bump()
            n = n + 1
        }
    }
    if (n) { mag.bump() }
    return n
},
//#endregion

// Heard_find — the Card for one (id, pub), anywhere in the Mag.  ONE CARD PER (id, pub), find-or-create,
//  never re-minted: "heard again" bumps the Card that exists, so nothing ever duplicates an id across
//   pages and a heist holding a Card can never be handed a corpse.
Heard_find(mag, id, pub) {
    if (!mag || !id) { return null }
    for (const pg of mag.o({ Cloud: 1 })) {
        let hit = pub ? pg.o({ Card: 1, id: String(id), pub: String(pub) })[0] : pg.o({ Card: 1, id: String(id) })[0]
        if (hit) { return hit }
    }
    return null
},
// Heard_mag_card — find-or-create a Card directly under an ALREADY-RESOLVED Mag (not an identity) —
//  the door a mirror home uses (Heard_mirror_merge), since a sibling's mirror has no identity to
//   resolve through.  `Heard_card` below is this same door, resolving the Mag from `(w, me)` first.
Heard_mag_card(mag, id, pub, now) {
    if (!mag || !id) { return null }
    let card = this.Heard_find(mag, id, pub)
    if (card) { return card }
    let pg = this.Heard_page(mag, now)
    let sc = { Card: 1, id: String(id) }
    if (pub) { sc.pub = String(pub) }
    card = pg.i(sc)
    card.c.up = pg
    return card
},
// Heard_card — find-or-create, landing a fresh Card in the open page.
Heard_card(w, me, id, pub) {
    if (!id) { return null }
    let mag = this.Heard_mag(w, me)
    if (!mag) { return null }
    return this.Heard_mag_card(mag, id, pub, this.Heard_now(w))
},
//#endregion

//#region the two events — a track started, a track played through, a heart pressed

// Heard_mark — A TRACK STARTED.  The Card exists from here on, oblique: id, who.  Called from
//  Radio_open, which is the one place every route into the playhead passes through.
Heard_mark(w, me, rec) {
    if (!w || !me || !rec || !rec.sc.id) { return null }
    let pub = this.Ra_pub_of ? (this.Ra_pub_of(rec) || '') : ''
    return this.Heard_card(w, me, rec.sc.id, pub)

},
// Heard_through — PLAYED ALL THE WAY THROUGH, WITH A PERSON IN THE ROOM.  A kitchen phone playing to an
//  empty room is not attention, and `humdinger` is already the app's word for "a human is here" — so the
//   same predicate that keeps a Book from growing a Streams Mag keeps it from growing taste.
//  NO BUMP, deliberately (§5): `%Identity` bumps rewrite the whole account file inside the beliefs mutex,
//   and a play-through must never cost a disk write.  The value is snapped the next time anything else
//    on the Mag bumps — a take, an un-take, a page roll — which is soon enough for a score nobody reads.
//#region REACTIONS — Yay (♥, `take`), Nay, Meh: what a person indicates toward material (the owner 2026-09-15:
//  "they're all reactions|moods the user indicates towards some material"; there is NO unlove, anywhere).
//   Every reaction is a scalar on the Card, like every road (THE HANDOFF header) — no state elsewhere.
//   · Nay: bars the track from every future pool draw and evicts a pooled copy; NEVER deletes a heisted file.
//   · Meh: written by the radio on an EARLY skip (humdinger only — a Book's skips are fixtures, not moods);
//      the pool never draws it. A later ♥ outranks a meh; a nay ends a yay.
// Heard_settle — a reaction is a change to the durable ledger, so it goes durable NOW through the ONE outcome
//  seam (Swarm_account_settle: whole-ledger restash + the disk-mirror nudge).  Until 2026-09-17 no reaction called
//   it: a ♥ or a 👎 reached the stash only when some pier/crew frame happened to settle later, and a Nay pressed
//    before a reload was simply gone the next morning.  Live-self-guarded inside, so a Book's shelf no-ops.
Heard_settle(w, me, why) {
    let M = this.top_House ? this.top_House() : null
    let live = (M && M.Swarm_live_self) ? M.Swarm_live_self() : null
    if (!live || String(live.sc.prepub || '') !== String(me) || !M.Swarm_account_settle) { return 0 }
    try { M.Swarm_account_settle(live, why) } catch (e) {}
    try { this.Heard_gossip_beat(w, live) } catch (e) {}
    return 1
},
// Heard_gossip_beat — THE MIRROR MILE (rung 3): MY OWN heard Mag, snapped under the protocol's
//  reaction-only law (bare hearings and GC'd rows never cross — the OBLIQUE ruling, unchanged), to
//   every sibling body.  The SAME target `Heard_hand_beat` used to reach (my OWN device family,
//    `Swarm_body_roster`) — now ALL of them at once, not just the first trove one, since nobody is
//     "handed" anything any more; a trove sibling just SEES this in its own union read and hauls it.
//      Store-and-forward like the roster: a miss is fine, re-sent on every future reaction AND whenever
//       a sibling's roster frame proves it just came online (Swarm_roster_heard's wake, Swarm.g).
async Heard_gossip_beat(w, ident) {
    if (!w || !ident) { return 0 }
    let me = String(ident.sc.prepub || '')
    if (!this.Heard_hand_on(w, me)) { return 0 }   // the person switched the cross-device road off
    let mag = this.Heard_mag_find(w, me)
    if (!mag) { return 0 }
    let out = await this.enWaft(mag, { matching: this.Swarm_protocol('heard') })
    if (out.errors && out.errors.length) { return 0 }
    let mineaddr = this.Heard_hand_myaddr(ident)
    let sent = 0
    for (const b of this.Swarm_body_roster(ident)) {
        let addr = this.Swarm_body_addr(b)
        if (!addr || addr === mineaddr) { continue }
        if (this.Swarm_sibling_reach(w, ident, b, { kind: 'heard', page: this.Swarm_page(ident), from: mineaddr, snap: out.snap })) { sent = sent + 1 }
    }
    return sent
},
// Heard_react_at — the timestamp a reaction writer stamps: `Heard_now`, floored past whatever the OTHER
//  two reactions already wear (2026-09-21).  Real seconds-resolution clocks (and a Book's PINNED one) can
//   land two different reactions in the exact same second — press, nay, in one beat — and since nothing
//    strips any more, Heard_reaction's "newest wins" needs a genuine ordering to compare, not a tie.  This
//     guarantees the CALL that happens later always reads later, one second past the highest it beat.
Heard_react_at(w, card, others) {
    let now = this.Heard_now(w)
    let floor = 0
    for (const k of others) { let v = +(card.sc[k] || 0); if (v > floor) { floor = v } }
    return '' + Math.max(now, floor + 1)
},
Heard_nay(w, me, rec, by) {
    if (!w || !me || !rec || !rec.sc.id) { return 0 }
    let card = this.Heard_card(w, me, this.Heard_take_id(rec), this.Heard_take_pub(rec, by))
    if (!card) { return 0 }
    card.sc.nayed_at = this.Heard_react_at(w, card, ['hearted_at', 'mehed_at'])
    card.bump()
    this.Heard_settle(w, me, 'heard_nay')
    return 1
},
Heard_meh(w, me, rec, by) {
    let M = this.top_House ? this.top_House() : null
    if (!M || !M.c.humdinger) { return 0 }
    if (!w || !me || !rec || !rec.sc.id) { return 0 }
    let card = this.Heard_card(w, me, this.Heard_take_id(rec), this.Heard_take_pub(rec, by))
    // ever hearted or nayed ⇒ a background skip is refused forever, exactly as before (the OLD guard
    //  read the same two flags — under stamps-not-flags they just never go away either, so the check is
    //   the same presence test it always was).
    if (!card || card.sc.hearted_at || card.sc.nayed_at) { return 0 }
    card.sc.mehed_at = this.Heard_react_at(w, card, ['hearted_at', 'nayed_at'])
    card.bump()
    this.Heard_settle(w, me, 'heard_meh')
    return 1
},
// Heard_reaction — WHICH OF THE THREE IS CURRENT, decided by comparing timestamps rather than by which
//  key survived a strip (2026-09-21): "a nay ends a yay" is now `nayed_at > hearted_at`, not a delete.
//   Every reader that used to test `card.sc.take`/`nay`/`meh` directly goes through this now.
Heard_reaction(card) {
    if (!card) { return '' }
    let best = ''
    let bestAt = 0
    for (const r of [['take', 'hearted_at'], ['nay', 'nayed_at'], ['meh', 'mehed_at']]) {
        let at = +(card.sc[r[1]] || 0)
        if (at > bestAt) { best = r[0]; bestAt = at }
    }
    return best
},
// Heard_barred_ids — {id:1} for every track whose CURRENT reaction is Nay or Meh, mine or any sibling's
//  (rung 3): the pool's exclusion set, read once per steward pass (Ra_quarter).  Ids are ORIGINALS, the
//   Mag's own id-space.
Heard_barred_ids(w, me) {
    let out = {}
    for (const c of this.Heard_cards_union(w, me)) {
        if (!c.sc.id) { continue }
        let r = this.Heard_reaction(c)
        if (r === 'nay' || r === 'meh') { out[String(c.sc.id)] = 1 }
    }
    return out
},
//#endregion

Heard_through(w, me, rec) {
    let M = this.top_House ? this.top_House() : null
    if (!M || !M.c.humdinger) { return 0 }
    let card = this.Heard_mark(w, me, rec)
    if (!card) { return 0 }
    card.sc.played_through = '' + ((+(card.sc.played_through || 0)) + 1)
    return 1

},
// Heard_verdict_field — the STAMP a wire verdict key lands on (2026-09-21): the wire vocabulary
//  (held/unvouched/landfail, also what a job's own markers wear — Heard_verdict_of reads those, a
//   DIFFERENT particle, untouched by this rename) stays the short name; the Card wears the timestamp.
Heard_verdict_field(key) {
    if (key === 'held') { return 'already_had_at' }
    if (key === 'unvouched') { return 'offer_unsigned_at' }
    if (key === 'landfail') { return 'landing_failed_at' }
    return ''
},
// Heard_verdict_keys — the wire vocabulary (unchanged) plus `why`, still what Heard_untake forgets.
Heard_verdict_keys() {
    return ['held', 'unvouched', 'landfail', 'why']
},
// Heard_listing_keys — what a taken Card clones off the friend's answer (§1, stage 2).
Heard_listing_keys() {
    return ['title', 'artist', 'dir', 'path', 'bytes', 'body_hash', 'keep']
},
// Heard_verdict — the answered-but-not-landed word on a Card, or ''.  A verdict only counts if it is NEWER
//  than `hearted_at` (2026-09-21): a re-press no longer STRIPS the old verdict, it OUTDATES it by
//   comparison — "asking again" is a later hearted_at, not a deletion.
Heard_verdict(card) {
    if (!card) { return '' }
    let hearted = +(card.sc.hearted_at || 0)
    let best = ''
    let bestAt = 0
    for (const key of ['held', 'unvouched', 'landfail']) {
        let at = +(card.sc[this.Heard_verdict_field(key)] || 0)
        if (at && at >= hearted && at > bestAt) { best = key; bestAt = at }
    }
    return best

},
// Heard_take — THE HEART.  Not a score that "reaches" a threshold: the heart is a decision, and a
//  threshold nobody can see is magic (§2, the human lens).  (The fat-thumb un-press that once lived here is
//   UN-presses it — every phone there is means undo by that gesture, and an accidental press had no exit
//    but a ✕ on a keep that might not have been minted yet.  A press outside the window re-affirms: it
//     re-stamps `hearted_at` (re-arming the gave-up clock and OUTDATING any failure verdict by comparison
//      — Heard_verdict — rather than clearing it), so pressing again is how a human retries a track the
//       wire could not bring.
//  Returns 1 for taken, -1 for un-taken, 0 for nothing.
//  A POOL COPY NAMES ITS ORIGINAL, WITH NO HOLDER (2026-09-15, Love_todo §0): a pooled %Record is
//   `id:<lofi>,of:<original>` and carries no holder on purpose (the pool does not track where its try-outs
//    came from), and its shelf's pub is ME — so a heart on it used to write a take on my own music, which
//     is a taste fact nobody hauls. The take names `of` and leaves `pub` off; Heard_takes resolves a
//      holder-less card against the mirrors when it is time to haul. Heard_take_id/_pub are the one seam.
Heard_take_id(rec) {
    return String((rec && rec.sc && (rec.sc.of || rec.sc.id)) || '')
},
Heard_take_pub(rec, by) {
    if (rec && rec.sc && rec.sc.of) { return '' }
    return String(by || (this.Ra_pub_of ? this.Ra_pub_of(rec) : '') || '')
},
Heard_take(w, me, rec, by) {
    if (!w || !me || !rec || !rec.sc.id) { return 0 }
    let pub = this.Heard_take_pub(rec, by)
    let card = this.Heard_card(w, me, this.Heard_take_id(rec), pub)
    if (!card) { return 0 }
    // NO UNLOVE, ANYWHERE (the owner 2026-09-15): a heart is a reaction, and a second press re-affirms —
    //  re-stamps `hearted_at` (re-arming the gave-up clock).  Nothing is CLEARED any more (2026-09-21,
    //   stamps not flags): a fresh hearted_at simply OUTDATES a stale verdict or a stale nay by comparison
    //    (Heard_verdict, Heard_reaction) — the wire never needed to un-set anything for a re-press to work.
    //     Taking it back is not a thing a heart does; a keep is called off on the Haul row, and a Nay is
    //      its own reaction.  Floored past every OTHER stamp on the card (not just nay/meh — the three
    //       verdicts too), so "asking again" always outdates a same-tick verdict rather than tying it.
    card.sc.hearted_at = this.Heard_react_at(w, card, ['nayed_at', 'mehed_at', 'already_had_at', 'offer_unsigned_at', 'landing_failed_at'])
    // the listing STARTS here (§1: acting is when it becomes yours to hold) with whatever the mirror card
    //  in hand already knows; the rest is cloned off the describe answer when it lands (Heard_clone_beat).
    //   Guarded stamps — an absent value would brand the snap {"undef":[…]}, the mint-bug law.
    if (rec.sc.title && !card.sc.title) { card.sc.title = this.Radio_clean(rec.sc.title) }
    if (rec.sc.artist && !card.sc.artist) { card.sc.artist = this.Radio_clean(rec.sc.artist) }
    card.bump()
    this.Heard_settle(w, me, 'heard_take')
    return 1

},
// Heard_forget — drop a set of keys off a Card without bumping (the caller bumps once).  ONLY Heard_untake
//  calls this now (2026-09-21): every AUTO writer above stopped stripping (Repli's law — a key cannot be
//   UN-set over the wire), so the one surviving deleter is the human's own explicit ✕, a local act with no
//    wire-replication claim on it.  Prefer deleting a key to setting it 0 (CLAUDE.md).
Heard_forget(card, keys) {
    let n = 0
    for (const k of keys) { if (card.sc[k] != null) { delete card.sc[k]; n = n + 1 } }
    return n

},
// Heard_untake — RETIRE THE HEART: the ✕, on a Haul row or on the keep the row became.  The Card stays
//  (you did hear the track, and the GC forgets it on the ordinary clock); what goes is the ask and
//   everything the ask accreted.  The machine never calls this — only a human does, which is the whole
//    content of "you can't lose a heart" (§C).
Heard_untake(w, me, pub, id) {
    let mag = this.Heard_mag_find(w, me)
    let card = mag ? this.Heard_find(mag, id, pub) : null
    if (!card || this.Heard_reaction(card) !== 'take') {
        // rung 3: the Haul row can surface a wish that lives only on a sibling's mirror (never adopted,
        //  e.g. nobody has hauled it yet) — adopt it here first, so the ✕ has something local to retire.
        //   A LOCAL-ONLY act still, same as the retired handoff lane's law: never travels back.
        let union = this.Heard_cards_union(w, me).find((c) => String(c.sc.id) === String(id) && String(c.sc.pub || '') === String(pub || ''))
        card = union ? this.Heard_adopt(w, me, union) : null
    }
    if (!card || this.Heard_reaction(card) !== 'take') { return 0 }
    this.Heard_forget(card, ['hearted_at', 'looked_at'])   // stop wanting it ⇒ stop nagging about it
    this.Heard_forget(card, ['already_had_at', 'offer_unsigned_at', 'landing_failed_at', 'why'])
    this.Heard_forget(card, this.Heard_listing_keys())
    card.bump()
    return 1

},
// ── ATTENTION (the owner 2026-09-10: *"we might like to get attention on the Haul cell when that
//  happens, or some new loved track info is synced over, but it should also ambiently work in the
//   background"*).  Design: Radio_circuit_todo §9.6.
//  AMBIENT MEANS THE CELL SIMPLY KNOWS.  No notification, no sound, no interruption.  Rebuilt 2026-09-21
//   (stamps not flags) as a PURE DERIVATION instead of an explicit `unseen` mark a writer had to remember
//    to set: `Heard_news_at` is the newest of the "look at this" event stamps, and news is simply that
//     stamp being newer than `looked_at` — so every writer above that already stamps its own event
//      (carried_at, landed_at, the three verdict `_at`s) needs no separate notice call any more.
//  ⚠ MARK IT WHERE THE TAKE WAS MADE, NOT WHERE THE WORK HAPPENED.  A landing on the laptop you are
//   sitting at is not news; the same landing seen from the phone is.  `pressed_on` is stamped by
//    Heard_adopt on a take that was CARRIED here from a sibling's mirror (rung 3) — so a pressed_on card
//     excludes EVERY outcome stamp from counting as news on ITSELF: `carried_at`/`landed_at` are this
//      body's OWN doing (Heard_haul_beat/Heard_land_beat stamp them right here), and the verdict trio
//       just as much.  Only that OTHER body — the presser, whose card has no pressed_on, reading this
//        one through its OWN mirror — is told.  Without the guard the device doing the work nags itself.
Heard_news_at(card) {
    if (!card) { return 0 }
    let best = 0
    let consider = (k) => { let at = +(card.sc[k] || 0); if (at > best) { best = at } }
    if (!card.sc.pressed_on) { consider('carried_at'); consider('landed_at'); consider('already_had_at'); consider('landing_failed_at'); consider('offer_unsigned_at') }
    return best
},
Heard_news(card) {
    let at = this.Heard_news_at(card)
    return (at && at > +(card.sc.looked_at || 0)) ? 1 : 0

},
// Heard_seen — one row was actually LOOKED AT.  ⚠ Per ROW, never per cell: clearing on the cell merely
//  being visible means a glance at a busy screen silently spends every mark, which is the same mistake
//   `Heard_through` avoids by asking whether a person is in the room rather than whether audio ran.
Heard_seen(w, me, pub, id) {
    let mag = this.Heard_mag_find(w, me)
    let card = mag ? this.Heard_find(mag, id, pub) : null
    if (!card || !this.Heard_news(card)) { return 0 }
    card.sc.looked_at = '' + this.Heard_now(w)
    card.bump()
    return 1

},
// Heard_unseen — what the Haul cell shows.  A COUNT, nothing more; the rows themselves say what changed
//  (Heard_word already turns a verdict into a sentence a person reads).
Heard_unseen(w, me) {
    let mag = this.Heard_mag_find(w, me)
    if (!mag) { return 0 }
    let n = 0
    for (const card of this.Heard_cards(mag)) { if (this.Heard_news(card)) { n = n + 1 } }
    return n

},
// Heard_taken — IS THIS TRACK ON MY LEDGER WITH THIS HOLDER?  The ♥ glyph reads the durable thing, not a
//  runtime mirror: a mirror dies with the process, so after a reload the heart went hollow while the ask
//   it stood for was still standing — the button saying the opposite of the truth.  Pure probe.
//    Rung 3: checks every sibling's mirror too — a heart pressed on the OTHER device still fills the glyph.
Heard_taken(w, me, pub, id) {
    for (const mag of this.Heard_mags(w, me)) {
        let card = this.Heard_find(mag, id, pub)
        if (card && this.Heard_reaction(card) === 'take') { return 1 }
    }
    return 0

},
// Heard_set — THE DEDUP SET the dial skips by: `{id:1}` over every Card, heard or taken, MINE OR ANY
//  SIBLING'S (rung 3 — a track my laptop already heard should not replay on my phone's dial either).
//   This is the `radio.c.heard` replacement, and the difference that matters is that it SURVIVES A
//    RELOAD and ages by `heard_ttl` rather than by a 100-entry cap — Mag_todo §8's "ABSOLUTELY durable …
//     keep OBLIQUE track of Records heard", finally by construction.
//  Rebuilt per call rather than cached: the set only changes when a Card mints or the GC drops one, and a
//   cache would need invalidating from three places to save a walk over a few hundred bare ids.  Callers
//    hoist it out of their own loops, exactly as they hoisted `radio.c.heard` before.
Heard_set(w, me) {
    let set = {}
    for (const c of this.Heard_cards_union(w, me)) { if (c.sc.id) { set[String(c.sc.id)] = 1 } }
    return set
},
//#endregion

// Heard_tally — WHAT THIS LISTENER'S HISTORY SAYS ABOUT EACH TRACK, as id → {score, why, …}.  The pool
//  steward's taste input (Ra_quarter_tally delegates here).  It read a %Jam ledger of %Spin/%Like/%Grab
//   rows until 2026-09-04 and scored Like 3 · Grab 2 · Spin 1; the same three signals live on the Card now
//    and keep the same weights, with one deliberate change:
//      the heart, CURRENTLY the reaction (Heard_reaction) → 3   — a decision outranks exposure, as before
//      `keep` (the original was materialised)   → 2   — they carried it: the old %Grab, by evidence
//      `played_through` (someone present)       → 1 each — the old %Spin, but ATTENTION rather than mere
//                                                    exposure: a bare hearing now scores ZERO.
//  That last is the point. A %Spin was "it streamed at you", which a radio does all day whether anyone is
//   in the room; `played_through` only moves when a person was there and let the track finish. Taste built
//    out of what a machine played to an empty kitchen was never taste.
Heard_tally(shelf) {
    let out = {}
    for (const c of this.Heard_cards_union_near(shelf)) {
        let id = String(c.sc.id || '')
        if (!id) { continue }
        let mire = +(c.sc.played_through || 0)
        let took = this.Heard_reaction(c) === 'take' ? 1 : 0
        let kept = c.sc.keep ? 1 : 0
        let why = took ? 'took it' : 'heard it'
        if (kept) { why = why + ' — carried' }
        if (mire) { why = why + ' — played through ' + mire }
        out[id] = { score: took * 3 + kept * 2 + mire, why: why, mire: mire, took: took, kept: kept, at: +(c.sc.hearted_at || 0) }
    }
    return out
},
// Heard_latest — the LAST SITTING's tracks, in the order they were heard, unioned across MY last
//  sitting and each sibling's own (rung 3 — each body's Cloud pages are its own sittings, so this is a
//   union of "the last page", not one shared page).  The old 'latest' pool policy read the last %Jam
//    session; a Cloud page IS a sitting, which is the same idea with a coordinate the model already
//     keeps, and it is clockless (page order, not a timestamp) so no fixture moves on it.
Heard_latest(shelf) {
    let out = []
    for (const mag of this.Heard_mags_near(shelf)) {
        let pages = mag.o({ Cloud: 1 })
        let last = pages[pages.length - 1]
        for (const c of (last ? last.o({ Card: 1 }) : [])) { if (c.sc.id && !out.includes(String(c.sc.id))) { out.push(String(c.sc.id)) } }
    }
    return out

},
//#region the query that IS the heist

// Heard_shelf — MY OWN STOCK SHELF, FOUND, never minted.  `Ra_home_self` is find-or-create and every
//  reader here is called from a face poll or a beat, so going through it would conjure the shelf it is
//   reporting on.
Heard_shelf(rw, me) {
    if (!rw || !me) { return null }
    let home = rw.o({ Mine: 1, pub: String(me) })[0]
    return home ? (home.o({ stock: 1, pub: String(me) })[0] || null) : null

},
// Heard_landed — IS THE THING ON MY SHELF?  Done-ness is derived, in one home: there is no `landed` key
//  on a Card, because "do I have it" is a question the collection answers however the track arrived (this
//   heist, a Cave of my crew, a pool press, a folder I copied in by hand).  A hook on any ONE of those
//    roads would leave the ask standing after the other four.
//  TWO ID-SPACES, so two probes (§4): the Mag `id` is the enid of the STREAMED rendition, while the
//   original the heist lands was materialised on their side under its own keep-id, which the Card learns
//    as `keep:` when the describe answers.  A pool press lands the streamed bytes; a heist lands the
//     original.  Either one answers the ask.
Heard_landed(shelf, card) {
    if (!shelf || !card) { return null }
    if (card.sc.id) {
        let hit = this.Ra_rec_find(shelf, { Record: 1, id: String(card.sc.id) })
        if (hit) { return hit }
    }
    if (card.sc.keep) { return this.Ra_rec_find(shelf, { Record: 1, id: String(card.sc.keep) }) }
    return null

},
// Heard_takes — WHAT I AM OWED, AND BY WHOM.  THE heist, as a query: `take` Cards not yet on my shelf,
//  oldest first, grouped by holder, one holder pulled at a time.  No operation particle survives this —
//   %Caper-as-ledger, %Pick-as-intent, %Jam/%Like/%Grab, %Provisions/%Want all dissolve into it.
//  A Card whose track is my OWN is never owed by anyone: liking your own music is a taste fact.
//  A Card wearing a verdict is ANSWERED (the wire replied and the reply was not the track), so it waits
//   for a human — a re-press or a ✕ — rather than re-asking forever behind the holder's one live keep.
//  Pure: `o` throughout, so a face may call it every poll.
Heard_holder_of(w, me, id) {
    if (!w || !w.o || !id) { return '' }
    let crew = null
    try { crew = this.Ra_pool_owner ? this.Ra_pool_owner(w).o({ Crew: 1 })[0] : null } catch (er) { crew = null }
    let mates = crew ? crew.o({ mate: 1 }).map((m) => String(m.sc.mate || '')) : []
    let hits = []
    for (const t of w.o({ Theirs: 1 })) {
        let pub = String(t.sc.pub || '')
        if (!pub || pub === String(me)) { continue }
        let stock = t.o({ stock: 1 })[0]
        if (!stock || !this.Ra_rec_find(stock, { Record: 1, id: String(id) })) { continue }
        hits.push(pub)
    }
    hits.sort((a, b) => {
        let ac = mates.some((m) => m && (a.startsWith(m) || m.startsWith(a))) ? 0 : 1
        let bc = mates.some((m) => m && (b.startsWith(m) || m.startsWith(b))) ? 0 : 1
        return ac - bc || (a < b ? -1 : 1)
    })
    return hits[0] || ''
},
// Heard_takes — WHAT I AM OWED, AND BY WHOM (rung 3: mine or any sibling's — a body with an empty own
//  Mag but a mirrored sibling still has wishes to carry).
Heard_takes(w, me, shelf) {
    let out = []
    let cards = []
    let holder = {}
    for (const c of this.Heard_cards_union(w, me)) {
        if (this.Heard_reaction(c) !== 'take' || !c.sc.id) { continue }
        let pub = String(c.sc.pub || '')
        if (pub === String(me)) { continue }
        if (this.Heard_landed(shelf, c)) { continue }
        // no holder on the card ⇒ whoever has it now (a pooled take): the %Theirs mirrors are local
        //  catalogs of every shelf shared with me, so this is a local lookup, crew first. Nobody has it
        //   ⇒ the card stands, exactly like a take whose holder is away.
        if (!pub) { pub = this.Heard_holder_of(w, me, String(c.sc.id)); if (!pub) { continue } }
        holder[String(c.sc.id)] = pub
        cards.push(c)
    }
    cards.sort((a, b) => (+(a.sc.hearted_at || 0)) - (+(b.sc.hearted_at || 0)))
    let rows = {}
    let order = []
    for (const c of cards) {
        let pub = String(c.sc.pub || holder[String(c.sc.id)] || '')
        if (!rows[pub]) { rows[pub] = { pub: pub, cards: [] }; order.push(pub) }
        rows[pub].cards.push(c)
    }
    for (const p of order) { out.push(rows[p]) }
    return out

},
// Heard_gave_up — 90 days and no holder has ever answered.  A STATE, not an exit: the Card is shown and
//  offers a ✕, and is never deleted by the machine.  "A take with no exit is immortality" and "you can't
//   lose a heart" reconciled — the clock can change what a heart SAYS, never whether it exists (§3).
Heard_gave_up(mag, card, now) {
    if (!card || this.Heard_reaction(card) !== 'take' || card.sc.keep) { return 0 }
    let ttl = (+((mag && mag.sc.take_ttl) || 90)) * 86400
    let at = +(card.sc.hearted_at || 0)
    return (at && (now - at) >= ttl) ? 1 : 0

},
// Heard_word — the ONE SHORT PHRASE a Haul row carries for a wish, decided here rather than in the face
//  (the Heist_keep_gist doctrine).  These are §C's words verbatim: a person meets these, never `played_through`.
Heard_word(mag, card, now) {
    if (!card) { return '' }
    let v = this.Heard_verdict(card)
    if (v === 'held') { return 'already had it' }
    if (v === 'unvouched') { return 'could not be verified' }
    if (v === 'landfail') { return 'failed' }
    if (this.Heard_gave_up(mag, card, now)) { return 'gave up' }
    if (card.sc.landed_at) { return 'landed' }
    if (card.sc.carried_by) { return 'handed to ' + String(card.sc.carried_by) }
    if (card.sc.waiting_for) { return 'waiting for ' + String(card.sc.waiting_for) }
    return 'waiting'

},
// Heard_gc — HOW IT FORGETS (§3).  One rule, run per page rather than per Card because a page IS the
//  sitting and carries the only `created_at` either needs:
//    drop  if  ¬take  ∧  created_at < now − heard_ttl
//    keep  if  take                     — a heart is never dropped by a clock
//  An emptied page goes with its last Card; the OPEN page never goes (it is this boot's sitting, and the
//   latch still points at it).  A page holding a take is pinned by construction — the model's "never drop
//    a Cloud any position sits on", where a want is a position, honoured without a re-mint.
//  Bound: (tracks heard in the last 30 days) + (hearts not yet landed).  Flat, no immortals.
Heard_gc(w, me, now) {
    let mag = this.Heard_mag_find(w, me)
    if (!mag) { return 0 }
    let ttl = (+(mag.sc.heard_ttl || 30)) * 86400
    let n = 0
    let pages = mag.o({ Cloud: 1 })
    for (const pg of pages) {
        let born = +(pg.sc.created_at || 0)
        if (!born || (now - born) < ttl) { continue }
        let goners = []
        for (const c of pg.o({ Card: 1 })) { if (this.Heard_reaction(c) !== 'take') { goners.push(c) } }
        for (const c of goners) { pg.drop(c); n = n + 1 }
        if (!pg.o({ Card: 1 }).length && pg !== mag.c.sitting) { mag.drop(pg) }
    }
    if (n) { mag.bump() }
    return n

},
// Heard_landed_cap — how many landed takes the pool's `recent` compartment may see.  A compartment
//  that could name three hundred tracks would only ever draw the same handful anyway.
//  (Restored 2026-09-17: e42be0a0 dropped the def and kept the caller below — every steward press
//   round on eed threw `Heard_landed_cap is not a function` for a night.)
Heard_landed_cap() {
    return 60
},
// Heard_keeps_cap — how many keeps the ♥ may have standing at once, across every holder.  Not a
//  transfer bound (that is `heist_inflight`, enforced in the beat): a bound on CELLS, so a take spree
//   does not open twelve of them.  Three is "a couple going, one queued".
//  (Restored 2026-09-17 evening — the SAME commit, e42be0a0, dropped this def too; its caller in
//   Heard_haul_beat stayed, so every haul beat threw `Heard_keeps_cap is not a function` and no heart
//    hauled at all — MusuHeard's step 7 carried the Err row.  `Heard_thumb` went in that commit as well,
//     and that one was deliberate: a later press re-affirms, there is no un-press window any more.)
Heard_keeps_cap() {
    return 3
},
// Heard_landed_ids — MY LANDED TAKES, newest first: the pool's `recent` compartment input (the one pool
//  source that CHOOSES nothing, because the choosing happened when you took the track).  This replaces
//   the dontSnap %Hauls/%Newly/%Fresh mirror, which existed only because the disk's arrivals ledger is a
//    read and the goal builder is synchronous — the Mag is both durable and in memory, so no mirror.
Heard_landed_ids(w, me, shelf) {
    let out = []
    if (!shelf) { return out }
    let cards = []
    for (const c of this.Heard_cards_union(w, me)) {
        if (this.Heard_reaction(c) !== 'take' || !c.sc.id) { continue }
        // a heart on a track of MY OWN is a taste fact, not an acquisition — nothing was acquired, so it
        //  is not "what came in lately" and the pool's recent compartment must not draw it (the same
        //   exclusion Heard_takes makes for the same reason: nobody was ever owed it).
        let pub = String(c.sc.pub || '')
        if (pub === String(me)) { continue }
        let got = this.Heard_landed(shelf, c)
        if (!got) { continue }
        cards.push({ card: c, id: String(got.sc.id || c.sc.id) })
    }
    cards.sort((a, b) => (+(b.card.sc.hearted_at || 0)) - (+(a.card.sc.hearted_at || 0)))
    for (const e of cards.slice(0, this.Heard_landed_cap())) { out.push(e.id) }
    return out
},
//#endregion

//#region the seam to the byte lane — one keep per holder, and what it writes back

// Heard_keep — mint the `%Heist` one take has earned.  Radio_keep's shape minus everything that belongs
//  to a GESTURE: no `last_touch` (this keep must not steal focus off whatever the human is looking at),
//   no glass pop, no feebly_ponder — the beat that called this is already awake.  `take:1` is the tell
//    that makes it start itself and take ONE track (Heist_keep_take_go).
//  The keep is SCAFFOLDING: it exists only while a Card is being carried, and the Card is the ledger.
Heard_keep(w, rw, shop, dj, rec) {
    let seed = String(rec.sc.id || '')
    if (!seed || !shop) { return null }
    if (shop.o({ Heist: 1, seed: seed })[0]) { return null }
    let keep = shop.i({ Heist: this.Radio_clean(rec.sc.title || 'this'), seed: seed, pub: String(dj), state: 'primed', take: 1 })
    keep.c.up = shop
    this.Heist_keep_born(keep, this.Heard_now(w))
    let nice = this.Radio_friendly ? this.Radio_friendly(rw, dj) : ''
    if (nice) { keep.sc.from_name = nice }
    if (rec.sc.artist) { keep.sc.artist = this.Radio_clean(rec.sc.artist) }
    let dflt = this.Heist_defaults_get ? this.Heist_defaults_get() : {}
    if (dflt && dflt.lofi) { keep.sc.lofi = 1 }
    keep.bump()
    console.log('♥⇊ hauling ' + String(keep.sc.Heist).slice(0, 32) + ' from ' + (nice || String(dj).slice(0, 8)))
    return keep

},
// Heard_clone_beat — WHAT THE WIRE ANSWERS, WRITTEN BACK ONTO THE CARD.  Two things land here, both of
//  them facts only the holder could supply:
//   THE LISTING — the describe answers with the ORIGINAL's own head (`%Record,re:<streamed id>`, §4's
//    two id-spaces), carrying the real `total`, `path` and `body_hash`.  Cloning it onto the Card is what
//     makes the Card self-describing: everything a later reader needs for the way back rides the line —
//      `pub` is the Pier, `id` is the content hash, `keep` is the original's own id on their side.
//   THE VERDICT — `held` (you already had it by artist+title), `unvouched` (the offer's signature did not
//    verify), `landfail,why` (three throws on the landing).  The engine stamps these on the JOB, which
//     FLATTENS when the keep finishes, so nothing durable would remember them; and each of them removes
//      the husk from the mirror, which leaves the keep pulling something that is no longer there — the
//       holder's one live slot wedged forever.  So: copy the verdict onto the Card (as a TIMESTAMP,
//        Heard_verdict_field) and END the keep, and the queue moves on.  A re-press of ♥ OUTDATES the
//         verdict by comparison and asks again (Heard_take, Heard_verdict) — nothing is cleared here.
//  Runs at the top of every haul beat.  Pure `o` until something has actually landed.  Neither branch below
//   calls a separate "mark it unseen" any more — Heard_news derives that off the very stamps written here.
Heard_clone_beat(w, rw, me, shop) {
    if (!shop || !rw || !me) { return 0 }
    let mag = this.Heard_mag_find(rw, me)
    if (!mag) { return 0 }
    let n = 0
    for (const keep of shop.o({ Heist: 1 })) {
        if (!keep.sc.take) { continue }
        let seed = String(keep.sc.seed || '')
        let dj = String(keep.sc.pub || '')
        if (!seed || !dj) { continue }
        let card = this.Heard_find(mag, seed, dj)
        if (!card) { continue }
        let mir = rw.o({ Theirs: 1, pub: dj })[0]
        let mirstock = mir ? mir.o({ stock: 1, pub: dj })[0] : null
        let head = mirstock ? this.Ra_rec_find(mirstock, { Record: 1, re: seed }) : null
        if (head) { n = n + this.Heard_clone_head(card, head) }
        let v = this.Heard_verdict_of(this.Heist_job_of ? this.Heist_job_of(shop, keep) : null)
        if (v && String(keep.sc.state || 'primed') !== 'done') {
            card.sc[this.Heard_verdict_field(v.key)] = '' + this.Heard_now(w)
            if (v.why) { card.sc.why = String(v.why).slice(0, 120) }
            card.bump()
            keep.sc.state = 'done'
            keep.bump()
            try { this.Heist_job_drop(shop, keep) } catch (er) {}
            console.log('♥⚠ ' + String(card.sc.title || seed).slice(0, 32) + ' — ' + this.Heard_word(mag, card, this.Heard_now(w)))
            n = n + 1
        }
    }
    return n

},
// Heard_clone_head — the listing, off the original's head, guarded key by key (an absent value would
//  brand the snap {"undef":[…]}).  `bytes` is the head's promised `total` under the name a human reads;
//   `keep` is the original's id on their side, which is also the second probe Heard_landed makes.
Heard_clone_head(card, head) {
    if (!card || !head) { return 0 }
    let n = 0
    let put = (k, v) => { if (v != null && v !== '' && String(card.sc[k] || '') !== String(v)) { card.sc[k] = String(v); n = n + 1 } }
    put('title', head.sc.title ? this.Radio_clean(head.sc.title) : '')
    put('artist', head.sc.artist ? this.Radio_clean(head.sc.artist) : '')
    put('dir', head.sc.dir)
    put('path', head.sc.path)
    put('bytes', head.sc.total)
    put('body_hash', head.sc.body_hash)
    put('keep', head.sc.id)
    if (n) { card.bump() }
    return n

},
// Heard_verdict_of — the job's answered-but-not-landed row, as {key, tune, why}.  A ♥ keep is soloed to
//  ONE pick, so a job carries at most one of these and the first hit is unambiguous.
Heard_verdict_of(job) {
    if (!job) { return null }
    let h = job.o({ held: 1 })[0]
    if (h) { return { key: 'held', tune: String(h.sc.tune || '') } }
    let u = job.o({ unvouched: 1 })[0]
    if (u) { return { key: 'unvouched', tune: String(u.sc.tune || '') } }
    let l = job.o({ landfail: 1 })[0]
    if (l) { return { key: 'landfail', tune: String(l.sc.tune || ''), why: String(l.sc.why || '') } }
    return null

},
// Heard_land_beat — STAMP `landed_at` the moment a taken card's track actually turns up on MY OWN
//  shelf (rung 3).  Heard_landed is a live derived check this body can always make for itself (it has
//   the shelf right there) — but under the mirror model there is no separate ack any more, so the stamp
//    itself is what tells EVERY OTHER body, reading this card only through a mirror, that it finished.
Heard_land_beat(w, rw, me, shelf) {
    if (!shelf) { return 0 }
    let mag = this.Heard_mag_find(rw, me)
    if (!mag) { return 0 }
    let n = 0
    for (const card of this.Heard_cards(mag)) {
        if (this.Heard_reaction(card) !== 'take' || card.sc.landed_at) { continue }
        if (!this.Heard_landed(shelf, card)) { continue }
        card.sc.landed_at = '' + this.Heard_now(w)
        card.bump()
        this.Heard_settle(w, me, 'heard_landed')
        n = n + 1
    }
    return n

},
// Heard_haul_beat — THE PASS.  Clone first (an answer must be written down before anything is decided on
//  it), then AT MOST ONE MINT: a take spree should open one cell now and the next when that one is done,
//   not twelve at once.  A holder already carrying a live keep is skipped entirely — that is the per-Pier
//    serialisation the whole shape exists for.
//  A take whose record is not in the friend's mirror is LEFT STANDING rather than dropped: they are away,
//   or the mirror was swept between sessions.  The ask outliving the session is the point of a ledger.
//   `ident` now feeds `carried_by` (rung 3, below) instead of the retired handoff-lane ack — the ONLY way
//    a card tells anyone else, reading it through a mirror, whose keep is carrying it.
async Heard_haul_beat(w, rw, me, nav, shop, ident) {
    if (!nav || !rw || !me || !shop) { return 0 }
    this.Heard_clone_beat(w, rw, me, shop)
    this.Heard_land_beat(w, rw, me, this.Heard_shelf(rw, me))
    let live = 0
    for (const k of shop.o({ Heist: 1 })) { if (String(k.sc.state || 'primed') !== 'done') { live = live + 1 } }
    if (live >= this.Heard_keeps_cap()) { return 0 }
    let mine = this.Heard_shelf(rw, me)
    for (const row of this.Heard_takes(rw, me, mine)) {
        // ADOPT FIRST, ALWAYS — BEFORE the busy gate (rung 3): `row.cards` came off the UNION
        //  (Heard_takes), so a wish this trove body sees may still live only on a sibling's mirror.  A
        //   second wish from a holder already busy with another download must still be CLAIMED and
        //    gossiped back right away — only the KEEP has to wait its turn, not the card write.  Everything
        //     below — the waiting_for word, the verdict check, the keep itself — lands on MY OWN card
        //      (Heard_clone_beat only ever finds a card on `Heard_mag_find(rw,me)`), never the mirror copy.
        let cards = row.cards.map((c) => this.Heard_adopt(w, me, c)).filter((c) => c)
        let busy = 0
        for (const k of shop.o({ Heist: 1, pub: row.pub })) { if (String(k.sc.state || 'primed') !== 'done') { busy = 1 } }
        if (busy) { continue }
        let mir = rw.o({ Theirs: 1, pub: row.pub })[0]
        let mirstock = mir ? mir.o({ stock: 1, pub: row.pub })[0] : null
        if (!mirstock) {
            // THE HOLDER'S MIRROR HAS NOT STOOD YET — away, or swept between sessions.  Used to leave the
            //  take standing with no word at all; now it says WHY it is standing, cleared the moment the
            //   mirror shows up (below).
            let name = this.Radio_friendly ? this.Radio_friendly(rw, row.pub) : String(row.pub).slice(0, 8)
            let word = name + ' to come online'
            for (const card of cards) {
                if (this.Heard_verdict(card)) { continue }
                if (card.sc.waiting_for !== word) { card.sc.waiting_for = word; card.bump() }
            }
            continue
        }
        for (const card of cards) { if (card.sc.waiting_for) { delete card.sc.waiting_for; card.bump() } }
        for (const card of cards) {
            let id = String(card.sc.id || '')
            if (!id || this.Heard_verdict(card)) { continue }
            if (shop.o({ Heist: 1, seed: id })[0]) { continue }
            let rec = this.Ra_rec_find(mirstock, { Record: 1, id: id })
            if (!rec) { continue }
            if (this.Heard_keep(w, rw, shop, row.pub, rec)) {
                // "carried_by = which body's Card has a keep" (SoundPooling_todo §0.0 rung 3): the only
                //  way anyone ELSE, reading this card through a mirror, learns someone is working on it —
                //   there is no separate ack any more, the gossip mile carries the stamp itself.
                if (ident) {
                    let myname = String(ident.sc.friendly || 'a linked device')
                    if (String(card.sc.carried_by || '') !== myname) { card.sc.carried_by = myname; card.sc.carried_at = '' + this.Heard_now(w); card.bump() }
                }
                return 1
            }
        }
    }
    return 0

},
// Heard_haul_piers — THE TAKE, GROUPED BY WHO IS BRINGING IT (the owner 2026-09-04: "they have to be per
//  Pier").  One row per holder I have anything from: the keeps actually running, in the beat's own order
//   so the list cannot claim an order the machine does not have, and under them the wishes still waiting
//    — the hearts that have not been carried yet, which used to be invisible entirely.
//  Pure: `o` throughout, so a face may call it every poll.  A holder with neither is not a row.
Heard_haul_piers(rw, me) {
    let out = []
    if (!rw || !me) { return out }
    let mag = this.Heard_mag_find(rw, me)
    let now = this.Heard_now(rw)
    let rows = {}
    let order = []
    let at = (dj) => {
        if (!rows[dj]) { rows[dj] = { dj: dj, name: (this.Radio_friendly ? this.Radio_friendly(rw, dj) : String(dj).slice(0, 8)), keeps: [], waiting: [] }; order.push(dj) }
        return rows[dj]
    }
    for (const keep of this.Heist_live_rows(rw)) {
        // a POOL keep is the circulation fill's machinery, not something this person is bringing me (the owner
        //  2026-09-06: "a Venus trying to download I never clicked on") — the Pocket cell owns its legibility.
        if (this.Pool_is_machinery(keep)) { continue }
        if (String(keep.sc.state || 'primed') === 'done') { continue }
        // a keep with no `pub` has no holder to be a row of — it would open a nameless pier headed by the
        //  first 8 characters of nothing.  Every minted keep carries one; this is the guard, not a case.
        let dj = String(keep.sc.pub || '')
        if (!dj) { continue }
        at(dj).keeps.push(keep)
    }
    for (const row of this.Heard_takes(rw, me, this.Heard_shelf(rw, me))) {
        let r = at(row.pub)
        for (const card of row.cards) {
            if (this.Heard_carrying(r, String(card.sc.id || ''))) { continue }
            r.waiting.push({ card: card, of: String(card.sc.id || ''), title: String(card.sc.title || card.sc.id || ''), word: this.Heard_word(mag, card, now) })
        }
    }
    for (const dj of order) { out.push(rows[dj]) }
    return out

},
// Heard_carrying — is this holder's row already showing that track as a running keep?  A wish being
//  carried right now is not ALSO waiting, and saying it twice is how a list of four reads as a list of
//   eight (the same double-count the pool cap had to learn about intent vs sediment).
Heard_carrying(row, of) {
    if (!of) { return 0 }
    for (const keep of row.keeps) { if (String(keep.sc.seed || '') === of) { return 1 } }
    return 0
},
//#endregion


//#region THE HANDOFF — RETIRED 2026-09-21 (rung 3, SoundPooling_todo §0.0).  Thursday (Radio_circuit_todo
//  §7.5, ruled 2026-09-05) built a point-to-point relay — one `take` frame to the first trove sibling, a
//   `take_got` ack back — because a body with no folder had no other way to be seen.  The mirror mile
//    (Heard_gossip_beat above, Swarm_heard_mirror in Swarm.g) makes that unnecessary: EVERY sibling now
//     sees my whole heard Mag, so nobody needs to be SENT anything, and a trove body finding a wish it
//      does not itself carry just adopts it (Heard_adopt) and hauls — the ordinary gossip mile carries
//       the outcome back with no separate ack.  `a_folder_hands_nothing` stays true: nobody hands, ever;
//        there is simply nothing left TO hand.  `Heard_hand_myaddr` survives — the gossip mile's `from`.
//         `Heard_trove_siblings` survives too, PURELY INFORMATIONAL now (RadioFace's heart-settings sheet
//          names who might pick a wish up — "your linked device fetches it — Laptop"); nothing routes
//           off it any more.
Heard_hand_myaddr(ident) {
    let mine = this.Swarm_body_mine ? this.Swarm_body_mine(ident) : null
    return mine ? String(this.Swarm_body_addr(mine) || ident.sc.prepub) : String(ident.sc.prepub)
},
Heard_trove_siblings(ident) {
    let out = []
    if (!ident || !this.Swarm_body_roster) { return out }
    let mineaddr = this.Heard_hand_myaddr(ident)
    for (const b of this.Swarm_body_roster(ident)) {
        let addr = this.Swarm_body_addr(b)
        if (!addr || addr === mineaddr) { continue }
        if (!this.Swarm_organ_of(b, 'trove')) { continue }
        out.push({ body: b, addr: addr, name: String(b.sc.name || b.sc.post || 'a linked device') })
    }
    return out
},
//#endregion
// ── HEART-SETTINGS (the owner 2026-09-05: *"long-press the heart to open heart-settings"*) — the roads a ♥
//  can take on THIS device, and the one-time tip, as scalars on the heard Mag (durable, stashed with the
//   eighth pillar, per person — never re-asked across reloads or devices):
//    tipped      the sheet has shown itself once, after the first ♥ ever; long-press is the way back
//    no_handoff  the person switched the "share my hearts with my linked devices" road OFF (rung 3:
//                 gates the WHOLE mirror mile, Heard_gossip_beat — no device sees any of my wishes)
//  Absent = default (on / not yet tipped), so a snap stays clean.  A face reads these through the two
//   probes and writes through the two setters; nothing else touches them.
Heard_tipped(w, me) {
    let mag = this.Heard_mag_find(w, me)
    return mag && mag.sc.tipped ? 1 : 0
},
Heard_tip(w, me) {
    let mag = this.Heard_mag(w, me)
    if (!mag) { return 0 }
    if (!mag.sc.tipped) { mag.sc.tipped = '1'; mag.bump() }
    return 1
},
Heard_hand_on(w, me) {
    let mag = this.Heard_mag_find(w, me)
    return mag && mag.sc.no_handoff ? 0 : 1
},
Heard_hand_set(w, me, on) {
    let mag = this.Heard_mag(w, me)
    if (!mag) { return 0 }
    if (on) { if (mag.sc.no_handoff) { delete mag.sc.no_handoff; mag.bump() } } else { if (!mag.sc.no_handoff) { mag.sc.no_handoff = '1'; mag.bump() } }
    return 1
},
//#endregion


    })
    })
</script>
