services:
  jamsend-dev:
    image: oven/bun:latest
    container_name: jamsend-dev
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "9091:9091"
    command: bun run dev --host 0.0.0.0 --port 9091
    tty: true
    stdin_open: true
