# VM-specific overrides for jamsend-supervisor.conf
# Loaded second (with EnvironmentFile=-) so these values win
#
# Chrome profiles live on the VM's qcow2 disk so they're captured in snapshots.
# The host path from jamsend-supervisor.conf is a different machine entirely.
PROFILE_BASE=/home/jamsend/.chrome-profiles
