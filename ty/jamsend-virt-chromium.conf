TARGET_URL=https://jamsend.duckdns.org:9999/
# Comma-separated profile names; append :path-suffix when needed, e.g. private:/stream
# Ports are auto-assigned sequentially from CHROME_DEBUG_PORT_BASE
CHROME_PROFILES=public,private,tyrant
CHROME_DEBUG_PORT_BASE=9222
# profiles live on the VM disk so they're captured in snapshots
PROFILE_BASE=/home/jamsend/.chrome-profiles